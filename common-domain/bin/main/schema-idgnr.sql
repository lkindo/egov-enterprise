-- 전자정부 표준 ID 생성 서비스를 위한 테이블
CREATE TABLE IDS (
    TABLE_NAME VARCHAR(20) PRIMARY KEY,
    NEXT_ID NUMERIC(30) NOT NULL
);

-- 초기 시퀀스 값 설정
INSERT INTO IDS (TABLE_NAME, NEXT_ID) VALUES ('BBS_ID', 1);
INSERT INTO IDS (TABLE_NAME, NEXT_ID) VALUES ('FILE_ID', 1);
INSERT INTO IDS (TABLE_NAME, NEXT_ID) VALUES ('USER_ID', 1);
