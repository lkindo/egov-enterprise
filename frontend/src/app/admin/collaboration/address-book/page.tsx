'use client';

import React from 'react';
import CollaborationHubClient from '../CollaborationHubClient';

export default function AddressBookPage() {
 return <CollaborationHubClient defaultTab="ADDRESS_BOOK" />;
}
