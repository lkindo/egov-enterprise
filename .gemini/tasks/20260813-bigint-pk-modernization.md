# BIGINT 자동 PK 현대화 실행 원장

> 상태: L2 설계결정 승인 후 실행 중
> 승인 근거: 사용자는 비운영 시스템임을 전제로 수동 PK가 필요한 항목은 유지하고, 내부 기술 PK는 `BIGINT IDENTITY`로 전환하며 기존 데이터도 일괄 변환하는 방향의 계획 수립과 순차 실행을 명시했다.
> 실행 원칙: 공유/운영 DB에는 직접 DDL/DML을 실행하지 않는다. 저장소 Flyway와 일회성 PostgreSQL 17 검증 DB에서만 전환을 검증한다.

## 1. 물리 스키마 실측

- 실측 DB: 로컬 프로젝트 E2E PostgreSQL 17.9 (`egov-e2e-postgres`, `127.0.0.1:15432`)
- 실측 방법: `.agent/scripts/db-bridge.js`의 SELECT-only/read-only 연결
- 실측 시점 Flyway: rank 34, versioned migration 최신 적용본 `V2_31`, 실패 0
- 주의: 저장소 최신은 `V2_48`이므로 이 DB는 최신 구조 검증원이 아니라 기존 데이터·타입·FK census 증거다. 최종 정합은 빈 PostgreSQL 17에 전체 Flyway를 적용하는 `schemaValidationTest`로 판정한다.
- 대상 64개 엔티티가 매핑하는 물리 테이블은 모두 존재했다. 63개는 수동 문자열/복합 PK이고, `tb_menu_info.menu_sn`만 이미 `BIGINT`였다.
- 대상 테이블의 정확한 행 수 합계는 4,272행이다. 최다는 `tb_web_log` 4,003행이며, 44개 BIGINT 전환 후보 중 다수는 0행이다.
- 주요 참조 위험: `tb_file_master.atch_file_id` inbound FK 14개, `tb_user_info.esntl_id` 10개, `tb_bbs_master.bbs_id` 5개, `tb_bbs_item.pst_id` 3개다.
- 예외: `SurveyRespondent` 엔티티는 수동 단일 PK처럼 보이지만 실제 `tb_srvy_rspdnt` PK는 `(srvy_tmplt_id, srvy_id, srvy_rspdnt_id)` 3컬럼 복합키다. 선행 매핑 정합성 검토 없이 자동키로 바꾸지 않는다.
- 중복 매핑: `DeptManage`와 `OrganizationManage`가 모두 `tb_ognz_info.ognz_id`를 사용한다. 두 엔티티를 하나의 물리 PK 변경으로 함께 다뤄야 한다.

## 2. 표준 메타 판정

- `meta_standard_words`: 주소록=`ADBK`, 회원=`MBR`, 일련번호=`SN`이 대표어로 존재한다.
- `meta_standard_domains`: 기존 숫자 일련번호는 `일련번호N10`/`일련번호N22`이며 각각 `NUMERIC`이다.
- 자동 내부키는 PostgreSQL `BIGINT`이므로 `_id` 문자열 용어를 그대로 타입만 변경하지 않는다. `BIGINT`용 일련번호 도메인과 `_sn` 용어를 메타에 먼저 등록하고 물리 컬럼·JPA·API 명칭을 함께 맞춘다.

## 3. 64개 엔티티 분류

### 3.1 수동 자연키·공유키 유지 (20 엔티티 / 19 테이블)

다음 키는 외부 코드, 보안 주체, 카탈로그 코드, 공유 PK/FK 또는 복합 연관키이므로 문자열 수동키를 유지한다.

`AdministCode`, `Authority`, `BoardMaster`, `BoardMasterOption`, `CommonCodeCategory`, `CommonCodeGroup`, `DeptManage`, `GroupManage`, `InstitutionCode`, `LoginPolicy`, `OrganizationManage`, `Program`, `RefreshToken`, `RoleInfo`, `SurveyRespondent`, `SystemPolicy`, `Template`, `User`, `UserAbsence`, `UserAuthority`

- `Template` 재판정(2026-08-13): `TMPLT_HUB`·`TMPLT_LIST`·`TMPLT_QNA` 등 값 자체가 프론트 레이아웃 분기와 게시판 카탈로그 계약이다. 물리 FK는 0이지만 E2E DB의 `tb_bbs_master.tmplt_id` 논리 참조 8행과 코드 상 다수 참조가 있어 내부 기술키가 아닌 자연키로 유지한다.

### 3.2 이미 BIGINT, 생성전략만 보정 (1 엔티티)

- `Menu`: `tb_menu_info.menu_sn BIGINT`이지만 identity/default가 없고 JPA도 수동 PK다. 시드와 부모 FK를 보존하면서 별도 단계에서 DB 생성 기본값과 `@GeneratedValue`를 부여한다.

### 3.3 BIGINT 자동 내부키 전환 (43 엔티티)

`AddressBook`, `AddressBookUser`, `Banner`, `Blog`, `Board`, `Community`, `DeptJob`, `DeptJobBox`, `Diary`, `DtaUseStats`, `EventInfo`, `FileMaster`, `Hpcm`, `InformalSanction`, `InternetSvcGuidance`, `LoginLog`, `MemoReport`, `MyPageContent`, `Note`, `NoteRecptn`, `NoteTrnsmit`, `Notification`, `OnlineManual`, `OnlinePollArticle`, `OnlinePollManage`, `OnlinePollResult`, `Popup`, `PrivacyLog`, `ReprtStats`, `RewardManage`, `Schedule`, `Scrap`, `SentMail`, `SiteMap`, `Sms`, `SurveyArticle`, `SurveyInfo`, `SurveyQuestion`, `SurveyResult`, `SurveyTemplate`, `SysLog`, `WebLog`, `WorkReport`

## 4. 실행 순서

1. **Wave 1 — AddressBook 파일럿**: 외부 inbound FK 0, E2E 데이터 각 1행. `adbk_id`→`adbk_sn`, `adbk_constnt_id`→`adbk_mbr_sn`, 자식 FK를 명시하고 DB/JPA/API/FE를 한 폐포로 전환한다.
2. **Wave 2 — 무참조·0행 단독 테이블**: Hpcm·InternetSvcGuidance·OnlineManual·Popup(완료), MyPageContent, DeptJobBox 등. Template은 자연키로 재분류해 제외.
3. **Wave 3 — 단일 FK 업무 테이블**: Banner, DeptJob, Diary, DtaUseStats, MemoReport, RewardManage, Schedule, Scrap, SentMail, WorkReport 등.
4. **Wave 4 — 묶음형 도메인**: Note 3종, OnlinePoll 3종, Survey 5종, Board/Blog/Community.
5. **Wave 5 — 고참조·고용량**: FileMaster와 로그 3종. FK 14개 및 쓰기량/배치 성능을 별도로 검증한다.
6. **Wave 6 — Menu 생성전략 보정과 예외 정리**: 기존 BIGINT 시드 충돌 방지, SurveyRespondent 복합키 매핑 정합, 린터 베이스라인 최종 축소.

## 5. 각 Wave 공통 완료 조건

- Expand: 신규 `_sn BIGINT GENERATED BY DEFAULT AS IDENTITY` 및 참조 `_sn` 컬럼 추가
- Sync: 기존 행과 FK를 결정적으로 백필하고 null/고아 0건 검증
- Redirect: JPA `Long` + `GenerationType.IDENTITY`, repository/service/controller/DTO/OpenAPI/frontend 소비자를 모두 이관
- Contract: 기존 문자열 기술키와 수동 채번 제거, 사유가 적힌 `-- linter:ignore`를 파괴 DDL 한 줄에만 적용
- 검증: 관련 단위/계약 테스트, `harnessTest`, `schemaValidationTest`, 백엔드 compile, 프론트 codegen/tsc, 필요 시 전체 `localGate`
- 가드레일: `PkGenerationStandardLinterTest.GRANDFATHERED`는 실제 전환이 끝난 엔티티만 사유와 함께 제거하고 baseline integrity manifest를 같은 변경에서 갱신한다.

## 6. 현재 진행

- [x] 64개 엔티티-테이블-PK 타입-FK census
- [x] 대상 테이블 exact row count 및 PK identity/default census
- [x] 표준 메타 단어·용어·도메인 조회
- [x] 20 유지 / 1 보정 / 43 전환 분류(Template 자연키 재판정 반영)
- [x] Wave 1 AddressBook RED 기준선 및 Flyway 작성
- [x] Wave 1 DB/JPA/API/FE 전환
- [x] Wave 1 PostgreSQL 17 + 계약/회귀 검증
- [x] Wave 2 순차 실행 (`Hpcm`·`InternetSvcGuidance`·`OnlineManual`·`Popup`·`MyPageContent`·`DeptJobBox` 완료, `Template` 자연키 유지 재판정)
- [ ] Wave 3 이후 순차 실행 (`Banner`·`DeptJob`·`Diary` 완료, 다음 `DtaUseStats`)

### Wave 1 검증 증거

- 변경 전 주소록 도메인 테스트 21개: 실패 0
- 변경 후 주소록 도메인 테스트 21개: 실패 0
- `AddressBookApiControllerTest` 4개: 실패 0
- PostgreSQL 17 전체 Flyway + Hibernate validate + 쓰기 스모크 3개: 실패 0
- V2_48 구형 행 → V2_49 백필 회귀 테스트: 주소록/회원 행 보존, 신규 PK/FK 동일, 구 컬럼 제거, identity와 `sq_` 시퀀스 명명, 고아 FK 차단 확인
- 하네스: PK 생성, baseline integrity, zero-downtime, schema naming, entity-schema, unique mirror, OpenAPI path/field 8종 실패 0
- 프론트: OpenAPI TypeScript/Zod 재생성, `tsc --noEmit` 성공, 주소록 서비스 포함 Vitest 3개 성공
- mutation 증거: V2_49 백필 조인을 `WHERE FALSE`로 변조하자 `tb_adbk_info bigint key backfill failed`로 테스트 red; 원복 후 green

### Wave 2-1 Hpcm (`V2_50`) — 2026-08-13

- `hlp_id VARCHAR(20)` 수동 문자열 PK → `hlp_sn BIGINT GENERATED BY DEFAULT AS IDENTITY`
- 실측 위험도: 기존 E2E 0행, inbound FK 0. API/프론트 계약은 `hlpId/hpcmId:string` → `hlpSn:number`로 원자적 이관
- 사용자 승인 예외: 이번 PK 현대화에 한해 DB 헌법의 기존 `HLP_SN → 일련번호N22/NUMERIC` 매핑과 단일 릴리스 Contract 제약을 예외 처리. 헌법 문서 자체는 미수정
- 회귀 검증: V2.49 구형 문자열 행 주입 → V2.50 적용 후 데이터 보존, 구 컬럼 제거, PK=`hlp_sn`, identity=`BY DEFAULT`, 시퀀스=`sq_hlp_sn`, 신규 행 자동 증가 확인
- 계약 검증: OpenAPI 재생성 후 TypeScript/Zod codegen 결정성, 프론트 `tsc --noEmit`, 대상 ESLint 통과
- 전체 게이트: harness 29 suites/36 tests, schema validation 4 suites/6 tests — 실패·오류·skip 0
- mutation 증거: V2.50의 identity 선언을 일반 BIGINT로 변조하자 `tb_hlp_info.hlp_sn backfill failed`로 RED; 원복 후 동일 회귀 테스트 GREEN

### Wave 2-2 InternetSvcGuidance (`V2_51`) — 2026-08-13

- `itnt_svc_id VARCHAR(20)` 수동 문자열 PK → `itnt_srvc_sn BIGINT GENERATED BY DEFAULT AS IDENTITY`
- 신규 PK 명칭은 표준 단어 `인터넷(ITNT)+서비스(SRVC)+일련번호(SN)`를 사용한다. 기존 `svc` 비-PK 컬럼은 이번 PK 전환 범위에서 보존
- 실측 위험도: 기존 E2E 0행, inbound FK 0, 전용 프론트 소비자 0. `/admin/system/ism`은 별도 InformalSanction 도메인으로 제외
- API 계약: `intnetSvcId:string` → `itntSrvcSn:number`, 등록 응답은 DB 생성 일련번호 반환
- 회귀 검증: V2.50 구형 문자열 행 주입 → V2.51 적용 후 데이터 보존, 구 컬럼 제거, PK=`itnt_srvc_sn`, identity=`BY DEFAULT`, 시퀀스=`sq_itnt_srvc_sn`, 신규 행 자동 증가 확인
- mutation 증거: V2.51 identity 선언을 일반 BIGINT로 변조하자 `tb_intrn_svc.itnt_srvc_sn backfill failed`로 RED; 원복 후 동일 회귀 테스트 GREEN
- 계약·전체 게이트: OpenAPI/TypeScript/Zod 결정성·프론트 type-check 통과, harness 29 suites/36 tests 및 schema validation 5 suites/7 tests 실패·오류·skip 0

### Wave 2-3 OnlineManual (`V2_52`) — 2026-08-13

- `onln_mnl_id VARCHAR(20)` 수동 문자열 PK → `onln_mnl_sn BIGINT GENERATED BY DEFAULT AS IDENTITY`
- 신규 PK 용어 `온라인매뉴얼일련번호(ONLN_MNL_SN)`를 프로젝트 `일련번호N19` 도메인에 등록
- 실측 위험도: 기존 E2E 1행, inbound FK 0. 구 PK에 중복으로 걸린 `uk_tb_onln_mnl_info_onln_mnl_id`도 Contract 단계에서 제거
- API/프론트/E2E 계약: `onlnMnlId:string` → `onlnMnlSn:number`, 등록 응답은 DB 생성 일련번호 반환
- 회귀 검증: V2.51 구형 문자열 행 주입 → V2.52 적용 후 데이터·비PK 필드 보존, 구 컬럼/중복 UNIQUE 제거, PK=`onln_mnl_sn`, identity=`BY DEFAULT`, 시퀀스=`sq_onln_mnl_sn`, 신규 행 자동 증가 확인
- mutation 증거: V2.52 identity 선언을 일반 BIGINT로 변조하자 `tb_onln_mnl_info.onln_mnl_sn backfill failed`로 RED; 원복 후 동일 회귀 테스트 GREEN
- 계약·전체 게이트: OpenAPI/TypeScript/Zod 결정성, 프론트·E2E type-check와 대상 ESLint 통과. harness 29 suites/36 tests 및 schema validation 6 suites/8 tests 실패·오류·skip 0
- 하네스 정밀화: 과거 마이그레이션의 `ADD UNIQUE`만 누적하던 `UniqueConstraintMirrorLinterTest`가 V2.52에서 삭제된 구 PK 컬럼 제약을 오탐해, 후속 `DROP COLUMN`을 최종 상태에 반영하도록 보강하고 메타 가드에 상수를 등록

### Wave 2-4 Popup (`V2_53`) — 2026-08-13

- `popup_id VARCHAR(20)` 수동 문자열 PK → `popup_sn BIGINT GENERATED BY DEFAULT AS IDENTITY`
- 신규 PK 용어 `팝업일련번호(POPUP_SN)`를 프로젝트 `일련번호N19` 도메인에 등록
- 실측 위험도: 기존 E2E 0행, inbound FK 0. 관리자/사용자 API, 관리자 배너·팝업 화면, 공개 대시보드의 `localStorage` 숨김 키, E2E 정리 스크립트까지 숫자 일련번호로 이관
- API 계약: `popupId:string` → `popupSn:number`, 등록 응답은 DB 생성 일련번호 반환
- 회귀 검증: V2.52 구형 문자열 행 주입 → V2.53 적용 후 제목·파일 URL·좌표·크기·게시기간·Y/N 보존, 구 컬럼 제거, PK=`popup_sn`, identity=`BY DEFAULT`, 시퀀스=`sq_popup_sn`, 신규 행 자동 증가 확인
- mutation 증거: V2.53 identity 선언을 일반 BIGINT로 변조하자 `tb_popup_info.popup_sn backfill failed`로 RED; 원복 후 전체 스키마 게이트에서 GREEN 재확인
- 계약·전체 게이트: OpenAPI/TypeScript/Zod 결정성, 프론트·E2E type-check, 서버 액션 Vitest 12개와 대상 ESLint 오류 0. harness 29 suites/36 tests 및 schema validation 7 suites/9 tests 실패·오류·skip 0

### Wave 2-5 MyPageContent (`V2_54`) — 2026-08-13

- `cntnts_id VARCHAR(20)` 수동 문자열 PK → 표준 용어 `콘텐츠일련번호(CONTS_SN)`의 `conts_sn BIGINT GENERATED BY DEFAULT AS IDENTITY`
- `CONTS_SN`은 공공사전 `일련번호N22/NUMERIC`에서 이번 승인 범위의 프로젝트 `일련번호N19/BIGINT` 도메인으로 명시 재매핑
- 실측 위험도: `tb_indv_pg_conts` 기존 E2E 0행, inbound FK 0. 이미 삭제된 별도 헤더 테이블 `tb_indv_pg`와 혼동하지 않고 콘텐츠 테이블만 전환
- API/프론트 계약: `cntntsId:string` → `contsSn:number`, 등록 응답은 DB 생성 일련번호 반환. 동시에 백엔드 `cntntsUseAt`↔물리/프론트 `cntntsUseYn` 드리프트를 `cntntsUseYn`으로 정정
- 회귀 검증: V2.53 구형 문자열 행 주입 → V2.54 적용 후 명칭·접속/연계 URL·사용여부·설명 보존, 구 컬럼 제거, PK=`conts_sn`, identity=`BY DEFAULT`, 시퀀스=`sq_conts_sn`, 신규 행 자동 증가 확인
- mutation 증거: V2.54 identity 선언을 일반 BIGINT로 변조하자 `tb_indv_pg_conts.conts_sn backfill failed`로 RED; 선언 원복 완료
- 계약·전체 게이트: OpenAPI/TypeScript/Zod 재생성 결정성, 프론트·E2E type-check, 대상 ESLint 오류 0. harness 29 suites/36 tests 및 schema validation 8 suites/10 tests 실패·오류·skip 0

### Wave 2-6 DeptJobBox (`V2_55`) — 2026-08-13

- 부모 `tb_dept_job_bx.dept_task_box_id VARCHAR(20)` 수동 문자열 PK → `dept_task_box_sn BIGINT GENERATED BY DEFAULT AS IDENTITY`
- 자식 `tb_dept_task_info.dept_task_box_id` nullable FK도 같은 `dept_task_box_sn BIGINT`로 백필하고 기존 FK를 신규 숫자 키로 재결속·VALIDATE
- 표준 용어 `부서업무함일련번호(DEPT_TASK_BOX_SN)`를 프로젝트 `일련번호N19/BIGINT` 도메인으로 등록
- 실측 위험도: E2E 부모 0행·자식 0행·고아 0, V2.32 이후 물리 inbound FK 1건. 단독 PK가 아니므로 부모/자식 DB·JPA·DTO·API·FE 폼을 한 폐포로 전환
- API/프론트 계약: `deptTaskBoxId:string`·`deptJobbxId:string` → `deptTaskBoxSn:number`; 등록 응답도 DB 생성 일련번호 반환
- 회귀 검증: V2.54 구형 부모/자식 행 주입 → V2.55 적용 후 업무함명·부서·정렬·업무명·내용·우선순위와 부모-자식 연결 보존, 구 컬럼 제거, PK/FK/identity/시퀀스/신규 자동 증가 확인
- mutation 증거: V2.55 identity 선언을 일반 BIGINT로 변조하자 `tb_dept_job_bx.dept_task_box_sn backfill failed`로 RED; 선언 원복 완료
- 계약·전체 게이트: OpenAPI/TypeScript/Zod 재생성 결정성, 프론트·E2E type-check, 대상 ESLint 오류 0. harness 29 suites/36 tests 및 schema validation 9 suites/11 tests 실패·오류·skip 0

### Wave 3-1 Banner (`V2_56`) — 2026-08-13

- `tb_bnr_info.bnr_id VARCHAR(20)` 수동 문자열 PK → 표준 용어 `배너일련번호(BNR_SN)`의 `bnr_sn BIGINT GENERATED BY DEFAULT AS IDENTITY`
- `BNR_SN`은 공공사전 `일련번호N22/NUMERIC`에서 이번 승인 범위의 프로젝트 `일련번호N19/BIGINT` 도메인으로 명시 재매핑
- 실측 위험도: E2E 0행, inbound FK 0, 첨부파일 outbound FK만 존재. 관리자 API/UI·공개 슬라이더·서버 액션·E2E 정리 스크립트를 `bnrSn:number`로 전환
- 회귀 검증: V2.55 구형 문자열 행 주입 → V2.56 적용 후 명칭·링크·이미지명·설명·정렬·반영여부 보존, 구 컬럼 제거, PK/identity/시퀀스/신규 자동 증가 확인
- mutation 증거: V2.56 identity 선언을 일반 BIGINT로 변조하자 `tb_bnr_info.bnr_sn backfill failed`로 RED; 선언 원복 완료
- 계약·전체 게이트: OpenAPI/TypeScript/Zod 재생성 결정성, 프론트·E2E type-check, 서버 액션 Vitest 12개와 대상 ESLint 오류 0. harness 29 suites/36 tests 및 schema validation 10 suites/12 tests 실패·오류·skip 0

### Wave 3-2 DeptJob (`V2_57`) — 2026-08-13

- `tb_dept_task_info.dept_task_id VARCHAR(20)` 수동 문자열 PK → 신규 표준 용어 `부서업무일련번호(DEPT_TASK_SN)`의 `dept_task_sn BIGINT GENERATED BY DEFAULT AS IDENTITY`
- 실측 위험도: E2E 0행, inbound FK 0, 첨부파일·업무함·담당자 outbound FK 3건. 기존 outbound FK는 유지하며 업무 CRUD·워크허브·상세/등록 화면·E2E 계약을 `deptTaskSn:number`로 전환
- 회귀 검증: V2.56 구형 문자열 행 주입 → V2.57 적용 후 업무명·내용·우선순위·등록자 보존, 구 컬럼 제거, PK/identity/시퀀스/outbound FK 3건/신규 자동 증가 확인
- mutation 증거: V2.57 identity 선언을 일반 BIGINT로 변조하자 `tb_dept_task_info.dept_task_sn backfill failed`로 RED; 선언 원복 후 동일 회귀 테스트 GREEN
- 계약·전체 게이트: OpenAPI/TypeScript/Zod 재생성 결정성, 프론트·E2E type-check, 상세 화면 Vitest 5개와 대상 ESLint 오류 0. harness 29 suites/36 tests 및 schema validation 11 suites/13 tests 실패·오류·skip 0

### Wave 3-3 Diary (`V2_58`) — 2026-08-13

- `tb_diary_info.diary_id VARCHAR(20)` 수동 문자열 PK → 신규 표준 용어 `일기일련번호(DIARY_SN)`의 `diary_sn BIGINT GENERATED BY DEFAULT AS IDENTITY`
- 실측 위험도: E2E 0행, inbound FK 0, 첨부파일 outbound FK 1건. repository/service/API/프론트 소비자는 없고 `business-app` JPA 엔티티와 첨부파일 소유권 조회 테이블 계약만 존재
- 회귀 검증: V2.57 구형 문자열 행 주입 → V2.58 적용 후 일정 참조·진행률·일지명·지시/특이사항·등록자 보존, 구 컬럼 제거, PK/identity/시퀀스/outbound FK/신규 자동 증가 확인
- mutation 증거: V2.58 identity 선언을 일반 BIGINT로 변조하자 `tb_diary_info.diary_sn backfill failed`로 RED; 선언 원복 완료
- 전체 게이트: 백엔드 전체 compile, harness 29 suites/36 tests 및 schema validation 12 suites/14 tests 실패·오류·skip 0. API/프론트 소비자가 없어 OpenAPI codegen 변경 없음
