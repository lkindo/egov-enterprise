# react-hooks (React Compiler v6) 위반 전수 트리아지 — 2026-07-07

audit 백로그 "react-hooks 67건/51파일"을 워크플로우로 **전수 적대적 분류**한 기록. 백엔드 DB 다운으로 런타임 검증이 불가한 세션이었으므로, **정적으로 안전이 증명되는 것만 수정**하고 나머지는 근거와 함께 분류했다. (총 66건 — websocket-context refs 중복 1건 제외)

| 분류 | 수 | 처리 |
|---|---|---|
| ✅ fixed | 6 | 정적 안전 확증(참조 안정화). 커밋 469b7eb1b |
| ⚪ false_positive | 9 | 실제 정상 코드(수정 불요) |
| 🟡 deferred_runtime | 51 | 앱 구동 검증 필요(무한루프/거동 변화 위험) |

> **점진 정리 지침**: DB 복구 후 각 deferred 항목을 해당 UI 플로우를 구동하며 개별 검증한다. dep 배열/effect 재구조화는 무한루프를 유발할 수 있으므로 blind 일괄 수정 금지(BE/FE 헌법 및 audit 보고서 방침).

## ✅ Fixed (6) — 참조 안정화, 커밋됨

| 파일:라인 | 룰 | 변경 |
|---|---|---|
| `src/app/admin/system/monitoring/MonitoringHubClient.tsx:95` | exhaustive-deps | Wrapped auditLogs in useMemo(() => auditData?.list || [], [auditData]) to stabilize the array reference feeding the selectedItem useMemo deps. |
| `src/app/admin/system/monitoring/MonitoringHubClient.tsx:102` | exhaustive-deps | Wrapped systemLogs in useMemo(() => systemLogData?.list || [], [systemLogData]) to stabilize the reference. |
| `src/app/admin/system/monitoring/MonitoringHubClient.tsx:109` | exhaustive-deps | Wrapped loginLogs in useMemo(() => loginLogData?.list || [], [loginLogData]) to stabilize the reference. |
| `src/app/admin/system/monitoring/MonitoringHubClient.tsx:116` | exhaustive-deps | Wrapped comments in useMemo(() => commentData?.list || [], [commentData]) to stabilize the reference. |
| `src/app/approvals/ApprovalHubClient.tsx:55` | exhaustive-deps | Hoisted a module-level stable const `const EMPTY_APPROVALS: Approval[] = [];` after the ApprovalTab type declaration, and changed line 55 fallback from `|| []` to `|| EMPTY_APPROVALS` so `list` keeps … |
| `src/app/components/ui/global-command-center.tsx:104` | exhaustive-deps | Wrapped the `quickActions` array literal in `useMemo(() => [...], [logout])` so it no longer produces a new reference each render (stabilizing the `filteredItems` useMemo dep). Only structural change … |

## ⚪ False positive (9) — 정상 코드

| 파일:라인 | 룰 | 근거 |
|---|---|---|
| `src/app/admin/community/boards/detail/BoardDetailClient.tsx:63` | set-state-in-effect | Line 63 is the canonical client-mount / hydration-guard idiom: `const [mounted, setMounted] = useState(false)` with `useEffect(() => setMounted(true), [])`, gating a loading spinner via `if (!mounted) return ...`. It is provably loop-safe b… |
| `src/app/admin/community/boards/maker/components/BoardMakerWizard.tsx:181` | incompatible-library | The flagged watch('menuNm') is a react-hook-form call inside the nextStep EVENT HANDLER (invoked from the submit-button onClick / onSubmit flow), never during render. When called from a callback, watch() is a one-time synchronous read of th… |
| `src/app/admin/operation/external-hr/ExternalHrClient.tsx:79` | purity | Line 79 `otsdHrId: `HR_${Date.now()}`` sits inside the async function `onRegisterSubmit` (lines 73-93), which is an EVENT HANDLER wired via `form.handleSubmit(onRegisterSubmit)` on the modal's submit Button onClick (line 234). Date.now() is… |
| `src/app/admin/operation/rewards/RewardManageClient.tsx:79` | purity | Date.now() at line 79 is inside onRegisterSubmit (line 74), an `async (values) => {...}` form-submit handler, not render-phase code. It builds submitData only when the user submits the form; the impure call never runs during render, so it c… |
| `src/app/admin/operation/rewards/RewardManageClient.tsx:82` | purity | Date.now() at line 82 (informlSanctnId) is in the same async onRegisterSubmit event handler as line 79. It executes only on form submission inside the async callback, never during render, so the purity warning is a false positive; no code c… |
| `src/app/admin/system/hpcm/HpcmClient.tsx:61` | purity | Date.now() at line 61 is inside onRegisterSubmit (async function, lines 56-75), which is a form submit handler invoked via form.handleSubmit(onRegisterSubmit) wired to the Button onClick at line 162. It runs only on user submit — an event-h… |
| `src/app/components/layout/header.tsx:67` | set-state-in-effect | This is the canonical next-themes client-mount / hydration-guard idiom. `const [mounted, setMounted] = useState(false)` plus `useEffect(() => setMounted(true), [])` intentionally forces server render and first client render to produce mount… |
| `src/app/cop/cmy/selectCommunityDetail/[id]/page.tsx:23` | error-boundaries | Line 23 returns a client-component element from inside a try/catch in an async Next.js Server Component. The error-boundaries rule targets try/catch that wraps SYNCHRONOUS child rendering, since try/catch cannot catch a child's render error… |
| `src/components/ui/RichTextEditor.tsx:31` | set-state-in-effect | Line 31 is the canonical client-mount/hydration-guard idiom: `setMounted(true)` inside a `useEffect` with an EMPTY dependency array (line 32). It runs exactly once after first client mount and performs a monotonic boolean latch (false -> tr… |

## 🟡 Deferred — 런타임 검증 필요 (51)

| 파일:라인 | 룰 | 보류 사유 |
|---|---|---|
| `src/app/admin/collaboration/CollaborationHubClient.tsx:43` | set-state-in-effect | The effect (lines 41-45) synchronizes internal `activeTab` state from the `defaultTab` prop — the canonical prop-to-state sync pattern. It is NOT safely derivable during render because `activeTab` is also mutated locally by the tab buttons'… |
| `src/app/admin/collaboration/mail-send/MailSendHubClient.tsx:43` | set-state-in-effect | Line 43 (setCurrentTime(new Date().toLocaleTimeString())) synchronizes state from an external, non-render-derivable source (current wall-clock time). currentTime is intentionally initialized to '' and populated only client-side inside a use… |
| `src/app/admin/collaboration/scraps/selectScrapList/ScrapListClient.tsx:51` | set-state-in-effect | The useEffect calls fetchList(), which synchronizes multiple state values (setLoading/setList/setTotalCount/setTotalPages) from an async API response. This is the classic 'sync state from fetched data' pattern, not a value derivable during … |
| `src/app/admin/collaboration/scraps/selectScrapList/ScrapListClient.tsx:52` | exhaustive-deps | The effect deps are [pageNo] but it calls fetchList, a function recreated every render that closes over pageNo. The missing dependency is a FUNCTION with no guaranteed stable reference. Adding fetchList unwrapped would re-run the effect on … |
| `src/app/admin/community/boards/insertBoardArticle/BoardRegistClient.tsx:91` | set-state-in-effect | Lines 89-92 are the canonical client-mount gate (`const [mounted,setMounted]=useState(false); useEffect(()=>setMounted(true),[])`) feeding the `if(!mounted) return <Loader/>` gate at line 136. It exists to prevent SSR/hydration mismatch bec… |
| `src/app/admin/community/boards/selectBoardArticle/[id]/BBSDetailClient.tsx:66` | set-state-in-effect | The useEffect (lines 63-72) synchronizes local component state from asynchronously fetched data: setSelectedCategory(article.qnaCatCd) mirrors the `article` object returned by useQuery (getArticle). This is the classic data-sync set-state-i… |
| `src/app/admin/community/boards/selectBoardList/BoardListClient.tsx:61` | set-state-in-effect | setMounted(true) inside useEffect(..., []) is the intentional client-mount / hydration-guard pattern; `mounted` gates rendering of interactive admin buttons at line 360 ({mounted && ...}) so SSR markup matches the client and avoids a hydrat… |
| `src/app/admin/community/boards/selectBoardList/BoardListClient.tsx:66` | set-state-in-effect | This effect (deps [searchParams]) sets five local filter states (searchWrd/searchCnd/orderBy/startDate/endDate) from URL query params — the explicit 'set-state-in-effect that synchronizes state from URL' case. These states are also user-edi… |
| `src/app/admin/help/KnowledgeHubClient.tsx:94` | set-state-in-effect | Line 94 setIsScanning(true) drives a transient, time-based UI flag: the effect runs on searchQuery change, sets isScanning true, then a 800ms setTimeout resets it to false (cleanup clearTimeout on line 96). This flag gates the scan-line ani… |
| `src/app/admin/security/authority/SecurityHubClient.tsx:118` | set-state-in-effect | useEffect([usersData, selectedAuthorCode]) seeds the editable local state tempUserMappings from fetched React Query data (usersData.list filtered by regYn==='Y'). This is a mutable working copy the user subsequently mutates via toggleUserMa… |
| `src/app/admin/security/authority/SecurityHubClient.tsx:126` | set-state-in-effect | useEffect([menusData, selectedAuthorCode]) seeds the editable local state tempMenuMappings from fetched data (menusData.authorMenus.map(m=>m.menuNo)). This is a mutable working copy driven further by toggleMenuMapping/handleToggleGlobal and… |
| `src/app/admin/security/login-policy/LoginPolicyAdminClient.tsx:81` | set-state-in-effect | fetchData() (lines 68-78) synchronously calls setLoading(true) and asynchronously setData(response.list), synchronizing React state from an async backend fetch on mount. This is a data-syncing effect, not a value derivable during render, so… |
| `src/app/admin/security/login-policy/LoginPolicyAdminClient.tsx:82` | exhaustive-deps | The effect omits its fetchData dependency. fetchData is a plain component-scope function re-created every render (no useCallback, not a stable ref/setter) and it calls setData/setLoading. Adding it to the [] dep array unwrapped would cause … |
| `src/app/admin/stats/IntelligenceHubClient.tsx:42` | set-state-in-effect | This is the intentional client-only timestamp pattern: useState('') keeps server render and first client render identical, then a mount-only effect (empty deps []) sets nowStr = new Date().toISOString(). nowStr feeds the Active/Archived dec… |
| `src/app/admin/survey/manage/SurveyManageClient.tsx:22` | set-state-in-effect | setNow(new Date()) inside useEffect([]) is the canonical SSR-hydration-safe 'client mount' pattern: state starts null on server + first client render (making the line 79 status badge and line 139 'progressing' metric render deterministicall… |
| `src/app/admin/survey/polls/participate/OnlinePollParticipateClient.tsx:33` | set-state-in-effect | setTodayStr synchronizes state from new Date() inside a client-only useEffect with [] deps. This is a deliberate SSR-hydration-safe pattern: calling new Date() during render would produce a server/client mismatch, so the value is intentiona… |
| `src/app/admin/survey/polls/participate/OnlinePollParticipateClient.tsx:34` | immutability | fetchPolls is an async data-fetching function declared during render (a new reference each render) that calls multiple state setters (setLoading, setPolls). It is invoked in the mount effect but is unmemoized. Any static remediation — wrapp… |
| `src/app/admin/system/banner/BannerAdminClient.tsx:99` | set-state-in-effect | This is the SSR-safe 'current time on mount' idiom: `now` initializes to null (server render == first client render, no hydration mismatch), then a mount-only effect with empty deps `[]` calls setNow(new Date()) exactly once. It is loop-saf… |
| `src/app/admin/system/codes/institution/InstitutionCodeClient.tsx:87` | set-state-in-effect | The effect (lines 85-91) invokes loadListData/loadReceptionData, which do an async codeAdminService fetch and then call setData/setTotal/setPageNo/setLoading. This is state synchronized from fetched data keyed on activeTab — an async side e… |
| `src/app/admin/system/codes/institution/InstitutionCodeClient.tsx:91` | exhaustive-deps | Dep array is [activeTab]; missing deps are searchWrd (state) and loadListData/loadReceptionData (plain functions recreated every render, NOT wrapped in useCallback). Adding the fetchers unwrapped would re-run the effect on every render -> i… |
| `src/app/admin/system/common-code/CommonCodeClient.tsx:268` | set-state-in-effect | Effect re-seeds `flattenedNodes` from the `initialClusters` memo (derived from props clCodes/groups/details/selectedGroupId). This is NOT a pure derived value: `flattenedNodes` is independently mutated by the drag-and-drop handler (setFlatt… |
| `src/app/admin/system/common-code/CommonCodeClient.tsx:372` | set-state-in-effect | Effect synchronizes selectedClusterId/selectedGroup from props (selectedGroupId, details, initialClusters). The dependency array includes `selectedGroup`, which the effect itself writes via setSelectedGroup at line 373; non-loop behavior de… |
| `src/app/admin/system/layout/LayoutManagerClient.tsx:62` | set-state-in-effect | The mount effect (deps []) hydrates themeConfig from localStorage via setThemeConfig(parsed) and also runs the DOM side effect applyDesignTokens(parsed), which mutates document.documentElement CSS vars and writes localStorage. This is state… |
| `src/app/admin/system/menus/MenuAdminClient.tsx:311` | set-state-in-effect | The useEffect (lines 305-317) synchronizes local state from fetched server data: it reads initialMenus (from use(menusPromise)), transforms it via listToTree/flattenTree, and calls setFlattenedMenus(flat) plus setExpandedIds(...). flattened… |
| `src/app/admin/system/policies/PolicyAdminClient.tsx:67` | set-state-in-effect | The useEffect at line 66-68 calls fetchPolicies(), which sets loading/policies state from the result of an async network call (policyAdminService.getPolicies()). This is a data-fetching effect that synchronizes component state from fetched … |
| `src/app/admin/user/UserOrgHubClient.tsx:254` | set-state-in-effect | The effect (lines 250-256) syncs local state `flattenedDepts` from the fetched `departments` list (derived from react-query `deptsData`). This is a classic fetched-data synchronization pattern. `flattenedDepts` is NOT purely derived: it is … |
| `src/app/components/dashboard/DashboardCharts.tsx:31` | set-state-in-effect | Canonical Next.js 'mounted' hydration guard in DashboardVisitorChart: useState(false) + useEffect(() => setMounted(true), []) then `if (!mounted) return placeholder`. It is loop-safe (empty deps => effect fires once, re-render does not re-t… |
| `src/app/components/dashboard/DashboardCharts.tsx:95` | set-state-in-effect | Identical mounted-guard pattern in DashboardPostChart: useState(false) + useEffect(() => setMounted(true), []) gating a client-only Recharts BarChart. Loop-safe by static reasoning (empty dependency array). Same conclusion as line 31: the m… |
| `src/app/components/layout/DynamicBreadcrumb.tsx:65` | exhaustive-deps | The effect deps are [pathname] but the body reads searchParams (useSearchParams()) at line 29. The missing dep is searchParams, which is NOT a provably-stable reference (not a module const, useRef, or useState setter) — its reference stabil… |
| `src/app/components/layout/NavItem.tsx:99` | set-state-in-effect | The effect (lines 98-103) sets two states: setIsMounted(true) is an SSR mount guard paired with `if (!isMounted) return null;` at line 183 to avoid hydration mismatch, and setIsOpen(true) syncs collapsible open-state from isActive (derived … |
| `src/app/components/layout/route-progress.tsx:14` | set-state-in-effect | setLoading(true) runs inside a useEffect keyed on [pathname, searchParams] to drive a route-change progress bar: it pulses loading=true on navigation, then a setTimeout flips it back to false after 400ms. This is a time-based side-effect (a… |
| `src/app/components/ui/app-notification-drawer.tsx:41` | set-state-in-effect | Line 41's setMounted(true) inside a []-dep useEffect is the canonical client-mount guard that protects the createPortal(..., document.body) render (line 60) from SSR/hydration mismatch. It is a genuine set-state-in-effect (not a rule misfir… |
| `src/app/components/ui/observability-charts.tsx:71` | set-state-in-effect | GaugeChart uses the canonical SSR client-mount gate: `const [mounted,setMounted]=useState(false); useEffect(()=>setMounted(true),[])`, with an early `return <skeleton/>` while `!mounted` (line 78). This intentionally defers the Recharts/Saf… |
| `src/app/components/ui/observability-charts.tsx:131` | set-state-in-effect | RealtimeSparkline uses the identical SSR client-mount gate as line 71 (`useEffect(()=>setMounted(true),[])` with early `return <skeleton/>` at line 133) to defer the Recharts LineChart render until after hydration. Same analysis: rule fires… |
| `src/app/components/ui/session-expiry-warning.tsx:61` | set-state-in-effect | setShowWarning(false) at line 61 runs directly in the effect body (inside the `if (!user)` branch of the effect at lines 59-100), so it is a genuine set-state-in-effect, not a false positive (not in an event handler/async callback). It sync… |
| `src/app/components/ui/smart-onboarding-hub.tsx:25` | set-state-in-effect | Line 25 `setMounted(true)` is a client-mount guard inside `useEffect(..., [])`. Loop-safety itself is statically provable (empty deps run once; flag flips a single time and the effect never re-runs), so it is not a loop hazard. However the … |
| `src/app/components/ui/standard-modal.tsx:29` | set-state-in-effect | Line 29 is setMounted(true) inside useEffect(..., []) — the canonical SSR/hydration two-pass 'mounted guard'. `mounted` starts false, component returns null until it flips true post-mount, so createPortal(document.body) is deferred to after… |
| `src/app/note/page.tsx:49` | set-state-in-effect | The effect at lines 48-50 calls loadNotes(), a useCallback (deps [tab, toast]) that performs an async API fetch and then calls setLoading(true)/setNotes(res.list || [])/setLoading(false). This is a data-syncing effect: notes/loading state i… |
| `src/app/search/SearchClient.tsx:66` | exhaustive-deps | The effect's dependency array [query, initialQuery, initialResults] omits `results`, which the effect body reads at line 40 (`results === initialResults`) — that is the missing dependency. The effect also calls setResults() at line 50, so t… |
| `src/app/smart-toolkit/dept-job/[id]/DeptJobDetailClient.tsx:52` | set-state-in-effect | The effect calls fetchList(), an async data fetch (GET /deptjob) that sets loading/list/totalCount/totalPages from the API response. This synchronizes component state from fetched data — the legitimate, unavoidable use of setState-in-effect… |
| `src/app/smart-toolkit/dept-job/[id]/DeptJobDetailClient.tsx:53` | exhaustive-deps | The missing dependency is fetchList, a plain function defined in the component body (lines 36-49) and recreated on every render — NOT a provably stable reference (not module-level, not a ref, not a setState setter). Adding it unwrapped woul… |
| `src/app/smart-toolkit/dept-job/selectDeptJobList/DeptJobListClient.tsx:52` | set-state-in-effect | The effect calls fetchList(), which synchronizes four state values (loading, list, totalCount, totalPages) from the /deptjob API response. This is a legitimate data-fetching/sync effect, not a derivable-during-render computation. Converting… |
| `src/app/smart-toolkit/dept-job/selectDeptJobList/DeptJobListClient.tsx:53` | exhaustive-deps | The dep array [pageNo] omits fetchList, which is used inside the effect. fetchList is a plain function re-created on every render, so its reference is NOT stable. Adding it unwrapped would cause an infinite loop: fetchList sets state -> re-… |
| `src/app/smart-toolkit/schedule/dept/ScheduleDeptClient.tsx:64` | set-state-in-effect | The effect (lines 63-65) invokes fetchList(), a useCallback(deps=[params]) that awaits getDeptScheduleList(params) then calls setSchedules(). This is a data-fetching effect that synchronizes the `schedules` state from asynchronous backend d… |
| `src/app/UnifiedDashboardClient.tsx:82` | set-state-in-effect | setIsMounted(true) in a useEffect with empty [] deps is the client-mount/hydration-guard idiom: isMounted is false during SSR and the first client render, then flips true after hydration. It is NOT an infinite-loop risk (empty deps -> runs … |
| `src/contexts/AuthContext.tsx:117` | set-state-in-effect | The effect (lines 116-118) invokes checkAuth(), a useCallback-stabilized async function that awaits authService.getCurrentUser() and then calls setUser/setLoading to synchronize React state from the fetched user data (and from localStorage/… |
| `src/contexts/LayoutContext.tsx:23` | set-state-in-effect | The effect (lines 22-24) calls setSidebarOpen(false) keyed on [pathname] to close the sidebar on route change (mobile UX), while still allowing the user to open it within the same page. This is a legitimate URL->state synchronization: isSid… |
| `src/contexts/websocket-context.tsx:118` | refs | Line 118 reads stompClient.current (a useRef, declared line 21) DURING render to populate the WebSocketContext.Provider value — a genuine ref-read-during-render, not the false-positive callback/effect case. It cannot be statically fixed saf… |
| `src/hooks/use-auto-save-draft.ts:90` | set-state-in-effect | The flagged setHasDraft(true) (and adjacent setLastSavedAt) at lines 90-91 live in a mount effect (deps [restoreDraft]) that synchronizes React state FROM data read out of localStorage via restoreDraft(). This is the canonical data-syncing … |
| `src/hooks/useAppForm.ts:29` | immutability | Line 29 mutates form.handleSubmit (the useForm() return value) during render, which the immutability rule correctly flags (note the intentional @ts-ignore on line 28). It is NOT a false positive. The only static fixes that satisfy the rule … |
| `src/lib/hooks/use-notifications.ts:78` | set-state-in-effect | Line 78 calls fetchNotifications() inside the mount/connectivity useEffect (lines 76-102). fetchNotifications (lines 26-53) is an async network fetch that ultimately calls setNotifications and setUnreadCount (lines 45-46), which is why the … |

---
*생성: Claude Code 워크플로우 react-hooks-triage (52 agents). 원본 근거 전문: 워크플로우 저널.*
