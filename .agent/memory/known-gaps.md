---
schema_version: 1
memory_kind: known-gaps
status: active
authority: derived-active-index
scope: repository
sensitivity: public-repo-safe
verified_at: 2026-09-06
verified_against: ab7fd67b09aa9458b6bafacb1d562a0e9aef9d3d
canonical_sources:
  - ../../frontend/public/governance_harness_atlas.html
  - ../../docs/02-architecture/decisions/ADR-0008-multi-source-approved-migration-workflow.md
  - ../../docs/02-architecture/decisions/ADR-0009-controlled-url-search-state.md
  - ../../docs/02-architecture/legacy-migration-tool-design.md
  - ../../docs/04-operations/verification-blindspots.md
  - ../../docs/04-operations/pending-decisions.md
refresh_triggers:
  - gap-state-change
  - production-readiness-change
  - verification-evidence-change
---

# 공용 활성 Gap 인덱스

## 범위·상태 정의

이 파일은 현재 코드나 외부 실측으로 재현 가능한 미해결 위험의 얇은 인덱스다. 상세 원장은 링크된 문서·코드이며, 완료 이력은 Git에 남긴다.

- 우선순위: `P0` 즉시 안전/false-green, `P1` 높은 회귀·운영 위험, `P2` 계획 필요, `P3` 개선.
- 상태: `open`, `blocked-external`, `deferred`, `accepted-risk`, `needs-revalidation`.
- 추측이나 과거 task의 unchecked 항목만으로 새 gap을 만들지 않는다.

## 활성 Gap registry

| GAP-ID | 우선순위 | 상태 | 영역 | 요약 | 근거 | 다음 행동/재개 조건 | 결정권자 | 검증일 |
|---|---|---|---|---|---|---|---|---|
| GAP-MIG-001 | P1 | open | migration-tool | 실제 YAML의 source/target `endpointId` loader 유실과 그로 인한 승인 경로 차단은 2026-09-05 보정·회귀 테스트로 닫혔지만, 승인형 워크플로에는 다음 production 잔여 위험이 있다. PostgreSQL source adapter는 `EXPERIMENTAL`이고, Oracle·Tibero·MySQL·MariaDB·SQL Server adapter는 `UNVERIFIED`라 commit이 차단되며 외부 JDBC JAR 경로도 commit을 허용하지 않는다. 따라서 실제 DB 버전·최소권한·snapshot·LOB/대용량 streaming 증거가 없다. Target은 `endpointId` 라벨과 schema fingerprint만 결속해 같은 라벨·동일 구조의 다른 PostgreSQL instance를 구분하지 못하고, schema allowlist도 기계 강제되지 않는다. load는 INSERT-only라 upsert·증분/CDC가 없고, 앞 chunk commit 이후 실패의 reconciliation·전체 rollback과 cutover/rollback 런북도 없다. 검증 결과도 영속·기계판독 가능한 실행 artifact로 보존되지 않는다. | [ADR-0008](../../docs/02-architecture/decisions/ADR-0008-multi-source-approved-migration-workflow.md), [MappingLoader](../../migration-tool/src/main/java/nuri/migration/model/MappingLoader.java), [loader endpoint regression](../../migration-tool/src/test/java/nuri/migration/model/MappingLoaderEndpointBindingTest.java), [file-based workflow regression](../../migration-tool/src/test/java/nuri/migration/MigrationWorkflowRunnerTest.java), [workflow binding gate](../../migration-tool/src/main/java/nuri/migration/MigrationWorkflowRunner.java), [source adapters](../../migration-tool/src/main/java/nuri/migration/adapter/SourceAdapterRegistry.java), [EtlExecutor](../../migration-tool/src/main/java/nuri/migration/etl/EtlExecutor.java), [Atlas migration](../../frontend/public/governance_harness_atlas.html#content-migration) | 다음 선행 과제는 credential-redacted target endpoint/location·instance identity 결속과 target schema allowlist다. 이어 각 지원 source DB의 승인 버전·드라이버·최소권한에서 inventory completeness, snapshot/LOB, crash/resume를 실증하고 결과 artifact를 보존한다. 외부 driver commit 경계를 정하고 idempotency·제한적 upsert·증분/CDC 정책, commit reconciliation·backup·cutover/rollback 런북을 승인한 뒤 production readiness를 재평가한다. | 사용자/DB 운영 | 2026-09-05 |
| GAP-ARCH-001 | P2 | open | module boundaries | `business-app` 서비스 계층의 exact type-reference census는 app→app **4건**·app→core **33건**이다. 과거 주입 필드만 세던 측정은 2026-08-29 app→core를 8→9로 올렸으며, 당시 신설분은 `report/WorkReportService → user (UserRepository)` 하나였다. import·signature·event·인라인 FQN까지 포괄하는 전수 scanner 도입 시 24건으로 기준을 바로잡았고, 2026-09-05 Mail·SMS의 `UserContactService` 결합 2건이 추가돼 26건이 됐다. 2026-09-06 원 업로더만 첨부를 재할당하도록 `AttachmentAssignmentPolicy`를 Mail·MemoReport·RewardManage·WorkReport·Schedule·Banner·Popup 서비스에 연결하면서 file edge 7건이 추가돼 33건이 됐다. Board의 file edge는 기존 `FileService` 참조로 이미 포함돼 있었다. 2026-08-20 census 동결 게이트를 걸어 더 늘지 못하게 했고, 2026-08-23 `BoardUserDeletionCleanupListener`의 comment 정리를 `CommentUserDeletionCleanupListener`(comment 자체 `UserDeletionEvent` 구독)로 역전해 app→app 6→5로 낮췄다. **2026-08-29 5 → 4** — `BoardEventListener` 의 board→comment 를 역전했다. comment 가 커밋 이후 `PostCommentCountChangedEvent` 를 **개수까지 실어** 발행하고 board 는 그것만 반영한다. 이벤트를 `foundation/core/event` 에 둔 것이 핵심이다 — 어느 한쪽 도메인 패키지에 두면 반대편이 import 하게 되어 **주입만 사라지고 컴파일 의존은 남는다**(census 숫자만 내려가는 거짓 진척, H2). ⚠ 같은 변경에서 **댓글 수가 영원히 0 이던 결함**도 닫혔다 — `syncCmntCntAtomic` 호출부가 저장소 전체에서 board 의 `PostCreatedEvent` 리스너 하나뿐이었고 그 경로는 **게시글 생성 시점**에만 돌아 언제나 0 을 썼다(comment 서비스에는 `cmntCnt` 를 건드리는 코드가 0줄, DB 트리거도 없음). 세 화면이 그 값을 표시하고 있었다. 잔여 4건: dashboard→notification, stats→board, informalsanction→sms, informalsanction→mail. `DomainIsolationTest`는 엔티티 패키지만 보고 서비스 계층을 javadoc으로 명시 배제하므로 이 축을 원리적으로 탐지하지 못했고, 그 사각을 새 린터가 덮는다. app→core는 코어가 삭제 대상이 아니라 허용 가능하며 `허용`과 `미탐지`를 구분하려고 함께 동결했다. | [CrossDomainCouplingLinterTest](../../api-server/src/test/java/nuri/api/harness/CrossDomainCouplingLinterTest.java), [CommentUserDeletionCleanupListener](../../business-app/src/main/java/nuri/business/service/comment/listener/CommentUserDeletionCleanupListener.java), [ReportStatsService](../../business-app/src/main/java/nuri/business/service/stats/ReportStatsService.java), [SanctionEventListener](../../business-app/src/main/java/nuri/business/service/informalsanction/event/SanctionEventListener.java) | 잔여 app→app 4건을 port/event로 역전하고 상수를 낮춘다. `BoardEventListener`(board→comment)는 2026-08-29 에 역전 완료했다 — comment 가 개수를 실어 발행하고 board 가 반영하며, 이벤트를 foundation 에 둬 양쪽이 서로를 import 하지 않는다. 남은 넷 중 informalsanction→sms/mail 은 발송 port 추상화가 선행된다. | 아키텍처 소유자 | 2026-09-06 |
| GAP-API-001 | P2 | open | API contracts | 비정형 payload(`ApiResponse<Map<...>>`)가 **2026-08-29 이행으로 5건 → 1건**이 됐다. 남은 1건은 `DashboardApiController` 이며 foundation `DashboardItemProvider` SPI 가 페이로드를 `Map` 으로 규정해 typed 이행이 SPI 계약 변경을 동반한다. 상환한 4건: `HealthCheckApiController#checkHealth`(`HealthStatusResponse`), `MenuUserApiController#getHeadMenu`·`#getLeftMenu`(`MenuListResponse`), `SatisfactionApiController#getAverage`(`SatisfactionAverageResponse`). ⚠ **만족도 이행은 표현 변경이 아니라 결함 수정이었다** — `Map.of` 는 null 값을 담으면 NPE 라 "평가 없음"(서비스의 `null`)을 `0.0` 으로 뭉갤 수밖에 없었고, 그래서 아무도 평가하지 않은 게시글이 화면에서 별 0개·`0.0` 으로 **모두가 최하점을 준 게시글과 똑같이** 보였다. 같은 핸들러의 `@Operation` 설명은 "응답이 없으면 null 이다 — 0 과 구분해야 한다" 로 정반대를 약속했고, 컨트롤러 테스트는 그 결함을 "null 직렬화 회피" 라는 이름으로 계약에 고정하고 있었다. record 는 null 을 그대로 실어 나르므로 제약 자체가 사라졌다(Jackson 이 null 을 생략하므로 필드가 부재로 온다). wrapper 밖 binary/stream 축은 2026-08-23 헌법 제6조 3항으로 파일명·핸들러 단위 양방향 exact census 가 완료돼 있다. | [ResponseContractLinterTest](../../api-server/src/test/java/nuri/api/harness/ResponseContractLinterTest.java), [백엔드 헌법 제6조](../knowledge/backend-api-constitution/artifacts/constitution.md), [DashboardApiController](../../api-server/src/main/java/nuri/api/controller/business/main/DashboardApiController.java) | Dashboard 는 SPI 를 typed 위젯 목록으로 뒤집는 설계 결정이 선행이다 — 그 결정 전에는 상수 1을 낮추지 않는다. | API/사용자 | 2026-08-29 |
| GAP-CONTRACT-001 | P1 | open | input contracts | Entity→요청 DTO→OpenAPI 의미 계약은 위험 기반 표적만 검사하며 전체 쓰기 DTO를 포괄하지 않는다. **2026-08-29 표적을 12→13 묶음(길이 필드 61→66)으로 넓혔다** — `TemplateDto` 신설. 그 확장에서 실제 결함이 드러났고 **그 축은 닫혔다** — 당시 `tmplt_id` 는 PK·NOT NULL 인데 엔티티에 생성 전략이 없고 서비스도 값을 만들지 않는데 등록 폼이 그 값을 묻지 않아 등록이 언제나 실패했다. **2026-09-03 재확인**: `TemplateDto.tmpltId` 에 `@NotBlank @Size(max = 20)` 이 있고 `TemplateAdminClient` 가 '템플릿 ID' 입력 필드를 렌더·검증하며, 엔티티의 비-PK 네 컬럼이 전부 `nullable = false` 로 물리 DDL(`V2_0__baseline.sql:9494`)과 일치한다. ⚠ **종전 이 행이 적어 둔 '같은 테이블에 엔티티가 둘' 은 현재 사실이 아니다** — `business-app/.../domain/board/` 에 `Template.java` 가 없고, `tb_tmplt_info` 를 참조하는 main 소스는 `domain/template/Template.java` 와 `TemplateDto.java` 둘뿐이다(2026-09-03 실측). 이 낡은 문장은 실제로 후속 감사를 오도했으므로 남기지 않는다. **2026-09-06 표적 확장**: `MyPageContentDto`, `ScrapDto`, `AddressBookDto`와 중첩 `AddressBookUserDto`, `ExternalHrDto`, `MemoReportDto`, `WorkReportDto`, `DeptJobBoxDto`, `DeptJobDto`를 추가해 현재 길이 binding은 **24 DTO·113필드**, enum은 **16필드**, 명시적 required 기준선은 **37필드**, 중첩 validation cascade는 **2필드**다. **2026-09-07 표적 확장(DEC-OPS-048)**: `InternetSvcGuidanceDto`를 추가해 길이 113→**116필드**, 명시적 required 기준선 37→**39필드**, 서버 소유·응답 전용 28→**31필드**가 됐다. 이 편입은 어휘 정합이 선행이었다 — 종전 DTO는 `intnetSvcNm`/`intnetSvcDc`/`reflctAt`를 써서 엔티티(`itntSvcNm`/`itntSvcExpln`/`rfltYn`)와 이름이 달라 **이름 기반 바인딩 자체가 불가능**했고, 그래서 `itntSvcExpln`의 DTO 상한 1000이 컬럼 4000보다 좁은 상태가 오래 남아 있었다. 정확한 현재 수는 린터 소스의 `MIN_*` 상수가 정본이다. 이 과정에서 `ExternalHrDto.gndrCd`의 DTO/OpenAPI 상한 30이 Entity·물리 스키마 12를 넘던 결함을 12로 맞췄고, 네 서버 소유 감사 필드는 요청에서 제외하며 서비스가 인증된 관리자 loginId를 강제하도록 위조 경로도 닫았다. 같은 DTO의 `emlAddr` 50은 컬럼(320)보다 좁은 임의 제한이라 320으로 맞췄고(DEC-OPS-036 — 2026-09-06 병합에서 수정·삭제 경로도 등록과 같은 ADMIN/SYSTEM 서비스 경계·fail-closed 감사자 해석을 공유한다), 커뮤니티·업무함 이름의 `@NotBlank`(DEC-OPS-037)와 문자 수신자의 필드 단위 필수 해제(DEC-OPS-035)도 required 기준선에 반영해 **37필드**가 됐다. 이어 두 보고 DTO의 직접 저장 문자열 8개에 빠져 있던 길이와 제목·수신자 3개의 필수 계약을 복원했고, 두 보고 작성 화면이 물리 `varchar(100)` 제목에 200자를 허용하던 경계를 100자로 맞췄다. 업무함·부서업무 DTO의 직접 저장 문자열 6개 상한과 서버 소유·응답 전용 15필드도 같은 계약으로 묶었다. `@RequestBody`에 직접 선언된 구조형 type은 **67종**이고 이 중 top-level binding은 **22종**, 남은 미검사는 **45종**이다. 중첩 body graph까지 포함한 unique type은 **72종**, binding은 **24종**, 남은 미검사는 **48종**이다(2026-09-06 main 기준 실측 — 같은 날 병합된 감사 스택이 들여온 `ApprovalDraftRequest` 등 신규 요청 DTO 는 이 수치에 포함되지 않았다). 이 type census는 하나의 DTO 안에서 직접 저장 필드 일부만 묶을 수 있으므로 **type coverage가 field-complete를 뜻하지 않는다**. 남은 gap은 표적 밖 쓰기 DTO의 미검사뿐 아니라, 표적 안에서도 `@NotBlank`의 공백 의미가 OpenAPI/Zod까지 보존되는지와 root controller validation reachability를 전수 확인하지 않는 사각을 포함한다. | [InputContractMirrorLinterTest](../../api-server/src/test/java/nuri/api/harness/InputContractMirrorLinterTest.java), [DeptJobBoxDto](../../business-core/src/main/java/nuri/business/service/deptjob/dto/DeptJobBoxDto.java), [DeptJobDto](../../business-core/src/main/java/nuri/business/service/deptjob/dto/DeptJobDto.java) | 남은 쓰기 DTO의 직접 저장 경계를 위험 기반으로 계속 편입한다. `RewardManageDto` 는 레거시 필드명(`rwardwnrId` 등)이 엔티티와 달라 이름 기반 바인딩이 불가하며 `@Size` 8종만 수기로 정합했다 — 이름 매핑을 지원하는 바인딩이 생기면 편입한다. 병행 후속으로 root validation reachability와 OpenAPI/Zod의 blank 의미 보존 계약을 별도 설계한다. Template 축(중복 엔티티 매핑·nullability 불일치·등록 실패)은 2026-09-03 실측으로 전부 닫혀 재확인 대상이 아니다. | API/도메인 소유자 | 2026-09-06 |
| GAP-DEP-001 | P1 | needs-revalidation | dependency security | PR 코드는 read-only token으로 Gradle graph artifact만 생성하고, 신뢰된 `workflow_run`은 checkout/run 없이 이를 제출하며, required `secret-scan`은 base/head snapshot 완전성을 최대 600초 fail-closed 대기한 뒤 신규 runtime High 이상을 차단하도록 구현됐다. 다만 신규 publisher workflow는 기본 브랜치 선반영 후에만 동작하므로 public fork 고위험 probe가 아직 없고, 백엔드 헌법의 CVSS 7 build fail 서술과 주간 `continue-on-error` full scan의 outcome/SLA 차이도 남아 있다. **2026-08-26 실측 — base 스냅샷 부재가 병합을 영구 차단하는 축**: main 커밋 하나(#500 머지 `8c850384`)에 push 트리거 producer 실행이 누락돼(같은 SHA의 워크플로 실행이 publisher 2건뿐) base 스냅샷이 0이 됐고, 그 커밋을 base 로 삼는 PR 의 `secret-scan` 이 600초 대기 후 `snapshots compared for the base SHA (0) and the head SHA (1) do not match` 로 fail-closed 했다. **대기로는 해소되지 않는다** — base 스냅샷을 사후 생성해 줄 자동 경로가 없어 후속 PR 이 모두 막힌다. 해소는 `gh workflow run dependency-submission.yml --ref main` 한 번이며, 실측으로 재실행 후 green 을 확인했다. 게이트 자체는 정상 동작했고 빠진 것은 base 쪽 데이터다. **2026-08-26 두 번째 양상 — head 스냅샷 부재**: `update-visual-baseline` 워크플로가 만든 커밋은 GitHub 의 재귀 트리거 방지로 후속 워크플로가 `action_required` 로 대기해, producer 가 돌지 않아 **head SHA 에 스냅샷이 없다**는 이유로 같은 게이트가 fail-closed 했다(`No snapshots were found for the head SHA`). base 축(앞 항목)과 원인이 다르며, 해소도 다르다 — 대기 중인 producer run 을 승인하거나 사람이 만든 커밋을 하나 더 얹어 정상 트리거시킨다. 즉 **워크플로가 커밋을 만드는 경로(VRT 기준선 재생성)는 구조적으로 이 게이트와 충돌한다**. | [PR graph producer](../../.github/workflows/dependency-submission.yml), [trusted publisher](../../.github/workflows/dependency-submission-publish.yml), [snapshot readiness](../../scripts/dependency-snapshot-readiness.mjs), [CI workflow](../../.github/workflows/ci.yml), [dependency workflow](../../.github/workflows/dependency-check.yml), [Atlas gaps](../../frontend/public/governance_harness_atlas.html#content-gaps) | producer/publisher를 기본 브랜치에 반영한 뒤 public fork에서 신규 runtime High 의존성을 넣은 probe PR로 artifact 제출·600초 readiness·dependency review 차단을 순서대로 확인한다. 별도로 주간 full scan의 triage·issue·해결 SLA와 헌법 outcome을 사용자 승인으로 정렬한다. **2026-08-29 진단 축 해소.** readiness 가 실패 시 **어느 쪽 SHA 가 비었는지 분류하고 해소 명령을 함께 출력**한다(`classifySnapshotWarning`·`resolutionGuidance`). 자동 복구는 **적대적 검토 끝에 거부**했다 — `secret-scan` 은 PR 체크아웃 코드를 실행하므로(`node --test "scripts/*.test.mjs"`) 그 잡에 `actions: write` 를 주면 PR 이 자기 브랜치 ref 로 producer 를 dispatch 해 **스스로 head 스냅샷을 위조**할 수 있고, 정작 fork PR 은 토큰이 read-only 로 강등돼 힐러가 작동하지 않는다(위험한 곳에서만 동작). 같은 변경에서 `submit-trusted-snapshot` 의 ref 가드 부재도 닫았다 — 워크플로 주석은 "trusted ref만 contents:write" 라고 약속했는데 가드는 이벤트 이름 하나뿐이라 `workflow_dispatch` 로 **임의 브랜치의 빌드가 write 토큰으로** 돌 수 있었고 계약이 그 약한 조건을 동결하고 있었다. 경고 문자열 census(공개 PR 코멘트 279 표본)로 확정한 사실: base 부재는 count 형태(`The number of snapshots compared for the base SHA (0) and the head SHA (1) do not match. You may see unexpected additions in the diff.`)로만 나타나고 **base 전용 문구는 존재하지 않는다**; head 부재는 전용 문구와 count 형태 **둘 다**로 온다; `dependency-review-action` 은 retry timeout 후 `Retry timeout exceeded. Proceeding...` 을 찍고 실패하지 않으므로 fail-closed 는 readiness 단독 책임이다. **⚠ 같은 조사에서 새 fail-open 축을 발견했다**: base·head **양쪽 모두 스냅샷이 0** 이면 GitHub 이 경고를 아예 보내지 않을 수 있고(액션 PR #452 "only repositories that have canonical snapshots will receive warnings of any kind"; count 문구는 0==0 이라 "do not match" 가 성립하지 않는다), 그러면 readiness 가 ✅ 로 통과하고 dependency review 는 **아무것도 비교하지 않은 채** green 이 된다. 공개 PR 코멘트 279 표본에 이 조합이 0건이라 실제 동작을 확인하지 못했다 — 이 저장소는 main 이 항상 스냅샷을 갖고 있어(최근 25 push tip 누락 0) 현재 노출은 낮지만, 파생 제품이 스냅샷 없이 시작하면 게이트가 처음부터 무력하다. 닫으려면 헤더가 빈 경우에도 base SHA 의 스냅샷 존재를 별도로 확인해야 하는데, 그 조회에 필요한 권한과 fail-open 여부 확정이 선행이다. **남은 것**: head 부재의 하위 원인 7가지는 전부 같은 경고를 내므로 문자열로 갈리지 않는다 — 자동 판별에는 `actions: read` 가 필요하나 그 잡이 PR 코드를 실행하고 이 저장소가 재사용 base 라(private 파생에서 다른 실행의 로그·아티팩트 노출) 부여하지 않았다. 대신 가르는 조회 명령을 안내한다. public fork 고위험 probe 와 주간 full scan SLA 정렬은 그대로 미해결이다. | 보안/사용자 |  2026-08-26 |
| GAP-ZDM-001 | P1 | open | zero-downtime | **2026-08-30 선행 Expand 축을 닫았다** — Contract 성격(DROP TABLE/SEQUENCE·DROP COLUMN·RENAME) waiver 는 `expandMigration` 으로 선행 Expand 를 지목해야 하고, 그 파일이 실재하며 Flyway 버전이 **엄격히 앞서야** 한다. 자기참조(같은 마이그레이션에서 Expand+Contract)는 차단하고, 버전을 읽지 못하면 통과가 아니라 위반이다(fail-closed). 버전은 문자열이 아니라 세그먼트로 비교한다 — 문자열이면 `V2_9` 가 `V2_72` 보다 뒤로 판정된다. ⚠ 종전에는 헌법 제7조가 Expand→Sync→Redirect→Contract 4단계를 "예외 없이" 요구하는데 하네스는 파괴적 DDL 차단과 waiver 메타데이터만 봐서, **한 파일에서 Expand 와 Contract 를 함께 해도 waiver 만 있으면 통과**했다. **남은 축: 관측 기간(호환 창)과 backfill 완료 증거** — 이 저장소에는 release·배포 시점을 나타내는 모델이 없어 "몇 번의 릴리스가 지났는가" 를 판정할 근거가 없다. 없는 근거로 필드를 만들면 첫 실제 사용에서 맞지 않으므로 지어내지 않았다. 부수: 구조화 `waivers` 는 현재 0건이라 이 규칙의 실물 소비자가 아직 없다 — 합성 JSON 으로 green·red 를 증명했다(가짜 waiver 와 SQL marker 를 저장소에 남기지 않기 위해서다). | [ZeroDowntimeMigrationLinterTest](../../api-server/src/test/java/nuri/api/harness/ZeroDowntimeMigrationLinterTest.java), [Atlas gaps](../../frontend/public/governance_harness_atlas.html#content-gaps) | 관측 기간·backfill 증거를 요구하려면 **release/배포 시점 모델이 선행**이다 — 무엇을 "선행 릴리스" 로 셀지 정의가 없으면 검사할 수 없다. v* 태그(DEC-OPS-021)를 그 기준으로 쓸지, 별도 배포 원장을 둘지를 먼저 결정한다. 그 전까지 선행 Expand 지목이 4단계 중 검증 가능한 유일한 축이다. | DB/사용자 | 2026-08-30 |
| GAP-DATA-001 | P1 | blocked-external | legacy crypto data | 레거시 password hash와 이전 ARIA key 암호문이 운영 데이터에서 0건인지 저장소 테스트만으로 증명할 수 없다. | [EgovPasswordEncoder](../../business-core/src/main/java/nuri/business/security/service/EgovPasswordEncoder.java), [RrnoEncryptionConverter](../../business-core/src/main/java/nuri/business/domain/common/RrnoEncryptionConverter.java), [verification blindspots](../../docs/04-operations/verification-blindspots.md) | 권한 있는 read-only census와 전환 증거를 확보한 뒤 호환 adapter 제거 여부를 결정한다. | 보안/DB 운영 | 2026-08-19 |
| GAP-OPS-002 | P1 | blocked-external | backup and DR | 운영 백업의 존재·복원 가능성·RTO/RPO는 repository CI나 로컬 compose로 검증되지 않는다. | [verification blindspots](../../docs/04-operations/verification-blindspots.md) | 승인된 격리 환경에서 restore drill을 수행하고 무결성·시간·소유자 증거를 secure channel에 남긴다. | 운영 관리자 | 2026-08-19 |
| GAP-FE-001 | P3 | accepted-risk | frontend security | `script-src`의 `unsafe-inline`은 2026-08-20 요청별 nonce로 제거 완료다(DEC-OPS-011: PPR 포기 확정, proxy.ts 단일 소스, cacheComponents off + 루트 force-dynamic, next-themes에는 x-nonce로 전파, CI e2e green). `strict-dynamic`은 Next lazy chunk 비호환으로 채택 불가가 실측됐다(CI run 32310837353). 남은 `style-src`의 `unsafe-inline`(Phase 3)은 **2026-08-30 측정 결과 accepted-risk 로 전환**한다. 설치본 소스 실측이 비대칭을 드러냈다 — framer-motion 12.43.0 은 `createElement("style")` 직후 `if (nonce) style.nonce = nonce` 로 **nonce 를 지원**하지만(주입도 AnimatePresence popLayout 한정), sonner 2.0.7 은 모듈 최상위에서 `__insertCSS` 를 2회 호출해 전체 스타일시트를 `<style>` 로 삽입하면서 패키지 전체에 `nonce` 문자열이 **0건**이라 끌 수도 nonce 를 붙일 수도 없다. 따라서 `style-src-elem` 에서 `unsafe-inline` 을 빼면 **토스트가 전부 무스타일**이 되고, `style-src-attr` 은 React style prop 때문에 어차피 `unsafe-inline` 이 필요하다 — 쪼개면 지시문만 둘로 늘고 보안 이득이 0 이다. 이 판정은 `csp-policy` 계약이 고정하며, 세분화를 시도하면 red 가 되어 재측정을 강제한다. CSP 위반 리포트 수집 채널 결정(PD-CSP-002)도 별도 대기다. | [proxy.ts](../../frontend/src/proxy.ts), [csp-policy contract](../../frontend/src/__tests__/csp-policy.test.ts), [pending decisions](../../docs/04-operations/pending-decisions.md) | **재개 조건은 sonner 의 nonce 지원 또는 교체**다 — 그 하나가 유일한 차단 요인이며, 해소되면 `style-src-elem` 을 nonce 기반으로 좁힐 수 있다. 그때 `csp-policy` 계약이 red 로 알린다. CSP 위반 리포트 수집 채널 결정(PD-CSP-002)은 별도로 남는다. | 보안/제품 소유자 | 2026-08-30 |
| GAP-UIQ-001 | P2 | deferred | ui quality evidence | UI quality baseline 의 8개 시나리오는 모두 `partial-automated-evidence`이고 manifest `currentBaseline.status`는 `unmeasured`다. 자동 축은 r12 가 exact 96/96 state·48/48 performance·assertion 156/156·axe 위반 0 으로 닫혔지만, r12 에는 실행 시점 protocol hash 가 없어 ADR-0005 상 영구히 `measured` 자격이 없다. 수동 접근성 48건 중 40건은 전문가 검토 대기, NVDA/Chrome 8건은 외부 차단이다. 2026-08-22 DEC-OPS-012 로 새 authoritative run(r13)과 수동 수집을 보류하고 UI 개선을 우선했다. 구현물(launch 실행기·combined summary v2 계약)은 보존돼 있어 재개에 추가 구현이 필요 없다. | [사용자 런북 §8](../../docs/04-operations/ui-ux-modernization-user-action-runbook.md), [baseline protocol §13](../../docs/04-operations/ui-ux-baseline-protocol.md), [ADR-0005](../../docs/02-architecture/decisions/ADR-0005-ui-quality-durable-evidence.md) | 지정 접근성 평가자와 승인된 Windows 기록 환경을 확보하면 protocol hash 를 기록한 새 run 과 수동 48건을 같은 combined summary 로 발행한다. 그전까지 `measured` 선언·protocol hash 소급 대입·수동 항목 자동 충족은 금지다. ⚠ **2026-09-05 신설 — 이 gap 에 기계적 기한이 생겼다.** `config/ui-quality-scenarios.json` 의 reviewBy 28건을 상한인 2026-11-19 로 연장했는데(종전 2026-10-31), `asOf 2026-08-21 + maxReviewDays 90` 이 천장이고 둘 다 계약 소스에 동결돼 있다(`REVIEWED_ON` exact 비교 · `maxReviewDays must remain exactly 90`). 따라서 **2026-11-20 부터는 `reviewBy >= TODAY` 와 `<= asOf+90` 을 동시에 만족하는 날짜가 0개**가 되어 config 만으로는 어떤 값도 넣을 수 없고 required `secret-scan` 이 red 가 된다(상한 +1일 red 실측 완료). 그때 가능한 선택지는 둘뿐이다 — ⓐ 외부 평가자·환경을 확보해 실제 재검토를 완료하고 `asOf`·`REVIEWED_ON` 을 함께 올린다, ⓑ 계약 소스의 상한 정책 자체를 사용자 승인으로 바꾼다. **연장으로 미루는 경로는 그날부터 존재하지 않는다** — 이 벽은 의도적으로 남겼다. | 접근성 소유자/사용자 | 2026-09-05 |
| GAP-OPS-001 | P2 | blocked-external | verification | NVD 기반 dependency scan, 실제 k6 부하, 인증된 admin ZAP은 외부 자격·환경이 없으면 완전 검증되지 않는다. | [verification blindspots](../../docs/04-operations/verification-blindspots.md) | 필요한 secret·대상 환경·운영 창구가 준비되면 스킵 없는 증거를 재수집한다. | 저장소/운영 관리자 | 2026-08-18 |
| GAP-AGENT-001 | P2 | deferred | coordination | 공용 메모리는 지속 지식만 공유하며 실시간 claim·lock·presence 조정은 아직 설계 상태다. | [coordination design](../../docs/02-architecture/dual-operator-coordination.md), [AGENTS memory rule](../../AGENTS.md#documentation-and-memory) | 실제 동시 편집 충돌 비용이 정당화될 때 별도 coordination protocol을 구현한다. | 사용자 | 2026-08-18 |
| GAP-SEC-001 | P1 | blocked-external | secret lifecycle | 과거 노출 가능 자격의 외부 회전·폐기 완료 여부는 저장소만으로 증명할 수 없다. | [verification blindspots](../../docs/04-operations/verification-blindspots.md) | provider별 회전·dangling credential 폐기 증거를 secure channel에서 확인한다. 값이나 로컬 파일은 저장소에 기록하지 않는다. | 사용자/운영 관리자 | 2026-08-19 |
| GAP-SEC-003 | P2 | needs-revalidation | attachment authorization | 온라인 서비스 계층에서 확인된 기존 첨부 assignment writer는 공통 `AttachmentAssignmentPolicy`의 원 업로더 gate와 exact census에 결속됐다. 게시판은 owner/admin 확인을 물리 파일 변경보다 먼저 수행하고, 읽기 정책은 `PERSONAL` 참조의 소유 grant를 별도로 판정하며 참조 해석 실패를 fail-closed 처리하므로 mixed shared+personal 권한 확대 축도 닫혔다. 로컬 E2E DB의 aggregate-only census(식별자 조회·출력 없음)에서는 등록된 참조 테이블의 non-null 첨부 참조와 팝업 내부 첨부 URL이 모두 0건이었다. 다만 관리/운영 환경에서 도메인 참조 생성 actor와 파일 마스터 원 업로더를 비교하는 aggregate provenance census를 수행하지 않아 기존 불일치 여부는 미확인이고, 직접 DB 쓰기·import 경로는 서비스 정책을 우회할 수 있다. | [AttachmentAssignmentPolicy](../../business-core/src/main/java/nuri/business/service/file/AttachmentAssignmentPolicy.java), [assignment writer census](../../api-server/src/test/java/nuri/api/harness/AttachmentSourceRegistryLinterTest.java), [FileAccessPolicy](../../business-core/src/main/java/nuri/business/service/file/FileAccessPolicy.java), [JdbcAttachmentReferenceResolver](../../business-core/src/main/java/nuri/business/service/file/JdbcAttachmentReferenceResolver.java), [BoardService](../../business-app/src/main/java/nuri/business/service/board/BoardService.java) | 승인된 관리/운영 환경에서 원시 값·식별자를 노출하지 않는 read-only aggregate provenance census로 기존 참조의 source actor↔원 업로더 불일치를 확인한다. 이어 직접 DB/import 경로에 provenance 또는 동등한 assignment 강제를 둘지 결정한다. 그 전에는 전역·운영 전체 보호 완료를 주장하지 않는다. | 보안/도메인·운영 소유자 | 2026-09-06 |
| GAP-STORAGE-001 | P2 | open | attachment storage | DB↔파일 저장소 드리프트는 **한 방향만** 탐지된다. 2026-08-26 에 ① 실물 부재를 일반 404 와 타입으로 구분해 ERROR + 저장 경로로 기록하고(`StorageObjectMissingException`) ② 관리자 점검 엔드포인트·화면(`GET /api/v1/admin/files/integrity`, 모니터링 허브 OBSERVABILITY 탭)을 만들어 "어긋나면 드러나게" 는 닫혔다. 남은 축 셋: **(a) 역방향은 2026-08-29 에 닫았다** — 같은 보고서가 `general/<sn>` 2단 열거로 저장소 실물을 훑어 대응 레코드가 없는 것을 **후보**로 센다. 포트는 넓히지 않았다(기존 `loadAll` 만 사용). ⚠ 설계에서 걸러낸 함정 넷: ① **커밋 전 업로드와 진짜 고아는 저장소에서 완전히 같은 모습**이라 확정할 수 없다 — "고아" 가 아니라 "후보" 로 보고하고 자동 삭제하지 않는다 ② V2_72 가 마스터 PK 를 문자열 `atch_file_id` 에서 BIGINT `atch_file_sn` 으로 바꾸면서 `file_strg_path` 는 갱신하지 않아, **그 이전 첨부의 디렉터리 이름은 현재 번호와 무관**하다 — 숫자로 읽히지 않는 디렉터리는 고아가 아니라 **판정 불가**로 센다(고아로 셌다면 멀쩡한 파일을 지우게 했다) ③ `(file_strg_path, strg_file_nm)` 에는 인덱스가 없어 경로 역조회는 풀스캔이므로 인덱스가 있는 `atch_file_sn` 으로 조회하고, 돌려받은 행의 경로가 실제로 그 디렉터리인지 다시 확인한다 ④ `loadAll` 은 `Files.walk` 스트림을 닫지 않고 반환하므로 호출부가 닫는다(디렉터리 핸들 고갈). 보고서에 **훑은 저장소 루트의 절대 경로**를 싣는다 — 설정 기본값이 상대 경로(`./storage/uploads`)이고 오버라이드가 없어 작업 디렉터리에 따라 다른 트리를 보며, 실제로 이 저장소에 루트가 둘 존재한다. **(b) 정기 점검 없음** — 관리자가 누를 때만 돈다(전량 스캔이라 주기 조회는 의도적으로 배제했다). 즉 아무도 누르지 않으면 여전히 사용자가 먼저 발견한다. **(c) 로컬 구현만 존재** — `FileStorageService` 구현체는 `LocalFileStorageService` 하나이고 오브젝트 스토리지 구현이 없다. `exists()` 계약은 원격 저장소에서 호출당 네트워크 왕복이 되므로 전량 스캔 비용이 로컬과 근본적으로 다르다. | [FileStorageService](../../foundation/src/main/java/nuri/foundation/core/storage/FileStorageService.java), [AttachmentIntegrityService](../../business-core/src/main/java/nuri/business/service/file/AttachmentIntegrityService.java), [StorageObjectMissingException](../../foundation/src/main/java/nuri/foundation/core/storage/StorageObjectMissingException.java), [점검 화면 계약](../../frontend/src/app/admin/system/monitoring/__tests__/MonitoringHubClient.test.tsx) | (a) 남은 것은 **후보를 확정으로 승격하는 절차**다 — 유예 간격을 둔 2회 점검에서 모두 나온 것만 확정하는 방식이 유력하나, 점검 결과를 남기는 경로((b))가 선행이다. `general/` 바로 아래 낱개 파일과 상한(5만 디렉터리) 초과분은 판정 불가로만 보고한다. (b) 점검 결과를 남기고 임계 초과 시 알리는 경로를 정한다(주기 실행은 저장소 규모 근거가 선행). (c) 오브젝트 스토리지 구현 시 `exists()` 를 배치 조회로 승격하고 전량 스캔 대신 표본·증분 스캔을 재설계한다. | 인프라/운영 소유자 | 2026-08-29 |
| GAP-CODE-001 | P3 | accepted-risk | institution code | **2026-09-08 사용자 결정(PD-CODE-001, DEC-OPS-069)으로 조회 전용 유지가 확정됐다** — 원장 반영 경로를 만들지 않는다. 아래는 그 결정의 근거이자 재개 조건이다. 기관코드 **수신 이력을 원장에 반영하는 경로가 없다**. `InstitutionCodeService.updateInstitutionCodeRecptn` 은 수신 로그 행의 `procSe` 만 완료로 바꾸고, 원장(`tb_inst_cd`)에 쓰는 `institutionCodeRepository.save` 는 저장소 전체에서 관리자 수기 등록(`insertInstitutionCode`) 한 곳에서만 호출된다. 수신 로그는 원장과 같은 필드 집합(allInstNm·uprInstCd·instTypeLclsf 등 20여 개)을 이미 나르므로 payload 는 갖춰져 있다. 막는 것은 `chgSeCd`(변경구분)의 **값 도메인 미확정**이다 — 화면은 1/2/3(신규·수정·정제)으로 해석하고 이 모듈 테스트는 "I" 를 쓰며, DB 주석은 '변경구분코드' 뿐이고 seed·enum·계약이 없다. 근거 없이 해석하면 코어 데이터를 잘못 덮어쓰거나 지운다(H4). 2026-08-28 에 화면·서버가 하지 않는 일을 약속하던 축은 닫았고(확인·성공 문구를 실제 동작으로 정정, 알 수 없는 변경구분을 '신규'로 지어내던 폴백 제거) 양방향 계약을 뒀다 — 근거 없는 원장 쓰기를 주입하면 red 다. 부수: prod baseline 은 `chg_se_cd varchar(12)` 인데 테스트 스키마는 `varchar(1)` 로 폭이 다르다. | [InstitutionCodeService](../../business-core/src/main/java/nuri/business/service/code/InstitutionCodeService.java), [원장 무변경 계약](../../business-core/src/test/java/nuri/business/service/code/InstitutionCodeServiceTest.java), [화면 문구 계약](../../frontend/src/app/admin/system/codes/institution/__tests__/InstitutionCodeClient.test.tsx) | `chgSeCd` 의 값 도메인을 표준·공급처 명세로 확정한 뒤(공통코드 seed 또는 enum) 반영 경로를 설계한다. 삭제/정제 분기는 코어 데이터 DML 이므로 사용자 승인 경계다. 설계 시 두 스키마의 컬럼 폭 불일치도 함께 판정한다. | 코드 표준/DB 소유자 | 2026-08-28 |
| GAP-CMTY-001 | P3 | open | community membership | **2026-09-06 DEC-OPS-043 로 절차 부재 축은 닫혔다** — 가입 신청(`mbrSttsCd='A'`)을 읽고 승인(`P`)·반려(행 삭제)로 옮기는 API 와 관리자 화면(지식 허브 '커뮤니티 관리 → 회원 관리'), 사용자 상세의 내 멤버십 상태 표시가 생겼고 상태 어휘는 `CommunityMemberStatus` 가 정본이다. **남은 것은 멤버십이 아직 아무 기능도 열지 않는다는 점**이다 — 커뮤니티별 게시글·권한 같은 회원 전용 콘텐츠 경로가 없고(`BoardMaster.cmntySn` 컬럼과 `BoardMasterRepository.findByCmntySnAndUseYn` 은 있으나 호출자 0건), 화면도 그 사실을 고지한다('회원 전용 기능은 아직 제공되지 않습니다'). 부수: 승인 권한은 시스템 관리자(ADMIN/SYSTEM)뿐이다 — `mngrYn='Y'` 를 부여하는 경로가 없어 커뮤니티 운영자 위임이 없고, 탈퇴·강제 탈퇴 전이도 만들지 않았다(회원 행은 반려 대상이 아니다 — 400). 가입 API 는 인증 사용자면 통과하므로 '내부 임직원 전용' 을 집행하는 게이트는 여전히 없다(화면도 표방하지 않는다). `CommunityUser.userId` 는 esntlId 축이며 회원 목록의 이름 해석은 `UserRepository#findByEsntlIdIn` 으로 한다 — loginId 로 join 하면 조용히 0건이 되는 함정은 코드 주석과 서비스 테스트가 고정한다. | [CommunityService](../../business-app/src/main/java/nuri/business/service/system/content/community/CommunityService.java), [CommunityMemberStatus](../../business-app/src/main/java/nuri/business/domain/system/content/community/CommunityMemberStatus.java), [CommunityMembersPanel](../../frontend/src/components/business/community/CommunityMembersPanel.tsx), [화면 문구 계약](../../frontend/src/app/cop/cmy/selectCommunityDetail/[id]/CommunityDetailHubClient.pending.test.tsx) | 커뮤니티 귀속 콘텐츠(커뮤니티별 게시판 노출·열람 권한)를 설계할 때 회원 판정(`APPROVED`)을 그 경계에 쓰고, 그때 운영자 위임(`mngrYn`)과 탈퇴 전이를 함께 결정한다. 그 전까지 회원 상태는 기록일 뿐이며 화면이 그 사실을 계속 말한다. | 제품/도메인 소유자 | 2026-09-06 |
| GAP-BLOG-001 | P3 | open | blog domain | **블로그 도메인은 CRUD 가 없는 잔존 도메인이다.** 엔티티 2(`Blog`/`tb_blog_info`, `BlogUser`/`tb_blog_user_map` 복합키), 리포지토리 2, 리스너 1, 템플릿 기여자 1 뿐이고 **서비스 CRUD·DTO·컨트롤러·API operation 이 0** 이다. 살아 있는 역할은 둘 — `BlogUserDeletionCleanupListener`(사용자 삭제 시 멤버십 정리, FK `tb_blog_user_map.user_id → tb_user_info.esntl_id` 결속) 와 `BlogTemplateReferenceContributor`(템플릿 삭제 참조 차단, DEC-OPS-047). **그런데 `tb_blog_info` 에 행을 만드는 경로가 저장소 전체에 없고 시드도 없어 두 역할은 항상 0 을 반환한다.** ⚠ 커뮤니티(GAP-CMTY-001)와 **같은 형태이되 한 단계 뒤다** — 멤버십·운영자 플래그(`mngrYn`)·가입/탈퇴일·상태코드(`mbrSttsCd`)를 갖춘 모델이 있고 `BoardMaster.blogSn`·`Board.blogSn` 귀속 컬럼도 있으나, 커뮤니티에는 API 13본·화면 3개가 있는 반면 블로그는 그것마저 없다. 유일한 `BoardMaster` 생산 UI(`BoardMakerWizard`)는 `blogYn: 'N'` 을 하드코딩해 블로그형 게시판을 만들 방법이 없다. 부수: V2_70 이 사용자 승인을 받아 블로그 PK 와 참조 폐포(`tb_blog_user_map`·`tb_bbs_item`·`tb_bbs_master`)를 BIGINT IDENTITY 로 현대화했고 표준용어 `BLOG_SN` 을 등록했다 — 즉 이 도메인은 "제거 대상" 으로 취급된 적이 없다. `isg` 와 함께 demo pack(rank 3) 소유라 core·collaboration 프로필에서는 이미 빠진다. | [Blog](../../business-app/src/main/java/nuri/business/domain/blog/Blog.java), [BlogUserDeletionCleanupListener](../../business-app/src/main/java/nuri/business/service/blog/listener/BlogUserDeletionCleanupListener.java), [BlogTemplateReferenceContributor](../../business-app/src/main/java/nuri/business/service/blog/template/BlogTemplateReferenceContributor.java), [V2_70](../../api-server/src/main/resources/db/migration/V2_70__blog_bigint_identity.sql), [demo pack manifest](../../config/reusable-base-profiles.json) | **단독으로 판정하지 않는다.** 커뮤니티 귀속 콘텐츠(GAP-CMTY-001 의 재개 조건)를 설계할 때 함께 결정한다 — 두 도메인이 같은 멤버십 모델이고 `BoardMaster` 의 `cmntySn`·`blogSn` 이 같은 미사용 축이라, 따로 정하면 한쪽을 두 번 뒤집게 된다. 그때 선택지는 ⓐ 커뮤니티와 같은 수준으로 API·화면을 만든다 ⓑ 도메인을 걷는다 이며, ⓑ 는 테이블 DROP 을 동반해 승인받은 PK 현대화를 되돌리는 파괴적 DB 변경이므로 사용자 승인 경계다. 그 전까지 두 부수 역할(멤버십 정리·템플릿 참조 차단)은 논리적으로 옳고 실행 결과가 언제나 no-op 인 상태로 유지한다. | 제품/도메인 소유자 | 2026-09-07 |
| GAP-WIRING-001 | P3 | open | api surface | **문서화된 operation 중 소비자가 없는 19건이 부채로 남아 있다.** DEC-OPS-048 이 신설한 `operation-consumer-census` 가 379 operation 을 소비(327) · springdoc 별칭 기계 파생(30) · 사유가 붙은 원장(22) 로 전수 분류하고, 그중 진짜 부채 `unwired` 를 **단조 감소 래칫**(현재 19)에 결속한다. 2026-09-07 부재 관리 배선(DEC-OPS-049)으로 22→19 가 됐다. 남은 19건의 성격은 셋으로 갈린다 — ① **화면은 있는데 그 기능만 없는 것**(설문 응답자 등록·상세·수정 3, 설문 템플릿 상세 2, 로그인 정책 생성 1, 만족도 수정·조정 2) ② **다른 경로가 이미 그 일을 하는 것**(자기 알림 생성·상세·삭제 3 — 관리자 발송은 DEC-OPS-042 의 별도 endpoint 가 담당, 부서 단위 일정 조회가 있는 평면 일정 목록 1, 관리자 통계가 있는 사용자 통계 1, 메뉴 생성 후보 1) ③ **제품 결정이 선행인 것**(공개 회원가입·ID 중복확인 2 — 화면이 없고 `SignupContractLinter` 가 특권 상승 회귀만 동결한다, `/api/v1/health` 1 — compose 는 `/actuator/health` 를 쓴다, 기관코드 상세 1 — GAP-CODE-001 의 원장 반영 결정 대기, 강제 500 진단 1 — `@Profile("!prod")`). ⚠ 이 목록은 "지워야 할 것" 이 아니다 — 배선·제거·유지 중 무엇이 옳은지는 항목마다 다르며, 래칫은 **늘어나는 것만** 막는다. **⚠ 2026-09-07 실측 — 이 래칫은 하한이지 총량이 아니다.** census 의 `consumed` 는 "생산 TypeScript 모듈이 그 operation 을 부른다" 를 뜻하지 "사용자가 도달하는 화면이 쓴다" 를 뜻하지 않는다. 반례가 확인됐다 — `deleteRespondent` 는 `SurveyAdminService.deleteRespondent()` 가 생성 실행기를 불러 consumed 로 집계되지만 그 서비스 메서드를 부르는 곳은 **자기 단위 테스트뿐**이다(`app/`·`components/` 호출부 0). 즉 백엔드에서 `UnreachableServiceLinter` 가 닫은 "유일한 참조가 단위 테스트" 패턴이 프런트 **서비스 계층 한 겹 아래**로 내려가 있다. 거친 1차 측정(서비스 메서드 선언 위치로 호출부를 귀속시키는 이름 기반 스캔)에서 후보 73건이 나왔으나 클래스 프로퍼티 화살표 함수를 `super` 로 잘못 귀속시키는 오탐이 섞여 있어 **그 수치는 신뢰할 수 없다** — 정확한 규모는 별도 계측이 필요하다. ⚠ 응답자 3건(`createRespondent`·`getRespondent`·`updateRespondent`)은 부채가 아니라 **의도적 보류**로 재분류했다 — `tb_srvy_rspdnt` 는 성명·성별·생년월일·전화번호를 담는 개인정보인데 `tb_srvy_rslt` 와 **ID 로 연결돼 있지 않고**(결과 테이블은 `rspns_nm` 문자열만, V2_0) 응답자 행을 만드는 코드 경로가 저장소에 없어 register 가 구조적으로 비어 있다. 여기에 등록 폼을 붙이면 **소비처 없는 개인정보 입력 표면**을 새로 여는 일이라 배선이 아니라 제품 결정이 선행이다. | [census 스크립트](../../scripts/operation-consumer-census.mjs), [원장](../../config/governance/operation-consumer-census.json), [계약](../../scripts/operation-consumer-census.test.mjs) | ①은 해당 화면 소유자가 배선한다(부재 관리가 첫 사례 — census 의 사유 문구가 다음 후보를 지목한다). ②는 중복 표면 축소 판단이 선행이며 DEC-OPS-041·044 의 선례를 따른다. ③은 각각의 제품·운영 결정(회원가입 정책 · health probe 대상 · GAP-CODE-001)이 먼저다. 래칫은 실제 해소가 있을 때만 내리고, 올리는 변경은 사유를 같은 커밋에 남긴다. **2026-09-07 축 2 게이트 신설로 계측이 확정됐다(DEC-OPS-051)** — TypeScript AST 로 호출부의 소유 메서드를 정확히 귀속(귀속 실패 0, 실패 시 red)하고, 서비스 밖 생산 코드에 `.<메서드명>(` 이 없으면 고아로 센다. 실측: 서비스 메서드 **341개 중 77개**가 화면 호출부 0이며 관련 operation 73건이다. 1차 스캔의 73건은 `super` 오귀속이 섞인 우연한 근사치였고 지금 수치가 정본이다. 예외 목록은 두지 않는다 — 77건을 원장에 적으면 그 목록이 곧 서랍이 되므로 **단조 감소 래칫**(`expected.screenOrphanMax`) 하나로 신규 유입만 막고 실패 시 전체 목록을 출력한다. **2026-09-07 잔여 50건 전수 분류(DEC-OPS-057)** — ①형(대체된 표면) 정리를 마쳤고 **잔여는 사실상 전부 ②형(기능 부재)**이다. **2026-09-08 사용자 결정 6건 집행(DEC-OPS-069·070)** — PD-AUTH-003·PD-ADBK-001·PD-CODE-001·PD-OPS-003 은 현 상태 유지로 확정(원장 사유를 결정 기록으로 갱신), PD-SRVY-001·PD-MYPG-001 은 표면 제거. operation 379→370, unwired 14→11, 화면 도달성 37→34. ⚠ 두 제거 모두 **결정 자료보다 상황이 나빴다** — 등록 폼만 없는 줄 알았는데 조회 화면이 이미 있었고 둘 다 항상 빈 목록이었다. 마이페이지는 그 값을 읽는 화면조차 없었고, e2e 테스트 하나는 두 갈래가 모두 통과하는 위장이었다. **2026-09-08 대체된 공통코드 삭제 2건 정리(DEC-OPS-068 후속)** — `CodeAdminService` 의 죽은 위임 메서드 `deleteClCode`·`deleteCmmnCode` 를 걷고 축 1 원장에 `superseded-surface`(supersededBy: `updateClCode`·`updateCmmnCode`)로 등재했다. 엔티티 대조로 확인한 근거: 서버 `delete()` 는 `useYn='N'` 이고 `update(..., useYn, ...)` 가 같은 상태에 도달하며 오히려 `lastMdfrId` 를 남긴다. 화면 도달성 래칫 39→37, **unwired 는 14 그대로**(부채가 아니라 검증된 대체 관계로 기록). **2026-09-08 로그인 정책 해제 배선(DEC-OPS-068)** — 래칫 40→39. ⚠ **DEC-OPS-062 의 B형 판정 하나를 정정했다** — `deleteLoginPolicy` 를 "대체된 표면" 으로 봤으나, 정책을 비우면 행이 남아 목록에 `regYn='Y'` 로 계속 표시되고 지워야 `regYn='N'` 이 된다. 즉 정책을 완전히 해제할 방법이 없었다. 남은 B형 2건(`deleteClCode`·`deleteCmmnCode`)은 서버 `delete()` 가 `useYn='N'` 이고 화면의 사용여부 토글이 정확히 같은 결과를 내므로 판정이 유효하다. **2026-09-08 보안 3화면 일괄 삭제 UI 신설(DEC-OPS-067)** — 축 2 C형(일괄 삭제 UI 부재) 3건을 **삭제가 아니라 UI 신설**로 해소했다(래칫 43→40). ⚠ 폼 검증 census 가 신설 액션과 자식 폼의 **상호 잠금 부재**를 잡았다 — 그대로 뒀다면 권한 저장 도중 일괄 삭제가 나가 방금 만든 권한이 지워질 수 있었다. **2026-09-08 축 1 잔여 14건 전수 분류 — 배선 프로그램 종결.** 축 2(DEC-OPS-062)와 같은 지점에 도달했다. **A. 목록으로 충족(단건 조회 불필요) 3건**(`getTemplate`·`getNotification`·`getMenuCreationList` — 화면이 목록 projection 으로 필요한 값을 이미 받는다) · **B. 다른 경로가 이미 함 2건**(`getScheduleList` 는 부서 범위 endpoint, `getConnectStats` 는 관리자 통계) · **C. 제품 결정 선행 3건**(`signup`·`checkIdDplct` — 이 제품은 관리자 사용자 관리로 계정을 만든다, `createNotification` — 자기 알림 생성의 제품상 용도가 없다) · **D. 개인정보 보류 3건**(설문 응답자 등록·상세·수정 — DEC-OPS-050) · **E. 운영·진단 2건**(`checkHealth` — compose 는 `/actuator/health` 를 쓴다, `triggerError` — `@Profile("!prod")` 수기 진단) · **F. 다른 gap 대기 1건**(`getInstitutionCodeDetail` — GAP-CODE-001 의 원장 반영 결정). ⚠ **자율 배선 대상은 0건이다.** 이 연속 작업에서 배선한 4건(만족도 수정·대리 삭제, 알림 단건 삭제, 로그인 정책 신규 등록)은 모두 "서버는 완비인데 화면이 없어 사용자가 할 수 없던 일" 이었고, 그중 둘은 **기능이 절반만 동작**했다 — 로그인 정책은 목록에 나오는 정책 미보유 사용자에게 저장하면 404 였고, 만족도는 작성자 정보 없는 레거시 행을 아무도 지울 수 없었다. 남은 14건에는 그런 성질이 없다. **2026-09-08 로그인 정책 신규 등록 배선(DEC-OPS-066)** — 축 1 unwired 15→14. ⚠ 목록은 전체 사용자를 좌측 조인으로 돌려주는데 저장 경로가 수정 하나뿐이라, **정책이 없는 사용자에게 IP 제한·OTP 를 걸면 404** 였다 — 보안 기능이 절반만 동작했다. **2026-09-08 알림 단건 삭제 배선(DEC-OPS-065)** — 축 1 unwired 16→15. 읽은 알림이 쌓여도 정리할 방법이 없었다(보존 스케줄러는 기본 비활성·보존 개월 미정). 일괄 삭제는 서버 경로가 없어 범위가 제품 결정으로 남는다. **2026-09-08 만족도 관리자 대리 삭제 배선(DEC-OPS-064)** — 축 1 unwired 17→16. ⚠ 이 항목을 "대체된 표면" 으로 볼 뻔했다 — 일반 삭제가 owner-or-admin 이라 관리자도 지울 수 있어 보이지만, `assertCanModify` 는 **작성자 정보가 없으면 관리자도 거부**한다. ADR-0011 이전 익명 평가는 대리 삭제로만 지울 수 있었고 그 화면이 없었다. **2026-09-08 만족도 수정 배선(DEC-OPS-063)** — 축 1 unwired 18→17. ADR-0011 이 owner-or-admin 수정을 열어 뒀는데 화면이 없어 별을 잘못 누르면 지우고 다시 매겨야 했다. census 원장의 사유 문구가 그대로 다음 작업을 지목한 두 번째 사례다. 남은 `moderate`(관리자 강제 삭제)는 조정 화면 자체가 없어 별개 축이다. **2026-09-08 축 2 잔여 43건 전수 분류(DEC-OPS-062)** — **A. 단건·부가 조회 미사용 31건**(목록 projection 으로 충족 — 결함이 아니며 배선하면 왕복만 는다) · **B. 대체된 표면 3건**(`deleteClCode`·`deleteCmmnCode`·`deleteLoginPolicy`) · **C. 일괄 삭제 UI 부재 3건**(`deleteAuthors`·`deleteGroups`·`deleteRoles` — 단수는 화면에 있으나 부분 실패 의미가 달라 완전 대체는 아니다) · **D. 설계·결정 선행 3건**(메모보고 수정·삭제 — 인가 축 불일치, 주소록 사용자 검색 — `@PrivacyAccess` 개인정보 응답이라 배선이 곧 노출 표면 신설) · **E. 의도적 보류·소비처 없음 3건**. ⚠ **래칫 0 은 목표가 아니며, 43건 중 자율 배선 대상은 0건이다** — 최대 부류 A 를 "배선" 하면 신호만 좋아지고 제품은 나빠지며, D·E 는 전부 사용자 결정(개인정보 노출 범위·응답 계약 확장)이 선행이다. ⚠ 메모보고 수정·삭제는 **인가 축이 어긋나** 단순 배선이 불가능하다 — 서버는 `assertOwnerOrAdmin(frstRgtrId)`(loginId 축)인데 응답 DTO 에 그 필드가 없고 열람 인가는 esntlId 축(`userId`·`rptrId`)이다. 응답 계약 확장이 선행 결정이다. **2026-09-08 행정 구역 코드 정정 경로(DEC-OPS-061)** — 수정·삭제·단건 조회 3건을 배선했다(래칫 45→43). ⚠ **DEC-OPS-057 의 "잔여는 사실상 전부 ②형(기능 부재)" 판정을 정정한다** — 공통코드 분류·그룹의 `deleteClCode`·`deleteCmmnCode` 는 ①형(대체된 표면)이다. 두 서버 메서드는 `useYn='N'` 논리 삭제인데 화면의 분류·그룹 편집 폼에 이미 사용여부 토글이 있어 `updateClCode`·`updateCmmnCode` 로 **같은 결과**에 도달한다. 즉 이 축의 분류는 항목마다 실제 코드를 봐야 하며 래칫 수치만으로 성격을 단정할 수 없다. ⚠ 마이페이지 콘텐츠 추가·삭제(`createContent`·`deleteContent`)도 배선 대상이 아니다 — 2026-08-29 실측 주석이 기록한 대로 그 값을 읽는 화면이 없어(대시보드 위젯 SPI 구현 2개가 이 값을 쓰지 않는다) **켜고 꺼도 어느 화면에도 나타나지 않는다.** 소비처를 만드는 제품 결정이 선행이다. **2026-09-08 내 프로필 수정 배선(DEC-OPS-060)** — `updateMe`·`getMe` 를 헤더 계정 메뉴 다이얼로그에 이었다(래칫 46→45). ⚠ 배선하며 확인한 것: 이 경계는 **부분 수정**(null=유지, ""=지움)이라 바꾼 필드만 보내야 한다 — 설문 수정의 전체 치환과 반대이므로 한쪽 관례를 옮기면 데이터가 지워진다. 남은 마이페이지 콘텐츠 추가·삭제(`createContent`·`deleteContent`)는 `tb_indv_pg_conts` 에 시드도 생성 경로도 없어 ISG 와 같이 **구조적으로 비어 있을** 가능성이 높다(DB 실측 선행 필요). **2026-09-08 본인 비밀번호 변경 배선(DEC-OPS-059)** — DEC-OPS-032 가 관리자 초기화만 열고 남긴 빈 자리였다(래칫 47→46). ⚠ 남은 `updateMe`(내 프로필 수정)와 마이페이지 콘텐츠 추가·삭제는 아직 화면이 없다 — 특히 `tb_indv_pg_conts` 는 시드도 생성 경로도 없어 ISG 와 같이 **구조적으로 비어 있을** 가능성이 높다(DB 실측 선행 필요). **2026-09-07 1순위 완료(DEC-OPS-058)** — 설문 수정 3본을 배선해 래칫이 50→47 이 됐다(삭제가 아니라 기능으로 줄인 첫 배치). 남은 배선 우선순위: `updateSurvey`·`updateQuestion`·`updateItem`(설문이 등록·삭제만 되고 수정이 없다 — DEC-OPS-036 과 같은 유형) > `createContent`·`deleteContent`(마이페이지 콘텐츠 추가·삭제 없음) > `updateMe`·`changePassword`(본인 정보·비밀번호 변경 화면 자체가 없다 — DEC-OPS-032 는 관리자 초기화만 열었다) > 공통코드 분류·그룹·행정코드 삭제 3건 > 단건 조회 미사용 다수. **⚠ 축 2 의 고아는 최소 두 부류다(DEC-OPS-055 실측)** — ① **대체된 표면**(다른 경로가 이미 그 일을 한다: 배너·팝업 6, ISM 7, 게시글 쓰기 4) 은 삭제가 해소다. ② **기능 부재**(화면에 그 기능이 없다: `CodeAdminService` 의 공통코드 분류·그룹·행정코드 삭제·수정 8, 단건 조회 다수) 는 **삭제가 아니라 배선**이 해소이며 지우면 부재 사실만 사라진다. 두 부류를 한 숫자로 세고 있으므로 래칫만 보고 "지우면 된다" 고 읽지 않는다. **2026-09-07 판정 정정(DEC-OPS-054)** — 화면 소비 판정이 멤버 호출만 보다가 `.bind()` 재수출을 named import 로 직접 부르는 형태를 놓쳐 `deptScheduleService` 5건을 거짓 고아로 셌다. 두 호출 형태를 함께 보도록 고쳐 래칫이 64→59 가 됐다 — 앞선 77·64 수치는 그만큼 과대였다. **2026-09-07 2차 배치(DEC-OPS-053)** — 죽은 `IsmAdminService` 7메서드를 걷자 `InformalSanctionApiController` 의 12 operation 이 소비 0 으로 드러났다(죽은 서비스가 가리고 있었다). `superseded-surface` 카테고리로 기록했고 화면 고아는 64, unwired 는 18 이 됐다. **남은 판단: 백엔드 컨트롤러를 걷을 것인가.** 두 컨트롤러는 같은 `InformalSanctionService` 를 감싸지만 어느 쪽도 상대의 부분집합이 아니다 — `ApprovalApiController` 에는 `processed`·`task-types` 가, `InformalSanctionApiController` 에는 상세·수정이 있다. 기계적 중복 축소(DEC-OPS-041·044)가 아니라 "결재 상세·수정이 제품에 필요한가" 를 먼저 정해야 한다. ⚠ 고아 77건의 대표 유형은 **server action 중복**이다 — 예컨대 배너는 화면이 `promotionActions.ts` 에서 `executeGeneratedOperation(deleteBannerOperation, ...)` 을 직접 불러 `BannerAdminService.deleteBanner()` 가 통째로 죽어 있다. 즉 이 축의 부채는 "기능이 없다" 가 아니라 "같은 일을 하는 경로가 둘인데 하나가 죽었다" 인 경우가 많다. | API/제품 소유자 | 2026-09-07 |
| GAP-UI-001 | P3 | open | table sorting | 표의 열 정렬이 **서버가 내려준 현재 페이지 행만** 재배열하는 클라이언트 정렬이다(`createSortedRowModel` 이 `data` prop 위에서 동작). 2026-08-25 페이저 요약에 범위 고지를 넣고 계약으로 고정했다. **2026-08-26 OCI live 실측으로 남은 범위가 좁혀졌다.** ① 저장소 5종이 **이미 전부 최신순으로 정렬해 내려준다**(`ocrnYmd.desc()`·`crtDt.desc()`·`occrYmd.desc()`·`inqDt.desc()`) — 즉 "전체에서 가장 최근 N건"은 조건을 좁힌 결과의 1페이지이며, 빠져 있던 것은 기능이 아니라 그 사실을 화면이 말하지 않는다는 점이었다(문구 + 서버 정렬 양방향 계약으로 해소). ② 남은 것은 **날짜가 아닌 열로 전체 정렬**하는 경로다. 그런데 live 인덱스 실측에서 `tb_sys_log`·`tb_login_log`·`tb_privacy_log` 는 정렬·필터 컬럼에 인덱스가 **아예 없고**(PK 만), `tb_web_log`(occr_ymd)·`tb_user_log`(PK 선두 ocrn_ymd)만 있다. 행 수는 web 28,104 · sys 12 · 나머지 0이다. 즉 지금 서버 정렬 API 를 만들면 **인덱스 없는 컬럼 정렬을 API 로 승격**하는 셈이다. ⚠ 최초 실측 시점의 DB 는 저장소보다 뒤처져 있었으나(38/2.32), **2026-08-26 V2_84 까지 현행화해 재확인했다**. 결론은 같다: 저장소 [V2_20](../../api-server/src/main/resources/db/migration/V2_20__add_log_retention_index.sql)이 `tb_sys_log(12행)·tb_login_log(0행)는 규모상 인덱스 보류(비용>이득 실측)`로 **같은 판단을 이미 기록**해 두었고, 행 수도 그때와 같다. 새 근거 없이 그 결정을 뒤집지 않는다. | [정렬 구현](../../frontend/src/app/components/ui/standard-data-table.tsx), [범위 고지 계약](../../frontend/src/app/components/ui/__tests__/sort-scope-disclosure.test.tsx), [기본 정렬 고지 계약](../../frontend/src/__tests__/cross-stack/log-default-order-disclosure.test.ts), [카탈로그 G5](../../docs/02-architecture/work-screen-grammar-catalog.md) | **실사용 근거가 생기면** 진행한다 — 특정 화면에서 날짜 외 열의 전체 정렬 요구가 관측되면, 그 컬럼에 인덱스를 추가하는 Flyway 마이그레이션(사용자 승인 필요)과 정렬 키 allowlist 를 같은 변경에서 만든다. 그 전까지는 기간 필터·페이지당 건수·서버측 전량 export 로 대체 가능하며, 현재 정렬 범위는 화면이 고지한다. 별도로 **인덱스 부재 자체**(sys·login·privacy 의 날짜 컬럼)는 데이터가 쌓이면 기본 조회도 느려지는 축이라 용량 증가 시점에 재판정한다. | API/DB 소유자 |  2026-08-26 |
| GAP-AUTH-002 | P1 | open | authorization data | 권한 삭제가 **사용자 할당을 남긴다**. `AuthorManageService.deleteAuthor` 는 롤 매핑(`authorityRoleRepository`)과 메뉴 매핑(`menuAuthorityRepository`)만 정리하고 `tb_user_authrt_map` 은 건드리지 않는다. 그 테이블에는 `tb_authrt_info` 로의 FK 가 없어(V2_0 은 PK 만, V2_12 는 `tb_user_info` FK 만 추가) 삭제가 그대로 성공하고 행은 없어진 권한을 가리킨 채 남는다. ⚠ **같은 코드로 권한을 다시 만들면 그 사용자들이 아무도 배정하지 않은 권한을 그대로 물려받는다** — 권한 코드는 사용자가 입력하는 문자열이라 재사용이 흔하다. 2026-08-29 1차로 화면 문구를 실제 정리 범위로 정정했고, 2차로 **애플리케이션 계층 가드**를 넣었다 — `AuthorManageService.assertNoAssignedUsers` 가 보유자가 있으면 단건·일괄 삭제를 모두 거부하고 인원수를 알린다(회수 대신 차단을 택한 이유: 회수는 오삭제 시 복구 불가이고 인가 의미를 조용히 지운다 — H3). 화면도 '먼저 사용자 할당을 해제해 주세요' 로 바뀌었고 가드 부재는 계약이 red 로 잡는다. **2026-08-30 자기 검토가 이 서술의 과장을 잡았다** — 종전에는 "남은 것은 DB 제약 승격뿐" 이라고 적어 앱 계층이 닫힌 것처럼 읽혔으나, **쓰기 경로에는 존재 검증이 전혀 없어 정규 관리자 API 로 같은 끊긴 참조를 그대로 만들 수 있었다**(우회가 아니라 정상 경로). `saveUserAuthorities`·`saveDeptAuthorities` 가 `authrtId` 를 `tb_authrt_info` 와 한 번도 대조하지 않고 저장했고, JPA 연관이 `ConstraintMode.NO_CONSTRAINT` 라 DB 도 막지 않는다. 같은 날 두 쓰기 경로에 `assertAuthoritiesExist` 를 넣어 닫았다. 부수로 에러 코드도 정정했다 — 이 의미로 문서화된 `RESOURCE_IN_USE(409)` 가 이미 있고 부서 삭제 가드가 그것을 쓰는데, 이 가드만 `INVALID_STATE(400)` 를 써서 같은 도메인 의미가 두 상태 코드로 갈려 있었다. **남은 것은 DB 제약으로의 승격과 기존 고아 행 정리다** — 앱 가드는 애플리케이션을 우회하는 경로(직접 DML·다른 클라이언트)를 막지 못한다. | [AuthorManageService](../../business-core/src/main/java/nuri/business/service/auth/AuthorManageService.java), [V2_12](../../api-server/src/main/resources/db/migration/V2_12__cleanup_orphan_refs_add_user_fks.sql), [문구 계약](../../frontend/src/__tests__/cross-stack/honest-affordance-contract.test.ts) | ②(FK 로 차단)로 방향을 확정했고 앱 계층까지 구현했다. 남은 단계는 운영 DB 실측이 선행이다 — ⓐ read-only 고아 census(`SELECT COUNT(*) FILTER (WHERE a.authrt_cd IS NULL) FROM tb_user_authrt_map m LEFT JOIN tb_authrt_info a ON m.authrt_id = a.authrt_cd`) → ⓑ 백업 덤프 → ⓒ 고아 정리 DML → ⓓ Flyway FK(NO ACTION) 추가. V2_12 가 같은 테이블의 다른 축에서 이미 밟은 절차이며 그 선례를 따른다. ⓑⓒⓓ 는 코어 인가 데이터 DML·스키마 변경이라 사용자 승인이 필요하다. ⓐ 는 db-bridge 허용 범위이나 이 환경에 접속 변수가 없어 실행하지 못했다. | 보안/DB 소유자 | 2026-08-30 |

GAP-CONTRACT-001의 2026-09-06 보강은 길이·필수 제약에 더해 Memo/Work 보고 13필드와 DeptJob 계열 15필드, 총 28개의 서버 소유·응답 전용 필드를 Jackson/OpenAPI read-only로 고정했다. Work 보고 등록은 [서비스](../../business-app/src/main/java/nuri/business/service/report/WorkReportService.java)가 인증 loginId를 직접 확인하고, 수정·응답의 `rptYmd` 왕복도 복원한다. 이 완료 증거는 남은 미검사 DTO·공백 의미·root validation 사각을 닫았다는 뜻이 아니다.

## 재검증 대기

`needs-revalidation`과 `blocked-external` 항목은 외부 상태가 바뀌었다는 주장만으로 닫지 않는다. 날짜가 있는 API/UI 실측이나 운영 증거를 확보한 뒤 상태를 갱신한다.

## 해결 규칙

gap을 해결하면 정본 코드·문서·테스트를 먼저 갱신하고, 회귀 방지 실행 경로와 red 증거를 확인한 뒤 활성 표에서 제거한다. 장기 결정이 생기면 ADR 또는 [decisions.md](decisions.md)에 링크하고 상세 이력은 중복 복사하지 않는다.
