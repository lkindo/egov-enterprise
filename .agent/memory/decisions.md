---
schema_version: 1
memory_kind: decisions
status: active
authority: adr-index
scope: repository
sensitivity: public-repo-safe
verified_at: 2026-09-05
verified_against: 02ddf334e6267e9fb3fafd7be928fd8e3e2ce09b
canonical_sources:
  - ../../AGENTS.md
  - ../../docs/02-architecture/decisions/README.md
  - ../../.github/required-checks.json
  - ../../config/governance/gates.json
refresh_triggers:
  - accepted-decision
  - superseding-adr
  - project-rule-change
---

# 공용 결정 인덱스

## 범위와 승격 규칙

제품·아키텍처 결정의 정본은 [ADR 디렉터리](../../docs/02-architecture/decisions/README.md)다. 이 파일은 Accepted ADR과 장기적인 운영 결정을 찾기 위한 인덱스이며 ADR 내용을 복제하거나 바꾸지 않는다. 제안·보류 항목은 결정으로 기록하지 않고 [known-gaps.md](known-gaps.md)에 둔다.

- 허용 상태: `accepted`, `superseded`, `rejected`.
- 기존 결정을 소급 편집하지 않는다. 변경은 후속 ADR/DEC가 `supersedes`로 연결한다.
- 헌법과 `AGENTS.md`의 실질 정책 변경은 사용자 명시 요청이 먼저다.

## Accepted ADR index

| ID | 상태 | 결정 | 이유 | 정본 | 시행일 | supersedes |
|---|---|---|---|---|---|---|
| ADR-0001 | accepted | core/app 제품 경계와 main·release-tag 배포 기준을 분리한다. | 재사용 기반과 제품 구현의 결합을 낮추기 위해서다. | [ADR-0001](../../docs/02-architecture/decisions/ADR-0001-core-app-product-boundary.md) | 2026-08-15 | - |
| ADR-0002 | accepted | UI는 한국어 우선이고 API의 ko/en 메시지 계약은 유지한다. | 사용자 언어 일관성과 재사용 API 호환성을 함께 지키기 위해서다. | [ADR-0002](../../docs/02-architecture/decisions/ADR-0002-korean-first-frontend.md) | 2026-08-15 | - |
| ADR-0003 | accepted | 프런트엔드 UX 현대화는 사용자 과업을 우선하고, 브랜드 중립 프로필·WCAG 2.2·측정 기반 데이터 소유권·위험 기반 복구와 mutation 원칙을 적용한다. | 특정 미학과 일시적인 API를 core 헌법에 고정하지 않고 공공·민간 프로필이 같은 접근성·상태 계약을 공유하게 하기 위해서다. | [ADR-0003](../../docs/02-architecture/decisions/ADR-0003-frontend-ux-modernization-principles.md) | 2026-08-20 | - |
| ADR-0004 | accepted | 과업 중심 기본 내비게이션과 명시적 관리 센터를 결합한 하이브리드 IA를 prototype·research의 잠정 방향으로 채택하고 canonical URL과 route-level blocker는 유지한다. | live menu·role·privacy·사용자 연구를 창작하지 않으면서 최적 후보 하나에 검증을 집중하고, final IA 승인 전 consumer migration을 막기 위해서다. | [ADR-0004](../../docs/02-architecture/decisions/ADR-0004-provisional-hybrid-information-architecture.md) | 2026-08-21 | - |
| ADR-0005 | accepted | UI 품질 증거는 digest-derived versioned compact summary와 tracked index로 보존하고 기존 summary를 덮어쓰지 않는다. | ignored r12 원본 JSON을 source에 복제하거나 허위 CI provenance를 만들지 않으면서 clean checkout에서 bounded aggregate와 provenance를 장기 검증하기 위해서다. | [ADR-0005](../../docs/02-architecture/decisions/ADR-0005-ui-quality-durable-evidence.md) | 2026-08-21 | - |
| ADR-0006 | accepted | 반응형 표현은 단일 SSR DOM 위에서 CSS로만 전환한다. 같은 데이터를 두 벌 렌더해 `hidden`/`md:hidden`으로 한쪽만 보이거나, `matchMedia`·`useSyncExternalStore`·`next/dynamic ssr:false` 로 본문 데이터 표현을 분기하지 않는다. | nonce CSP 때문에 전 페이지가 force-dynamic 이라 SSR HTML 이 사용자가 보는 첫 화면이다. 훅 방식은 49개 화면에 새 하이드레이션 경계를 만들고, 이중 렌더는 accessor 를 2회 실행해 소비자가 만든 testid·aria-label 을 2벌로 복제한다(CI run 32555133776 strict mode violation). | [ADR-0006](../../docs/02-architecture/decisions/ADR-0006-css-only-responsive-table.md) | 2026-08-22 | - |
| ADR-0007 | accepted | 2026-08-23 G1 워크숍(참석: lkindo, 세 owner 역할)에서 하이브리드 IA를 참조-기본(reference-default) IA로 승인하고, 연구·live DB census·AT 증거 요건은 기관 채택 시점의 재검증 의무로 이전한다. route별 disposition은 일괄 승인되지 않으며 owner PR 리뷰로 개별 승인한다. PD-UX-002는 보류 유지. | 참조 구현에는 운영 DB도 실사용자도 구조적으로 존재하지 않아 원 G1 요건이 영구 미충족이었다. 사용자 연구 없는 승인임은 accepted-risk로 영구 기록하며, 파생 제품도 그 한계를 승계한다. | [ADR-0007](../../docs/02-architecture/decisions/ADR-0007-reference-default-ia-approval.md) | 2026-08-23 | - |
| ADR-0008 | accepted | `migration-tool`은 PostgreSQL·Oracle·Tibero·MySQL·MariaDB·SQL Server 소스를 inventory·plan·승인 artifact와 digest에 결속해 Flyway 소유 PostgreSQL 표준 스키마로 적재하는 독립 offline `bootJar`로 유지한다. | 소스별 객체 누락과 미검증 자동화를 fail-closed로 드러내면서 온라인 앱과 타깃 스키마 소유권을 침범하지 않는 재현 가능한 승인 경계를 만들기 위해서다. | [ADR-0008](../../docs/02-architecture/decisions/ADR-0008-multi-source-approved-migration-workflow.md) | 2026-08-31 | - |
| ADR-0009 | accepted | 업무상 개인정보가 포함될 수 있는 사용자 검색어는 화면별 route·query allowlist와 로그 비복제 조건 아래 URL에 둘 수 있다. 앱은 자격증명·고위험 개인정보·응답 원문용 URL state를 설계하거나 검색창에서 입력을 유도하지 않으며, 자유 입력에 예상 밖 값이 들어올 가능성은 잔여 위험이다. | GET·SSR·공유·새로고침 복원과 streaming download를 보존하면서 무제한 query 전파와 로그 노출을 차단하기 위해서다. | [ADR-0009](../../docs/02-architecture/decisions/ADR-0009-controlled-url-search-state.md) | 2026-09-05 | ADR-0003 §5의 검색어 절대 금지 부분 |
| ADR-0010 | accepted | 프론트엔드 `accessToken`·`session_exp` 쿠키의 `Secure`는 기본·배포 필수 속성으로 유지하고, 명시적으로 opt-in한 평문 HTTP local loopback 개발·검증에만 미설정을 허용한다. `accessToken`의 `HttpOnly`와 두 쿠키의 `SameSite=Strict`는 환경과 관계없이 유지한다. | 로컬 HTTP 흐름을 보존하면서 production 여부나 내부 URL만으로 공유 개발·preview·staging까지 예외가 넓어지는 것을 막고, 로그인·재발급을 같은 실행 계약에 결속하기 위해서다. | [ADR-0010](../../docs/02-architecture/decisions/ADR-0010-frontend-session-cookie-secure-policy.md) | 2026-09-05 | - |

## 운영 결정 index

| ID | 상태 | 결정 | 이유 | 정본 | 시행일 | supersedes |
|---|---|---|---|---|---|---|
| DEC-OPS-001 | accepted | 프로젝트 공통 규칙은 `AGENTS.md` 한 곳에 두고 저장소 `GEMINI.md`·`CLAUDE.md`는 자동 탐색용 얇은 어댑터로 유지한다. | 도구별 프로젝트 정책 복제와 글로벌 절번호 드리프트를 없애면서 각 런타임의 네이티브 진입점은 보존하기 위해서다. 사용자 홈 글로벌 파일의 동일성은 별도 프로비저닝 문제이며 도구 간 자동 상속으로 간주하지 않는다. | [AGENTS.md](../../AGENTS.md), [GEMINI.md](../../GEMINI.md), [CLAUDE.md](../../CLAUDE.md) | 2026-08-18 | - |
| DEC-OPS-002 | accepted | 공용 메모리 3종은 규범이 아닌, 근거 링크와 검증일을 가진 파생 인덱스로 운영한다. | Gemini·Claude·Codex 간 지속 지식을 공유하되 두 번째 SSOT와 세션 로그 유출을 만들지 않기 위해서다. | [AGENTS Documentation and memory](../../AGENTS.md#documentation-and-memory) | 2026-08-18 | - |
| DEC-OPS-003 | accepted | 프론트 도구체인은 Node 22+와 pnpm 9를 단일 축으로 사용하고 계약 생성은 offline-first로 한다. | 환경 스큐와 live API 의존형 codegen의 비결정성을 줄이기 위해서다. | [package.json](../../package.json), [frontend/package.json](../../frontend/package.json) | 2026-08-18 | - |
| DEC-OPS-004 | accepted | 로컬 훅은 범위별 빠른 피드백, required CI는 병합 권위로 구분한다. | 문서-only 작업의 비용을 낮추면서 우회 가능한 훅을 최종 통제로 오인하지 않기 위해서다. | [AGENTS Verification](../../AGENTS.md#verification-by-change-scope), [.github/required-checks.json](../../.github/required-checks.json) | 2026-08-18 | - |
| DEC-OPS-005 | accepted | `migration-tool`은 온라인 런타임에 포함하지 않는 독립·선택형 offline ETL이다. | 재사용 런타임 DAG와 일회성 레거시 이관 책임을 분리하기 위해서다. | [README.md](../../README.md), [migration-tool/build.gradle](../../migration-tool/build.gradle) | 2026-08-18 | - |
| DEC-OPS-006 | accepted | 하네스의 논리 규칙 ID는 유지하되 발견·실행·품질 하한은 중앙 `gates.json`과 소수 runner/task로 통합한다. | 개별 테스트를 거대한 단일 클래스로 합치지 않고도 누락·중복·threshold drift와 실행 경로 이탈을 exact census로 차단하기 위해서다. | [gate registry](../../config/governance/gates.json), [registry contract](../../scripts/governance-gates-contract.mjs) | 2026-08-19 | - |
| DEC-OPS-007 | superseded | main 목표 review policy는 approval 1, code-owner review, last-push approval, thread resolution을 요구하고 stale review 자동 dismiss는 끈다. | 독립 검토와 최신 push 책임을 보장하면서 매 push마다 모든 과거 승인을 지우는 비용은 피하기 위해서다. 실제 적용은 작성자 외 reviewer와 관리자 권한을 전제로 한다. | [.github/required-checks.json](../../.github/required-checks.json), [branch verifier](../../scripts/verify-branch-protection.mjs) | 2026-08-19 | - |
| DEC-OPS-008 | accepted | 조건부 heavy job은 내부 `*-scope`로 두고 브랜치 보호에는 항상 완료되는 안정 aggregate context만 노출한다. | docs-only처럼 작업이 명시적으로 제외된 SHA도 릴리스가 요구하는 `completed + success` 증거를 남기고, shard·matrix·scope 구조 변경이 원격 required 이름을 흔들지 않게 하기 위해서다. | [.github/required-checks.json](../../.github/required-checks.json), [CI workflow](../../.github/workflows/ci.yml), [aggregate evaluator](../../scripts/aggregate-required-check.mjs) | 2026-08-19 | - |
| DEC-OPS-009 | accepted | 단독 운영 기간의 main review policy는 approval 0·code-owner/last-push/thread-resolution 전부 비활성으로 확정하고, 명세·계약을 이 값에 양방향 동결한다. | 작성자 외 reviewer가 없는 상태에서 approval ≥ 1을 원격에 적용하면 자기 PR을 자기가 승인할 수 없어 모든 병합이 막히고, 명세만 강하게 두면 `verify:ops`가 영구 red로 남아 신호 가치가 죽는다. reviewer 확보 시 후속 DEC로 상향한다. | [.github/required-checks.json](../../.github/required-checks.json), [required-checks contract](../../scripts/required-checks-contract.mjs) | 2026-08-20 | DEC-OPS-007 |
| DEC-OPS-010 | accepted | 설문 열람(목록·상세·문항)과 제출은 인증 사용자(`@Authenticated`)에게 개방하고, 설문·템플릿·문항·항목의 관리 뮤테이션은 `@AdminOrSystem`으로 유지한다. 별칭 경로(`/api/v1/surveys/**`)의 URL 게이트(secure-paths + DB seed)는 V2_84로 제거하고 메서드 인가를 단독 방어선으로 한다. | 종전에는 컨트롤러에 메서드 인가가 없어 URL 게이트 1겹에만 의존했고, 제출 엔드포인트의 `@Authenticated`가 핸들러 도달 전에 403으로 죽어 애노테이션 의미와 실행 의미가 어긋났다(GAP-AUTH-001). 설문은 일반 사용자가 응답하는 제품이므로 열람·제출 개방이 제품 의도다. | [SurveyApiController](../../api-server/src/main/java/nuri/api/controller/foundation/controller/system/service/survey/SurveyApiController.java), [V2_84](../../api-server/src/main/resources/db/migration/V2_84__open_survey_alias_to_authenticated.sql), [authorization policies](../../config/governance/authorization-policies.json) | 2026-08-20 | - |
| DEC-OPS-011 | accepted | production CSP의 `script-src`에서 `unsafe-inline`을 요청별 nonce로 대체하고, 그 전제로 PPR(cacheComponents)을 포기해 전 페이지를 동적 렌더로 전환한다. `strict-dynamic`은 채택하지 않는다. | nonce는 요청마다 달라야 하므로 정적 프리렌더 HTML과 양립할 수 없고(정적 셸의 inline script가 전면 차단됨 — CI e2e 실측), strict-dynamic은 Next가 스트리밍 중 삽입하는 lazy chunk에 nonce가 없어 앱을 전면 파손시킨다(CI run 32310837353 실측). TTFB 비용은 실측 +2ms 수준으로 비물질적이며, PD-CSP-001 결정을 집행한 것이다. | [proxy.ts](../../frontend/src/proxy.ts), [next.config.ts](../../frontend/next.config.ts), [csp-policy contract](../../frontend/src/__tests__/csp-policy.test.ts), [FE 헌법 제10조](../knowledge/frontend-ux-constitution/artifacts/constitution.md) | 2026-08-20 | - |
| DEC-OPS-012 | accepted | UI quality baseline 의 새 authoritative run(r13)과 수동 접근성 48건 수집을 보류하고, r12 를 현재 자동 증거로 유지한 채 UI 개선 작업을 재개한다. r12 를 `measured` 로 승격하지 않는다. | 측정 인프라가 4,000줄을 넘긴 반면 사용자에게 보이는 개선은 소규모에 머물렀고, r4~r12 재실행의 상당수가 runner 자체 결함 수정이었다. r13 이 필요한 유일한 사유는 실행 시점 protocol hash 부재라는 메타데이터 한 축이며, 수동 48건 중 NVDA 8건은 외부 평가자·환경이 없으면 닫히지 않아 완벽 기준이 진척을 무기한 인질로 잡는다. 보류는 증거를 만들지 않으므로 UA-06 이후 게이트는 계속 잠긴 채 두고, r12 의 `unmeasured` 판정도 그대로 유지한다. | [사용자 런북 §8](../../docs/04-operations/ui-ux-modernization-user-action-runbook.md), [ADR-0005](../../docs/02-architecture/decisions/ADR-0005-ui-quality-durable-evidence.md), [durability 계약](../../scripts/ui-quality-evidence-durability-contract.test.mjs) | 2026-08-22 | - |
| DEC-OPS-013 | accepted | 단독 운영 기간 IA 프로그램의 세 owner 역할(Product·IA·Security/Privacy)을 저장소 소유자 lkindo 로 지명하고 승인 채널을 저장소 PR 리뷰로 기록한다. G1 decision workshop 날짜는 보류한다 — 따라서 T0 는 미성립이고 T0 기준 SLA 도 시작되지 않는다. | owner 미지명 상태로는 IA §13 의 blocked-input 행들이 영구히 열리지 않는데, 단독 운영에서 실존하는 결정권자는 저장소 소유자뿐이다. 반면 G1 날짜는 live DB census(blocked-external)와 연구 참여자 확보가 선행돼야 하므로, 날짜만 잡아 SLA 위반 상태를 만드는 대신 선행조건 해소 시점에 별도 결정으로 잡는다. 이 지명은 역할 기록이지 기관/critical role 입력·로그 URL 분류 승인·G0/G1 통과가 아니다. | [IA §13](../../docs/01-product/information-architecture.md#13-open-input와-reviewby) | 2026-08-23 | - |
| DEC-OPS-014 | accepted | 밀도·구조 토큰 어휘 12종(`--control-h`·`--control-h-sm`·`--cell-px`·`--cell-py`·`--font-size-body`·`--form-gap`·`--page-max-w`·`--page-pad`·`--page-pad-md`·`--page-pad-lg`·`--radius-control`·`--filter-pad`)을 두 브랜드 프로필의 라이트·다크 4개 블록에 신설한다(Phase 1, ERP 전환 마스터플랜 D2 — 마스터플랜 정본 문서는 아직 저장소 미반영이라 링크하지 않는다). | 사용자 위임(2026-08-23)에 따른 D2 집행이다. 값은 전부 현행 등가(h-9·px-6·max-w-7xl 등)로 선언해 시각 무변경이며, 소비자 이행은 후속 PR 범위다. 밀도는 컬러 모드 불변이므로 다크 블록도 같은 값을 재선언해 프로필 키 패리티를 유지한다. | [premium.css](../../frontend/src/styles/themes/premium.css), [krds-aligned.css](../../frontend/src/styles/themes/krds-aligned.css), [theme-token-contract](../../frontend/src/__tests__/theme-token-contract.test.ts) | 2026-08-23 | - |
| DEC-OPS-015 | accepted | 직교 data-density 축(comfortable·compact)을 신설한다 — 서버 env `UI_DENSITY`를 allowlist 검증해 `<html data-density>` 전역 1곳에 배선하고, ERP 밀도는 globals.css 의 compact 무레이어 오버라이드 블록 한 곳으로 전달한다. 기본 배포(미설정=comfortable)는 렌더링 무변화이며, 라우트별 밀도 배정은 ADR-0004 금지를 그대로 따른다. | 사용자 위임(2026-08-23) D1 집행 — KRDS 대민(넉넉한 타깃)과 ERP 업무(고밀도)를 한 저장소에서 동시 지원하려면 밀도는 브랜드와 직교여야 한다. 프로필별 복제 대신 무레이어 블록 1곳이 두 프로필 × 라이트·다크 전부를 이겨 드리프트가 없다. | [density.ts](../../frontend/src/lib/theme/density.ts), [globals.css](../../frontend/src/app/globals.css), [theme-token-contract](../../frontend/src/__tests__/theme-token-contract.test.ts) | 2026-08-23 | - |
| DEC-OPS-016 | accepted | 백엔드 헌법 제6조에 binary/stream 예외 3항을 제한적으로 신설한다(①Content-Disposition attachment ②명시적 produces/응답시점 Content-Type ③ResponseContractLinter binary census 등재의 동시 충족, 목록 밖 신설·stale 행 양방향 red). 1호 적용: FileApiController#downloadFile 동결분 정합 + LoginLogApiController#exportLoginLogs 전체 결과 xlsx export 신설. | 사용자 위임(2026-08-23 — "의사결정 필요 시 권고안으로 무조건 진행" 지시)에 따른 D4 집행이다. GAP-API-001 이 기록해 온 예외 부재(정당한 binary 가 조항 없이 동결로만 통과)를 익명 개수 동결 대신 파일명·핸들러 단위 exact census 로 강화하며 닫았고, red 실측을 동반했다. JSON 표현 가능한 응답의 예외 우회는 조문으로 금지된다. | [백엔드 헌법 제6조](../knowledge/backend-api-constitution/artifacts/constitution.md), [ResponseContractLinterTest](../../api-server/src/test/java/nuri/api/harness/ResponseContractLinterTest.java) | 2026-08-23 | - |
| DEC-OPS-017 | accepted | 파일럿 시각 회귀 기준선을 대시보드 1장에서 4장으로 확장한다 — 기존 단일 `Visual Regression Baseline` 테스트 내부에 로그인 로그(무매칭 검색 empty-state)·공통코드 허브(Flyway seed 파생)·로그인 화면(비인증 컨텍스트 + 01-core-base 와 동일한 /auth/me 401 ledger) 캡처를 추가하고, 기준선 생성·커밋은 기존 update-visual-baseline 워크플로의 `-g "Visual Regression Baseline"` 경로를 그대로 사용한다(신규 인프라·워크플로 변경·스펙 파일 신설 없음). 결재함(/approvals)은 목록 내용이 샤드 실행 순서에 의존해 비결정적이므로 고정 fixture 설계 전까지 제외한다. | 사용자 위임(2026-08-23)에 따른 D6 집행이다. VRT 는 기준선 생성 시점과 비교 시점의 DB 상태가 다르면 마스크로 못 막는 레이아웃 시프트가 생기므로 결정적 상태만 캡처했고, e2e-harness-hygiene 계약이 e2e 전체에 test.skip 1건만 허용해 테스트 분리 대신 단일 테스트 내부 확장을 택했다. 스냅샷 부재 시 CI 첫 실행은 생성이 아니라 실패다("A snapshot doesn't exist … writing actual" — CI=1 로컬 실측, 과거 run 30279822185 동일) — 리눅스 기준선은 update-visual-baseline workflow_dispatch(commit=true)로 생성·커밋해야 하며 win32 로컬 스냅샷 커밋은 폰트 렌더 차이로 금지다. | [04-quality-resilience.spec.ts](../../frontend/e2e/04-quality-resilience.spec.ts), [update-visual-baseline.yml](../../.github/workflows/update-visual-baseline.yml), [e2e-harness-hygiene](../../frontend/src/__tests__/e2e-harness-hygiene.test.ts) | 2026-08-23 | - |
| DEC-OPS-018 | accepted | survey/poll 도메인을 재사용 pack으로 승격한다(D7 집행) — ① 패키지를 `nuri.business.(domain\|service).system.service.survey`에서 1급 `nuri.business.(domain\|service).survey`로 이동하고 ② reusable-base manifest에 `survey` pack(rank 2, appDomains·전용 테이블 9·frontend removePaths 9)을 신설해 demo pack(rank 3으로 이동)에서 분리하며 demo 프로필 구성에 survey pack을 포함한다. API URL·컨트롤러 FQN·인가 애노테이션·DB 스키마·기존 3개 프로필의 projection 결과는 전부 무변경이고, `ServiceLayerIsolationTest`의 코어 금지 패키지 목록은 새 경로로 갱신했다. 별도 Gradle 모듈 분리와 survey 전용 프로필 신설은 이번 범위가 아니다. | 사용자 위임(2026-08-23)에 따른 ERP 마스터플랜 D7(business-app system 분할 — survey/poll pack 승격) 집행이다. survey는 실측상 자기 도메인·foundation·`SecurityUtil`만 참조하는 자기완결 도메인인데 `system` 우산 아래 3단 중첩 경로에 있어 pack 단위로 제외·채택할 수 없었다. 이동 직후 reusable-base census가 pack 소유자 부재 red를 실측으로 냈고 manifest pack 신설로 green이 됐다. CrossDomainCouplingLinter census는 app→app 5·app→core 8 그대로(survey 기여 0), DomainIsolationTest survey 슬라이스 형제 의존 0으로 결합 상수 변경이 없다. | [survey pack manifest](../../config/reusable-base-profiles.json), [reusable-base census](../../scripts/reusable-base-census.mjs), [survey 도메인](../../business-app/src/main/java/nuri/business/domain/survey/), [ServiceLayerIsolationTest](../../business-core/src/test/java/nuri/business/ServiceLayerIsolationTest.java) | 2026-08-24 | - |
| DEC-OPS-019 | accepted | 파생 제품(adopter)의 기준선 재동결 절차를 운영 문서로 도입한다(마스터플랜 Track B D10). 대상은 현존 게이트 6축 — status/hardcoded color guard 의 `BASELINE` 재측정(하향 전 red 실측 원칙), `config/ui-url-state-census.json` 재생성(`node scripts/ui-url-state-census.mjs --write`, 수기 편집 금지), `config/governance/gates.json` exact census, theme-token-contract 프로필 키 패리티, e2e shard-duration-profile 재생성, required-checks 결속 재확인 — 이며, 재동결 diff 리뷰를 의무화한다. | 사용자 위임(2026-08-23 — "의사결정 필요 시 권고안으로 무조건 진행" 지시)에 따른 D10 집행이다. base 의 양방향 동결 게이트는 adopter 커스터마이징 시 필연적으로 어긋나는데, 절차 없이는 게이트 비활성화·baseline 임의 인상으로 신호를 지우는 경로(H2 위반)가 최단 경로가 된다. 절차는 존재하는 게이트만 파일 경로로 실증하며 새 게이트를 창작하지 않는다. | [adopter-baseline-refreeze.md](../../docs/04-operations/adopter-baseline-refreeze.md), [ERP 전환 마스터플랜](../../docs/02-architecture/erp-transformation-master-plan.html) | 2026-08-24 | - |
| DEC-OPS-020 | accepted | disposition overlay의 acceptanceEvidence 계약을 reference-default(ADR-0007) 기준으로 개정한다 — 연구·live census 계열 4축(research·liveMenu·authorityAssignment·effectiveMenu)은 ADR-0007 본문 hash에 결속된 deferral 레코드(`deferred-to-institution-adoption`·`acceptedRisk: approved-without-user-research`)로 기관 채택 시점 재검증 의무 이전을 기록하고, `state=proposed`의 menu/generator 무조건 차단은 ADR-0007 §Decision 4 게이트(4축 전부 기록 + 개별 approved record ≥ 1 + entrypoint 등록, 미충족·미등록 소비는 red)로 대체한다. overlay state(proposed)·현행 binding(disabled)·route별 owner PR 리뷰 개별 승인 원칙은 무변경이다. | 사용자 위임(2026-08-23)에 따른 D5 2단계 집행이다. 종전 계약은 참조 구현에 구조적으로 존재할 수 없는 live census·연구 artifact를 null blocked-input으로만 표현해 acceptance가 영구 차단이었고, 소비 차단도 ADR-0007이 연 "개별 승인 record부터 소비 가능"과 어긋났다. deferral은 측정 주장도 면제도 아니며 accepted-risk를 명기 승계하고, null 회귀는 다시 blocker가 된다(red 실측 동반: adrSha256 드리프트·status 위조·무approved 소비·미등록 entrypoint 전부 red). | [disposition contract](../../scripts/ui-navigation-disposition-contract.mjs), [overlay](../../config/ui-navigation-disposition-proposal.json), [schema](../../config/ui-navigation-disposition.schema.json), [ADR-0007](../../docs/02-architecture/decisions/ADR-0007-reference-default-ia-approval.md), [IA §8.4](../../docs/01-product/information-architecture.md#84-승인-전에-먼저-만드는-실행-증거) | 2026-08-24 | - |
| DEC-OPS-021 | accepted | 저장소 첫 v* 릴리스 태그 `v0.1.0`을 main 커밋 fa2a79386 에 부여하고, core·collaboration·demo 3 프로필의 공식 reusable-base 산출물을 그 태그에서 생성한다(생성 로그는 릴리스 자산). 원격 태그 push·GitHub 릴리스 발행은 외부 발행이라 사용자 승인 경계에 남긴다. | 사용자 위임(2026-08-23)에 따른 Track B D9 집행이다. `generate-reusable-base-source.mjs`는 "공식 source artifact는 v* 릴리스 태그에서만 생성한다"로 fail-closed 게이트돼 있어 태그 부재 상태에서는 3 프로필 공식 생성이 원리적으로 불가능했고(종전 산출물은 전부 `--allow-non-release-ref` 로컬 빌드), 태그가 distributionMode `release-tag-generated`의 유일한 배포 참조다. 생성은 dev 서버 드리프트와 격리된 clean worktree(태그 체크아웃)에서 `--allow-*` 플래그 없이 수행했고, 세 산출물의 `reusable-base-lock.json`이 `sourceReleaseTag: v0.1.0`·`localDevelopmentBuild: false`로 공식성을 증명한다. | [소스 생성기](../../scripts/generate-reusable-base-source.mjs), [DB 번들 생성기](../../scripts/generate-reusable-base-db.mjs), [프로필 manifest](../../config/reusable-base-profiles.json), [ERP 전환 마스터플랜](../../docs/02-architecture/erp-transformation-master-plan.html) | 2026-08-24 | - |

| DEC-OPS-022 | accepted | 포털형→업무형 UI 전환의 판단 기준을 **업무 화면 문법 카탈로그**(공통 규칙 G1~G15 · archetype 8종 · 밀도 계약)로 명문화하고, A1 조회형 목록을 `WorkListPage` archetype 셸로 1급화한다. 참조 갤러리 `/admin/patterns`를 demo pack 소유로 신설하고(`demo-isolated` 제안, `reviewState=proposed` — 개별 승인은 owner PR 리뷰), 라우트 census 3종(capabilities 119→120 · disposition 119→120 · url-state 재생성)과 shell 집계를 같은 변경에서 갱신했다. 이행(W3)은 wave 1 협업 2화면 + wave 2 로그 4·운영 5화면으로 진행했고, 되돌리기와 신규 직접 소비 유입을 **채택 census 게이트**(셸 경유 12 · 직접 조립 39 exact)로 동결한다. 조회 조건·빈 상태 문구도 공용 조립기(`KeywordFilter`·`emptyResultMessage`)로 단일화했다 — 조회 조건이 표 밖으로 나오면 표가 검색어를 몰라 `결과 없음`과 `데이터 없음`이 합쳐지므로(G15) 화면이 문구를 내려주는 계약이다. h1 소유 계약은 "화면 소스에 `<h1`" 에서 "셸이 단독 소유 + 화면은 위임" 으로 개정했고, e2e page object 3건(포상 placeholder·`ANALYZE`→`조회`·외부인사 placeholder)은 단위 계약(`rewards-e2e-contract`)으로 문구를 고정한 뒤 함께 갱신했다. `/admin/system/logs/login` 은 VRT 기준선(DEC-OPS-017) 소속이라 기준선 재생성과 같은 변경에서 이행하도록 보류했다. W4(랜딩 → 업무 홈)는 순서를 처리 대기 → 내 목록 → 상태·활동 → 홍보로 뒤집어 완료했고, VRT 기준선이 `/` 가 아니라 `/admin` 을 캡처한다는 실측으로 기준선 재생성 없이 진행 가능했다. wave 4까지 셸 경유 21 · 직접 조립 30이며, 이행 과정에서 계측 원천 없는 고정 지표(`일일 상호작용 1.2k+` 등)와 `console.log` 만 하는 검색 컨트롤 같은 위장·죽은 어포던스를 함께 제거했다. W5 A2 1차로 `MasterDetailPage`를 신설하고 `/admin/user/departments`·`/admin/system/menus`를 단일 DOM master-detail, 선택 `aria-current`, 방향키·상세 Tab·선택 기반 저장 계약으로 이행했으며 route→consumer exact census를 별도로 동결했다. A2 2차는 `/admin/collaboration/mail-history`의 6열 직접 표를 제목·상태 중심 compact master와 우측 상세로 이행해 A2 소비자를 3화면, 직접 표 소비를 29화면으로 확정했다. 저장 동작이 없는 메일 이력에는 `Ctrl+S`를 만들지 않고 검색·페이지 변경 시 stale 선택을 해제하며, 메일 본문은 이 화면에 새로 노출하지 않는다. A2 3차는 `/admin/system/common-code` STANDARD만 전체 `MasterDetailPage`로 이행해 소비자를 4화면으로 늘리고, 포털 hero·metrics·진입 motion을 제거해 1280×720에도 실제 업무 영역을 노출했다. DnD handle과 선택을 분리하고 검색 중 이동을 비활성화했으며, 물리 스키마에 정렬 컬럼이 없는 계약에 맞춰 분류 자체와 같은 분류 안의 순서는 변경하지 않고 그룹의 교차 분류 이동만 저장한다. 선택+소속 변경 저장 및 검색 stale 선택 해제를 결속했다. ADMINIST·INSTITUTION의 A1과 `/admin/system/common-code/codes` alias는 보존했으며 `StandardDataTable` 50·직접 조립 29·`WorkListPage` 21·`PagePagination` 4도 불변이다. status color BASELINE은 공통코드 필수·오류·사용 상태 리터럴의 semantic token 이행으로 629→621이 됐다. 선택의 새로고침 복원은 메뉴·부서·메일에서 기존 보류를 유지하며, 공통코드의 기존 `groupId` query consumer 1건도 `deny`·`unverified` legacy로 보존하고 새 producer·local/session storage를 만들지 않는다. URL 이행과 privacy 승인은 별도 과제로 보류한다. W5의 남은 범위는 A4·A5다. | 사용자 위임(2026-08-24 "권고 실행 순서대로 작업 진행")에 따른 W1·W2 및 후속 A2 집행이다. 실측상 정렬·페이지당 건수·일괄 액션은 이미 `StandardDataTable`(headless TanStack)에 있는데 58개 소비 화면 중 채택이 각각 7·6·3건뿐이었다 — 병목은 컴포넌트가 아니라 **화면이 문법을 선언할 방법의 부재**였다. 디자이너 부재 상태에서 취향 판단을 없애기 위해 규칙 근거를 공개 가이드 교차 대조(KRDS 표 원문·MS model-driven 원문·Fiori/KRDS 검색 공개 스니펫)와 저장소 실측으로 제한하고, 본문을 확보하지 못한 출처는 근거에서 배제했다. 카탈로그의 밀도 표는 `globals.css` compact 블록과 양방향 exact 계약으로 결속했고(값 드리프트·토큰 누락 red 실측 완료), archetype 골격 순서도 테스트로 고정했다(G1 위반 red 실측). A2는 shared client import만 세는 false-green을 피하려고 부서 route의 `defaultTab="DEPTS"`와 실제 활성 guard를 결속하고, 메일 route는 importer·선택 시맨틱·`emlDsptchSn` URL/브라우저 저장소 금지를 exact 검사한다. 공통코드는 STANDARD 분기와 두 A1 분기, `groupId` legacy 1건, 신규 producer·저장소 부재를 exact 검사한다. KRDS·KWCAG 준수 주장은 하지 않는다. | [카탈로그](../../docs/02-architecture/work-screen-grammar-catalog.md), [WorkListPage](../../frontend/src/app/components/patterns/work-list-page.tsx), [MasterDetailPage](../../frontend/src/app/components/patterns/master-detail-page.tsx), [메일 이력 소비자](../../frontend/src/app/admin/collaboration/mail-history/MailHistoryHubClient.tsx), [공통코드 소비자](../../frontend/src/app/admin/system/common-code/CommonCodeClient.tsx), [A2 census](../../frontend/src/__tests__/master-detail-adoption-census.test.ts), [밀도 계약](../../frontend/src/__tests__/work-screen-grammar-contract.test.ts), [갤러리](../../frontend/src/app/admin/patterns/PatternGalleryClient.tsx), [ERP 전환 마스터플랜](../../docs/02-architecture/erp-transformation-master-plan.html) | 2026-08-24 | - |
| DEC-OPS-023 | accepted | W3(A1 이행 웨이브) 종료와 함께 두 가지 규칙을 확정한다. ① **도달 불가 화면은 archetype 이행 대상이 아니다** — [next.config](../../frontend/next.config.ts) 가 리다이렉트해 클라이언트가 렌더되지 않는 `/admin/security/login-policy`·`/cop/sms/selectSmsList` 는 이행하지 않고, 채택 census 의 "미이행" 잔량으로 남긴다. ② **관리자 표시 판정은 라우트 게이트와 같은 역할 집합을 쓴다** — [lib/auth/administrative-role](../../frontend/src/lib/auth/administrative-role.ts) 을 SSOT 로 두고 `proxy.ts` 의 `/admin` 게이트 집합과 parity 를 계약으로 고정한다. W3 결과는 셸 경유 40 · 직접 조립 4이며 남은 4건은 A5 스펙 계약 1 · 도달 불가 alias 2 · 공유 컴포넌트 1로 전부 대상이 아니다. | ① 도달 불가 화면을 이행하면 사용자에게 보이는 변화가 0인데 채택 수치만 올라 census 가 진척을 과장한다(H2 의 신호 은폐와 같은 방향의 왜곡이다). 실측으로 login-policy 를 이행했다가 되돌린 뒤 이 규칙을 세웠다. ② `/auth/me` 의 `role` 은 사용자 등급이 아니라 authority id 원문(`ROLE_ADMIN`)인데 화면이 `role === 'ADMIN'` 하나만 보면 **권한 있는 관리자에게 기능이 사라진다**. 게시판 마스터의 생성 마법사 진입이 정확히 그렇게 죽어 있었고, 같은 동작을 중복 제공하던 하단 마케팅 배너가 그 결함을 가리다가 배너 제거와 함께 e2e(CI run 32832181327)로 드러났다. 라우트는 열어 주는데 화면만 숨기는 비대칭은 조용히 죽는 결함이라 계약으로 막는다. 표시 판정일 뿐 인가가 아니므로 서버 권한은 넓어지지 않는다(H3). | [카탈로그 §6·§7](../../docs/02-architecture/work-screen-grammar-catalog.md), [A1 census](../../frontend/src/__tests__/work-list-adoption-census.test.ts), [관리자 판정 SSOT](../../frontend/src/lib/auth/administrative-role.ts), [parity 계약](../../frontend/src/__tests__/administrative-role-parity.test.ts) | 2026-08-25 | - |

| DEC-OPS-024 | accepted | `/admin/security/login-policy` 의 config redirect 를 제거해 로그인 정책 화면을 정본 경로로 복원한다. DEC-OPS-023 ① 의 규칙(도달 불가 화면은 archetype 이행 대상이 아니다)은 그대로 유효하되, **이 route 는 더 이상 도달 불가가 아니므로 그 예외에서 빠진다** — A1 이행 대상으로 복귀하며 이번 변경에서 이행하지는 않았다. 같은 변경에서 route capabilities routing(config-redirect→page)·url-state census(configRedirects 15→14)·disposition(consolidate-to-canonical→retain-canonical, manifest sha 갱신)·카탈로그 §6·§7 서술을 정합했다. 같은 파일 140행 `/admin/user/login-policy` 는 손대지 않았다. | 이 리다이렉트는 **동작하는 화면을 통째로 삼키고 있었다**. 메뉴 9020120 의 modern_route 가 이 경로를 정본으로 선언하는데(V2_47), 목적지 허브에 POLICY 탭이 없어 별칭이 LOGIN(로그 목록)으로 떨어뜨렸다 — DB 마이그레이션이 기록한 교정이 프런트 설정에 조용히 무효화된 상태였고, IP 제한·허용 시간대·2단계 인증(OTP)을 설정하는 424줄 화면과 API 5개가 전 경로에서 도달 불가였다. DEC-OPS-023 의 서술(도달 불가 alias 2건)은 이 변경으로 사실이 아니게 되지만 accepted 결정은 소급 편집하지 않으므로(AGENTS.md) 이 후속 결정으로 사실 변화를 기록한다. 140행을 함께 손대면 e2e(06-ops-governance) page object 갱신이 필요해 별건으로 분리했다. | [next.config](../../frontend/next.config.ts), [capabilities census](../../config/ui-route-capabilities.json), [disposition overlay](../../config/ui-navigation-disposition-proposal.json), [카탈로그](../../docs/02-architecture/work-screen-grammar-catalog.md) | 2026-08-27 | DEC-OPS-023 ① 의 login-policy 예외만 무효화(규칙 자체는 유지) |
| DEC-OPS-025 | accepted | ADR-0007의 2026-08-23 G1 워크숍과 참조-기본 IA 승인으로 DEC-OPS-013의 **G1 날짜 보류·T0 미성립·SLA 미개시 부분만** 종료한다. Product·IA·Security/Privacy owner를 lkindo로 둔 지명과 저장소 PR 리뷰 승인 채널은 계속 유효하다. | accepted 결정을 소급 편집하지 않으면서, ADR-0007이 바꾼 일정·게이트 상태와 그대로 유지한 의사결정 권한을 구분하기 위해서다. | [ADR-0007](../../docs/02-architecture/decisions/ADR-0007-reference-default-ia-approval.md), [IA decision log](../../docs/01-product/information-architecture.md) | 2026-08-31 | DEC-OPS-013의 G1 날짜·T0 보류 부분만 종료(owner·PR 채널은 유지) |
| DEC-OPS-026 | accepted | DEC-OPS-022에 포함된 A2 소비자 수·status-color baseline·`W5 진행 중` 같은 **이행 시점 스냅샷을 현재 상태 판단에서 종료**한다. W5는 완료됐고, A4는 실소비자가 없어 공용 셸을 만들지 않으며, A5는 단일 권한 매트릭스를 공용 셸 없이 스펙 계약으로 고정한다. 이후 소비자 수와 baseline 같은 변동 수치는 prose 결정문이 아니라 현재 live census·guard가 정본이다. | accepted 결정의 역사와 원 설계 원칙은 보존하면서, 빠르게 변하는 진행 수치를 장기 결정 인덱스가 다시 낡게 만들지 않기 위해서다. | [업무 화면 문법 카탈로그](../../docs/02-architecture/work-screen-grammar-catalog.md), [A1 census](../../frontend/src/__tests__/work-list-adoption-census.test.ts), [A2 census](../../frontend/src/__tests__/master-detail-adoption-census.test.ts), [A5 계약](../../frontend/src/app/admin/security/authority/__tests__/matrix-a5-contract.test.tsx), [status-color guard](../../frontend/src/__tests__/status-color-guard.test.ts) | 2026-08-31 | DEC-OPS-022의 진행 스냅샷만 종료(카탈로그·archetype 결정은 유지) |

| DEC-OPS-027 | accepted | 하네스 실행 경로 강화 5축을 도입한다. ① 백엔드 소스를 감사하는 크로스 스택 프런트 계약 7종(Atlas 계약 포함)을 `frontend/src/__tests__/cross-stack/` 로 분리하고, required `secret-scan` job 에서 `atlas ∪ backend` 축으로 실행한다(registry 신설 카탈로그 `GATESET-CROSS-STACK-CONTRACTS` + pre-push/verify 결속 + 배치 양방향 가드 — 루트 파일의 백엔드 참조도, cross-stack 무임승차도 red). ② `verify:full` 백엔드 경로에 `:foundation:check` 를 편입해 foundation 커버리지 ratchet 2종의 local-full tier 선언을 실행 실체와 정합했다. ③ census 재검토 기한(reviewBy)을 실시간 시계로 결속한다 — route-capabilities 는 실시간 검증 테스트(만료 red·60일 전 경고), url-state 는 만료 검증 자체를 신설(주입 가능한 시계, 기본 실시간). 만료 red 의 해소는 실제 재검토 또는 사유를 남긴 명시적 기한 연장 커밋이며, 2026-10-31 의 일괄 만료(495건)는 의도된 강제 재검토 지점으로 남긴다. disposition overlay 의 reviewBy 는 결정 결속(DEC-OPS-020)이라 의도적으로 제외했다. ④ 메타게이트 census 를 ArchUnit 계층(`*ArchitectureTest`·`*IsolationTest`·규칙 정의 `*Rules`)과 게이트 태그 술어(`@Tag`/`@ArchTag` — registry census 와 단일화)로 확장하고, 규칙이 상수 밖 메서드 체인에 사는 ArchUnit 계열 15파일은 소스 전체 해시(`__sourceHash`)로 동결했다(census 44→98). registry 계약에는 선언 root 밖 tagged gate 를 오류로 잡는 검사를 신설했다. ⑤ 부수 강화 — `GATESET-FRONTEND-VITEST` forbidSkips(당시 위반 0), e2e shard profile 신선도 상한 120일, 하네스 직접 I/O 금지 확대(`readAllLines`·`readAllBytes`·`list`·`lines`, 현행 7건 사유 동결), `resolveRepoRoot` 7개 사본의 공용 인덱스 위임(무음 폴백 1건 제거), `ResponseContractLinter` 핸들러 census 의 reflection 교차 검증(정규식 손실 = red). 전 축 red 실측 동반, 라우트 수 120 삼중 하드코딩은 파일시스템 파생 단일 원본으로 교체. | 2026-08-31 하네스 전수 진단이 "계약은 라인의 존재만 검사하고 실행 경로는 검사하지 못한다" 패턴의 P1 4건을 실측했다 — 백엔드-only PR 에서 크로스 스택 계약 실행 0(스코프 skip 을 집계가 성공으로 매핑; `ocrnYmd.desc()→asc()` 변경을 그 계약이 못 보는 경로), `verify:full` 의 foundation ratchet 미실행, reviewBy 만료 시계의 영구 정지(CI 는 고정 NOW 픽스처만 실행), ArchUnit allowlist 확장이 어떤 동결에도 안 걸리는 사각. 사용자 지시("순서대로 개선해줘")와 결정 위임(2026-08-23)에 따라 권고안대로 집행했다. | [gate registry](../../config/governance/gates.json), [registry contract](../../scripts/governance-gates-contract.mjs), [배치 가드](../../frontend/src/__tests__/cross-stack-boundary.test.ts), [메타게이트](../../api-server/src/test/java/nuri/api/harness/HarnessBaselineIntegrityTest.java), [CI workflow](../../.github/workflows/ci.yml), [required-checks contract](../../scripts/required-checks-contract.test.mjs) | 2026-09-01 | - |

| DEC-OPS-028 | accepted | 생성 Zod 계약에서 **응답(response) 방향의 non-required 필드는 `null` 을 허용**한다(`.optional().nullable()`). 요청(request) 방향과 required 필드는 그대로 엄격하게 둔다. 문서에 `nullable: true` 가 명시된 필드는 종전대로 `.nullable()` 이다. | 세 실측이 겹쳐 **정상 응답이 런타임에 throw** 되고 있었다. ① springdoc 은 Java DTO 에서 nullability 를 추론하지 않아 `@Schema(nullable = true)` 를 손으로 달지 않으면 문서에 표시가 남지 않는다 — 즉 문서의 nullable 부재는 "null 이 아니다" 라는 보장이 아니라 "말하지 않았다" 이다. ② 이 저장소에는 전역 Jackson null 생략 설정이 없어(`ApiResponse` 의 특정 필드에만 `@JsonInclude(NON_NULL)`) NULL 허용 컬럼의 값이 응답 JSON 에 `null` 로 그대로 실린다. ③ zod `.optional()` 은 undefined 만 허용하고 null 을 거부한다. 2026-09-01 CI e2e 에서 권한 관리·주소록·알림·부서업무 화면이 SSR 단계에서 동시에 죽었다(3개 샤드 전부 red, 서버 로그 `생성 API 응답이 OpenAPI 계약과 일치하지 않습니다`). 따라서 이 변경은 계약 완화가 아니라 **거짓 red 의 제거**다. 대안으로 검토한 전역 `NON_NULL` 은 응답 형태 자체를 바꿔(`{"a":null}` → `{}`) 기존 소비자와 d018b7bc0 이 추가한 런타임 nullability 계약에 영향이 커 채택하지 않았고, DTO 별 `@Schema(nullable = true)` 전수 표기는 대상이 광범위해 누락이 계속 재발한다. 서버가 실수로 null 을 보내는 진짜 결함은 백엔드 쪽 `OpenApiDocumentationTest` 의 nullability 검증이 담당한다. | [codegen-zod](../../.agent/scripts/codegen-zod.js), [generated-zod](../../frontend/src/types/generated-zod.ts), [OpenApiDocumentationTest](../../api-server/src/test/java/nuri/openapi/OpenApiDocumentationTest.java) | 2026-09-01 | - |

| DEC-OPS-029 | accepted | PD-UX-002 의 URL-state 판정을 네 축으로 확정한다. **Q1(사용자 직접 결정)** — 현재 URL 에 실리는 사용자 입력 검색어 14건을 전부 유지하고, 로그 화면이 검색어를 주소창에 싣지 않는 현행도 유지한다. 즉 판정 경계는 "검색어인가" 가 아니라 **"주소창에 실리는가"** 이며, 화면별 비대칭이 승인된 상태로 확정된다. **Q2** — 목록 화면의 copy-all 캐리어(`new URLSearchParams(searchParams)`)를 `LIST_PARAM_KEYS` 7키 allowlist 재조립으로 바꾼다. **Q3** — DB 가 소유한 URL 공간(`tb_menu_info.modern_route`)을 **저장 시점에** 형식 제한한다(절대경로 또는 레거시 `.do`, 쿼리 키는 `tab`·`bbsId` 만, 빈 문자열 허용). **Q4** — 죽은 URL 표면 14건 중 9건을 종결하되, 걷어내는 대신 **소비처를 만드는 쪽**을 택한다(`expired`·`auth_error` 는 안내를 신설, `nttId` 는 canonical 정합 + 별칭 수용). Q4 잔여 5건과 Q5(2026-10-31 일괄 만료)는 이 결정에 포함하지 않는다. | Q1 은 사용자 결정(2026-09-04)이고 Q2~Q4 는 결정 위임(2026-08-23)에 따른 집행이다. **Q1**: `/search` 는 "임직원 성명을 찾습니다" 라고 고지하고 백엔드도 "성명 부분일치 조회" 라고 명시한다 — 사람 이름 입력은 오용이 아니라 설계된 용도이고, 그 사실이 알려진 채 승인됐다. URL 에서 빼려면 네이티브 GET 폼을 포기해야 하는데 그러면 JS 로드 전 동작 상실과 함께 `page.tsx` 주석이 기록한 React #418(hydration mismatch) 계열이 다시 열린다. **Q3**: `modern_route` 는 프런트 `normalizeInternalRoute` 를 지나 그대로 URL 이 된다 — 그 함수는 결함이 아니라 문서화된 의도로 쿼리를 보존한다. 방어선이 `@Size(max=500)` 하나뿐이고 DB 에도 CHECK 가 없어, 관리자가 자유 입력칸에 `?q=<사람 이름>` 을 타이핑하면 그것이 사용자 URL 이 됐다. 읽기 쪽에서 자르면 저장값이 조용히 달라져 관리자가 이유를 모르므로 저장에서 거부한다. ⚠ 운영(OCI) 실측이 배포 안전을 증명했다 — 메뉴 84행 중 `modern_route` 70, 쿼리 보유 12, distinct 키는 `tab` 1종이고 **70행 전부 불통과 0** 이다. 다만 메뉴는 런타임 편집 가능하므로 이것은 측정 시점의 사실이며, 제약의 존재 이유는 미래의 자유 입력과 파생 제품(adopter)의 DB 다. **Q4**: 셋 다 코드가 이미 이유를 URL 에 실어 보내는데 화면만 버리고 있었다 — 걷어내면 그 의도까지 지운다. 방향이 기존 사례와 반대다: DEC-OPS-022·GAP-CMTY-001·GAP-CODE-001 은 **화면이 없는 일을 약속한** 것을 정정했고, 여기는 **아는 사실을 말하지 않은** 것을 고쳤다. ⚠ 집행 중 결함 5건이 드러나 함께 닫혔다 — 그중 `nttId` 는 초안이 "위험 0 인 이름 정합" 으로 분류했으나 실제로는 서버(`params.pstSn \|\| params.nttId`)와 클라이언트(`pstSn` 만)의 키 공간 불일치로 **`?nttId=` 진입 시 상호작용 전체가 0번 글로 동작**했다. `groupId` 도 "죽은 표면" 이 아니라 동작하는 딥링크 소비자였다. 이 결정은 census 의 `dataClass`·`approvalStatus` 분류 승인이 **아니다** — 새 레코드도 전 축 `unverified` 로 남는다. | [분류 초안](../../docs/01-product/url-state-classification-draft.md), [Q1 경계 계약](../../scripts/log-search-url-boundary-contract.test.mjs), [MenuDto](../../business-core/src/main/java/nuri/business/service/menu/dto/MenuDto.java), [MenuRouteQueryKeyTest](../../business-core/src/test/java/nuri/business/service/menu/dto/MenuRouteQueryKeyTest.java), [PD-UX-002](../../docs/04-operations/pending-decisions.md) | 2026-09-04 | - |

| DEC-OPS-030 | accepted | 결재 도메인을 완결한다(2026-09-05 도메인별 프로세스·UX 감사의 1순위 조치). ① 결재함 `/approvals` 에 **기안 다이얼로그**를 신설해 `POST /api/v1/approvals`(신청자 = 인증 주체, 본문은 업무구분·결재자·신청일만)로 상신한다. 업무 구분은 공통코드 COM075 의 사용 중 상세코드이며 서버가 등록 코드를 검증하고, 인증 사용자용 `GET /approvals/task-types` 가 선택지를 내려준다(공통코드 API 는 관리자 전용). 그룹이 비어 있으면 화면은 상신을 막고 코드 등록을 안내한다 — 임의 시드는 하지 않는다(PD-DB-003). ② 탭을 실제 질의 축으로 개명한다 — '결재 처리 이력'(실제로는 `/approvals/my` 신청자 기준) → '내가 올린 결재', 신설 `GET /approvals/processed`(결재자 본인의 승인·반려 건) → '내가 처리한 결재'. 비활성 '보관함' 버튼은 걷는다(G10). ③ 결재 결과 알림·문자·메일 본문을 enum 상수명(`APPROVED`)·내부 ID 표기에서 한국어 상태명(`승인`/`반려`)·문서 번호로 바꾸고 사유 절은 반려에만 싣는다. ④ 문자 발신 번호는 `nuri.notification.sender.tel` 프로퍼티로 옮기고 비어 있으면 문자 채널만 건너뛴다(메일 From 은 이미 `nuri.mail.from` 이며 리스너의 `admin@egov.enterprise` 리터럴은 어디에도 쓰이지 않던 값이라 제거). `/approvals/draft` 목업(demo-isolated approved)은 그대로 두되 결재 허브로 안내한다. | 사용자 지시(2026-09-05 "모두 순서대로 진행")와 결정 위임(2026-08-23)에 따른 집행이다. 감사 실측: 등록 API 와 프런트 서비스 메서드(`IsmAdminService.createInfrmlSanctn`)는 있었지만 호출부 0건이라 **승인·반려는 되는데 올릴 수 없는** 반쪽 흐름이었고, '결재 처리 이력' 탭은 `findByAplcntId` 를 불러 결재자가 처리한 문서를 볼 곳이 없었으며, `String.format("%s", SanctionStatus)` 가 사용자에게 `APPROVED 되었습니다. 사유: 없음` 을 보냈다. 종전 `POST /informal-sanctions` 는 `aplcntId @NotBlank` 를 검증한 뒤 서버가 덮어써 **클라이언트가 덮어써질 값을 보내야 통과하는** 계약이라 전용 요청 DTO(`ApprovalDraftRequest`)를 두었다. 상신 화면을 `/approvals/draft` 실화면이 아니라 허브의 다이얼로그로 둔 것은 그 라우트가 owner 승인된 demo-isolated 이고 새 URL·query 상태를 만들지 않기 위해서다. 게이트: 읽기 인가 표면 census 176→178(같은 컨트롤러·같은 인가 축 — H3), 쓰기 정책 registry 행 추가 + manifest 해시 갱신, 라우트 capability census(`/approvals` actions +create·refresh, E3), 생성 API 경계 census 재생성(347/347), e2e 11 을 "상신 미지원 정직성" 계약에서 **상신→대기함→승인→처리함 완주**로 전환(테스트가 관리자 API 로 COM075 코드 1건을 보장한 뒤 시작). | [ApprovalApiController](../../api-server/src/main/java/nuri/api/controller/business/approval/ApprovalApiController.java), [ApprovalDraftRequest](../../api-server/src/main/java/nuri/api/controller/business/approval/dto/ApprovalDraftRequest.java), [InformalSanctionService](../../business-app/src/main/java/nuri/business/service/informalsanction/InformalSanctionService.java), [SanctionEventListener](../../business-app/src/main/java/nuri/business/service/informalsanction/event/SanctionEventListener.java), [ApprovalDraftDialog](../../frontend/src/app/approvals/ApprovalDraftDialog.tsx), [ApprovalHubClient](../../frontend/src/app/approvals/ApprovalHubClient.tsx), [e2e 11](../../frontend/e2e/11-enterprise-workflow.spec.ts), [authorization-policies](../../config/governance/authorization-policies.json) | 2026-09-05 | - |

| DEC-OPS-031 | accepted | 설문 응답 제출에 **기간 가드**를 둔다(감사 2순위). 서버 `SurveyResultService.submitResponse` 는 존재·중복 검사에 더해 오늘(Asia/Seoul, yyyyMMdd)이 `srvyBgngYmd`~`srvyEndYmd` 안일 때만 저장한다 — 비어 있는 경계는 열린 것으로 보고(시작일 없음 = 즉시, 종료일 없음 = 무기한), 값이 있는데 8자리 날짜가 아니면 판정 불가로 **열지 않는다**. 화면은 같은 규칙(`lib/survey-status.ts`)으로 목록에 '진행중/예정/종료/알 수 없음' 상태 열을 두고, 응답 화면은 설문 제목·기간·상태를 보여 주며 기간 밖이면 사유 안내 + 입력 잠금 + 제출 버튼 비노출(통계는 그대로 열람). 목록 행 액션도 종료된 설문은 '결과 보기' 로 말한다. | 사용자 지시(2026-09-05 "모두 순서대로 진행")와 결정 위임(2026-08-23)에 따른 집행이다. 감사 실측: 등록·수정은 `validateSurveyDates` 로 기간을 검사하면서 제출은 기간을 보지 않아 **종료된 설문·시작 전 설문에도 응답이 저장**됐고, 화면도 "하나 이상 골랐는가" 만 보고 제출을 열었다. 같은 도메인의 투표(`OnlinePollService.vote`)는 이미 시작 전·종료 후를 막고 있어 도메인 안 비대칭이었다. 판정 규칙을 투표의 `poll-status.ts` 와 같은 8자리 문자열 비교로 두되, 설문은 기간 컬럼이 NULL 허용이라 빈 경계를 개방으로 본다(기간 없는 기존 설문의 응답을 막지 않기 위해서다). 응답 화면이 제목 대신 일련번호만 보여 주던 것도 함께 고쳤다. | [SurveyResultService](../../business-app/src/main/java/nuri/business/service/survey/SurveyResultService.java), [survey-status](../../frontend/src/lib/survey-status.ts), [SurveyDetailClient](../../frontend/src/app/survey/[id]/SurveyDetailClient.tsx), [SurveyClient](../../frontend/src/app/survey/SurveyClient.tsx), [SurveyResultServiceTest](../../business-app/src/test/java/nuri/business/service/survey/SurveyResultServiceTest.java) | 2026-09-05 | - |

| DEC-OPS-032 | accepted | 사용자 관리 화면(`/admin/user/manage`)의 상세 패널에 **관리자 비밀번호 초기화**를 붙인다(감사 3순위). 관리자가 새 비밀번호(8~20자, 백엔드 `AdminPasswordChangeRequest` 와 같은 한계)와 확인을 입력해 `PATCH /admin/system/users/{userId}/password` 를 부르고, 시스템은 새 비밀번호를 전달하지 않으며 화면이 "사용자에게 직접 안전한 경로로 알려 주세요" 라고 말한다. 로그인 화면에 비밀번호 찾기는 두지 않는다(엔터프라이즈 정책 유지). | 사용자 지시(2026-09-05 "모두 순서대로 진행")와 결정 위임(2026-08-23)에 따른 집행이다. 감사 실측: 초기화 API 와 프런트 서비스 메서드(`userAdminService.updatePassword`)는 있었지만 호출부가 0건이었고 로그인 화면에도 찾기 경로가 없어, 비밀번호를 잊은 사용자는 DB 직접 조작 말고는 길이 없었다(P1). 임시 비밀번호를 시스템이 생성·표시·메일 전송하는 대안은 전달 채널(메일 발신 설정 유무)과 노출 위험을 제품이 아직 정하지 않아 택하지 않았다 — 관리자가 정해 직접 전달하는 최소 흐름이 현재 제품 경계 안에서 정직하다. 폼은 프로젝트 표준(`useManualFormValidation` + 생성 zod 확장, 동기 제출 잠금, 서버 필드 오류 매핑·입력 보존)을 따르고 폼 검증 census 에 compliant 로 등록했다. | [AdminPasswordResetForm](../../frontend/src/components/admin/user/AdminPasswordResetForm.tsx), [UserOrgHubClient](../../frontend/src/app/admin/user/UserOrgHubClient.tsx), [UserApiController#updatePasswordByAdmin](../../api-server/src/main/java/nuri/api/controller/UserApiController.java) | 2026-09-05 | - |
| DEC-OPS-033 | accepted | 쪽지함(`/note`)과 결재함(`/approvals`)에 **서버 페이징**을 붙인다(감사 4순위). 쪽지함은 `StandardDataTable` 의 pagination 계약(기본 20건, 페이지당 건수 변경·탭 전환 시 1페이지 복귀)으로, 결재함은 `PagePagination`(고정 20건, 탭 전환·기안 등록 시 1페이지 복귀)으로 배선하고, 결재함은 페이지가 바뀌면 이전 페이지의 선택·반려 사유·오류를 함께 해제한다. 백엔드 API 는 이미 `Pageable` 계약이라 서버 변경은 없다. | 사용자 지시(2026-09-05 "모두 순서대로 진행")와 결정 위임(2026-08-23)에 따른 집행이다. 감사 실측: 쪽지함은 `{ page: 0, size: 100 }`, 결재함은 `{ page: 0, size: 50 }` 을 한 번만 조회하고 페이저가 없어 그 뒤 항목은 어떤 경로로도 도달 불가였다(P1) — 서버는 total 을 내려주는데 화면이 버리고 있었다. 결재함이 표 pagination 이 아니라 `PagePagination` 을 쓰는 이유는 A2 `MasterDetailPage` 의 master 가 표가 아닌 선택 목록이고, 선택이 페이지에 종속돼 stale 선택 해제를 페이지 변경과 같은 핸들러에 결속해야 하기 때문이다(페이지 넘긴 뒤 이전 페이지 문서를 승인·반려하는 오조작 차단). URL 에 page 를 싣지 않는다 — PD-UX-002 의 census 승인 경계 밖에서 새 URL producer 를 만들지 않기 위해서다. | [note/page.tsx](../../frontend/src/app/note/page.tsx), [ApprovalHubClient](../../frontend/src/app/approvals/ApprovalHubClient.tsx), [쪽지 페이징 계약](../../frontend/src/app/note/__tests__/NotePage.validation.test.tsx), [결재함 페이징 계약](../../frontend/src/app/approvals/__tests__/ApprovalHubClient.actions.test.tsx) | 2026-09-05 | - |
| DEC-OPS-034 | accepted | 게시글 작성 화면 3종을 정본 `/admin/community/boards/insert-board-article`(`BoardRegistClient`) 하나로 **수렴**하고, 첨부 **삭제 경로**를 연다(감사 5순위, D06-01·D06-02). ① 정본을 상위집합으로 만든다 — 새 첨부는 서버 액션 FormData `files` 로 실어 보내고(액션은 이미 그 키를 읽고 있었지만 화면에 파일 입력이 없었다), 수정 모드는 기존 첨부를 나열해 건별 삭제하며, `write` 화면만 갖고 있던 게시 기간·행사 일자 입력을 옮겼다(기간이 집행되지 않는다는 문구와 `BoardPredicate` 부재를 honest-affordance 계약이 함께 고정). ② `/admin/community/boards/write`·`/admin/community/boards/[id]` 는 page-redirect 로 정본에 보내고 클라이언트·테스트를 삭제했다(`/admin/survey` 선례 — 문자열 URL 참조는 정적 분석으로 안 잡혀 라우트 물리 삭제는 하지 않는다). 인바운드 링크 2건은 `?bbsId=` 를 실어 정본으로 직접 간다. `[id]` 의 식별자는 종전 화면이 쓰지 않았으므로 게시판 ID 로 해석하지 않는다(H4). ③ 백엔드 `DELETE /api/v1/files/{atchFileSn}/{fileSn}` 을 신설하고 삭제 인가를 열람 정책과 **별개의 판정** `FileAccessPolicy#assertDeletable` 로 둔다 — 업로더 본인·참조 행 소유자·(개인 귀속이 아닌 첨부의) 관리자만 지우고, **공유 열람 근거는 삭제 근거가 아니다**. ④ overlay 의 두 라우트 disposition 을 `consolidate-to-canonical`(proposed)로 제안한다 — 개별 승인은 owner PR 리뷰(ADR-0007). 게시글 삭제 시 첨부 물리 삭제는 보존 정책 결정 전까지 하지 않는다(GAP-STORAGE-001 (b)와 함께 설계). | 사용자 지시(2026-09-05 "모두 순서대로 진행")와 결정 위임(2026-08-23)에 따른 집행이다. 감사 실측: 작성 화면이 셋이고 어디서 들어왔느냐에 따라 첨부·임시저장·편집기가 있거나 없었다(D06-01, P2). 첨부는 서비스 계층 `deleteFile` 만 있고 HTTP 노출도 화면 버튼도 없어 한 번 올리면 지울 수 없었다(D06-02, P2) — 프런트는 2026-08-05 에 405 를 만나던 메서드를 제거하며 "인가와 함께 먼저 설계할 것" 을 남겼고, 이번에 그 순서대로 했다. ⚠ 실측이 감사와 달랐던 점: 정본 화면에는 첨부 UI 가 아예 없었고(감사는 '첨부' 를 갖춘 것으로 기술), `[id]` 화면은 인바운드 0·`id` 미사용이었다. 그래서 '나머지 둘을 리다이렉트' 만으로는 기능이 사라지므로 정본을 먼저 상위집합으로 만들었다. 삭제 판정을 `assertReadable` 재사용이 아닌 별도 표로 둔 이유: 비밀글이 아닌 게시글 첨부는 인증 사용자 누구나 읽지만 그것이 남의 첨부를 지울 권리는 아니다 — 계약 테스트가 '공유 근거만 있는 타인은 읽지만 못 지운다' 를 대조군으로 고정한다(H3). 같은 화면의 native `confirm()`(임시저장 복구)은 useConfirm 이 들어오며 이름이 겹쳐 함께 이행했다 — 감사 9순위 D06-03 의 4곳 중 1곳이 먼저 닫혔다. 하네스 갱신: 인가 정책 census DELETE 4행 + manual guard 1행(registry 해시 재결속), 생성 계약 370→374, boundary census, 폼 검증 census(−2 화면 +1 destructive action), capabilities·overlay(manifest sha)·URL-state census(registry sha) 재생성, reachability/capabilities 계약 alias 17→19·pageRedirects 5→7. | [BoardRegistClient](../../frontend/src/app/admin/community/boards/insert-board-article/BoardRegistClient.tsx), [첨부 계약](../../frontend/src/app/admin/community/boards/insert-board-article/__tests__/BoardRegistClient.attachment.test.tsx), [FileAccessPolicy](../../business-core/src/main/java/nuri/business/service/file/FileAccessPolicy.java), [FileAccessPolicyTest](../../business-core/src/test/java/nuri/business/service/file/FileAccessPolicyTest.java), [FileApiController](../../api-server/src/main/java/nuri/api/controller/business/file/FileApiController.java), [write redirect](../../frontend/src/app/admin/community/boards/write/page.tsx), [overlay](../../config/ui-navigation-disposition-proposal.json) | 2026-09-05 | - |
| DEC-OPS-035 | accepted | 메일·문자 발송의 **수신자 피커를 공용화**하고 주소록을 그 데이터 소스로 편입한다(감사 6순위, D09-02·D09-03·D09-04). ① 공용 `RecipientPicker`(사용자 검색 탭 · 주소록 탭, 다중 선택, 채널에 맞는 연락처가 있는 명함만 선택 가능)를 메일 작성·문자 작성에 붙인다. 직접 입력(이메일·번호)은 보존한다. ② 서버가 연락처를 해석한다 — 코어 `UserContactService`(esntlId→이메일·휴대전화, 요청 순서 보존·미존재 거부)를 두고, 메일은 `SentMailDto.recipients[{esntlId|emlAddr}]`(수신자마다 발송 이력 1건 — 100자 `recptnPerson` 한계 제거, 종전 필드는 호환 유지), 문자는 `SmsRecptnDto{esntlId|rcptnTelno}` 로 받는다. 등록 연락처가 없는 사용자가 하나라도 있으면 **이름을 밝히고 전체를 거부**한다(부분 발송 금지). ③ 사용자 검색 응답은 종전대로 개인정보를 담지 않으며, 해석된 연락처는 발송에만 쓰고 어떤 응답에도 실리지 않는다(H3). ④ app→core 결합 census 24→26(mail→user·sms→user)을 사유와 함께 갱신한다. | 사용자 지시(2026-09-05 "모두 순서대로 진행")와 결정 위임(2026-08-23)에 따른 집행이다. 감사 실측: 쪽지는 사용자 검색 피커로 사람을 고르는데 메일은 "이 화면은 ID로 주소를 찾지 못합니다" 라며 이메일만 받았고, 선택한 수신자를 쉼표로 이어 100자 컬럼에 넣어 3명 이상 동시 발송이 사실상 불가했으며(D09-02), 주소록은 어떤 발송 화면에서도 쓰이지 않는 고립된 CRUD 였고(D09-03), 문자는 서버가 수신자 배열을 받는데 화면이 번호 1건만 받았다(D09-04). ⚠ 화면이 주소를 해석할 수 없는 것은 결함이 아니라 설계다 — `UserSearchDto` 는 개인정보를 의도적으로 뺐다(계정 열거·연락처 노출 방어). 따라서 피커가 사람을 고르는 권한이 연락처를 읽는 권한으로 넓어지지 않도록 해석을 서버에 두고 결과를 응답으로 되돌리지 않았다. 다수 수신은 수신자 테이블 신설 대신 발송 건당 1수신자로 나눴다 — DB 변경 없이 컬럼 폭 제약을 없애고, 수신자별 결과가 이력에 따로 남아 문자의 `수신자 결과` 와 같은 모양이 된다. 시드 사용자에는 연락처가 없으므로 e2e 는 `PUT /users/me` 로 이메일을 먼저 등록한 뒤 이름으로 자신을 골라 완주한다. 피커의 탭은 Radix Tabs 대신 수동 구현이다 — jsdom 에서 role=tab 도 비활성 패널 숨김도 만들지 않아 계약 테스트가 두 패널을 동시에 보게 되기 때문이다. | [RecipientPicker](../../frontend/src/app/components/ui/recipient-picker.tsx), [피커 계약](../../frontend/src/app/components/ui/__tests__/recipient-picker.test.tsx), [UserContactService](../../business-core/src/main/java/nuri/business/service/user/UserContactService.java), [MailService](../../business-app/src/main/java/nuri/business/service/mail/MailService.java), [SmsService](../../business-app/src/main/java/nuri/business/service/sms/SmsService.java), [MailSendHubClient](../../frontend/src/app/admin/collaboration/mail-send/MailSendHubClient.tsx), [SmsAdminClient](../../frontend/src/app/admin/uss/ion/sms/SmsAdminClient.tsx), [결합 census](../../api-server/src/test/java/nuri/api/harness/CrossDomainCouplingLinterTest.java) | 2026-09-05 | - |

## 기록 템플릿

새 결정은 `ID | 상태 | 결정 | 이유 | 정본 | 시행일 | supersedes`를 모두 채운다. 아키텍처·제품 경계를 바꾸면 먼저 ADR을 만들고 이 표에는 한 줄 링크만 추가한다.
