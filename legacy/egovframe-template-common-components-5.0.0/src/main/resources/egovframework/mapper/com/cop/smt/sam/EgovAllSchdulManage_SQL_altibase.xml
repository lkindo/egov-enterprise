<?xml version="1.0" encoding="UTF-8"?><!-- 
	?섏젙??                ?섏젙??                         ?섏젙?댁슜
  =========     =======    =================================================
  2011.9.2   	?뺤쭊??    	 	FRST_REGIST_PNTTM而щ읆??FRST_REGISTER_PNTTM Alias 異붽?
  							SCHDUL_KND_CODE而щ읆??SCHDUL_KIND_CODE Alias 異붽?
  							(egovMap???ъ슜?섎뒗 議고쉶 荑쇰━?섍꼍??
  2011.09.19       ?쒖???		AllSchdulManage.selectIndvdlSchdulManage ?깅줉?쇱옄 Character ??낆쑝濡?蹂寃?
  2011.10.05    ?쒖???              蹂댁븞 痍⑥빟???먭??ы빆 諛섏쁺 $->#蹂寃?					
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="AllSchdulManage">

	<!-- ?꾩젣?쇱젙::紐⑸줉議고쉶_寃뚯떆臾쇱젙蹂?-->
	<select id="selectIndvdlSchdulManage" parameterType="comDefaultVO" resultType="egovMap">
	
		
	SELECT *
	FROM (
	SELECT T0.*, ROWNUM RNUM
		FROM (
	
	
	 
		SELECT 
			A.SCHDUL_ID,
			A.SCHDUL_SE,
			A.SCHDUL_DEPT_ID,
			A.SCHDUL_KND_CODE SCHDUL_KIND_CODE,
			A.SCHDUL_BGNDE,
			A.SCHDUL_ENDDE,
			A.SCHDUL_NM,
			A.SCHDUL_CN,
			A.SCHDUL_PLACE,
			A.SCHDUL_IPCR_CODE,
			A.SCHDUL_CHARGER_ID,		
			A.ATCH_FILE_ID,
			TO_CHAR(A.FRST_REGIST_PNTTM ,'YYYY-MM-DD') FRST_REGISTER_PNTTM,
			(SELECT USER_NM FROM COMTNEMPLYRINFO WHERE ESNTL_ID = A.FRST_REGISTER_ID) FRST_REGISTER_NM,
			A.FRST_REGISTER_ID,
			A.LAST_UPDT_PNTTM,
			A.LAST_UPDUSR_ID,
			A.REPTIT_SE_CODE 
		FROM COMTNSCHDULINFO A
		WHERE 1=1
	 
	 <if test="searchKeyword != null and searchKeyword != ''">
	 	
	 		<if test='searchCondition == "SCHDUL_NM"'>
 				AND SCHDUL_NM LIKE '%' || #{searchKeyword} || '%'
 			</if>
 			<if test='searchCondition == "SCHDUL_CN"'>
 				AND SCHDUL_CN LIKE '%' || #{searchKeyword} || '%'
 			</if>
	 	
	 </if>
	 
		ORDER BY A.FRST_REGIST_PNTTM DESC
	
	
		
		) T0
	) 
	WHERE RNUM BETWEEN #{firstIndex} + 1 AND #{firstIndex} + #{recordCountPerPage}
		
	

	</select>
	
	<!-- ?꾩젣?쇱젙::紐⑸줉議고쉶_寃뚯떆臾?珥앷컻?? -->
	<select id="selectIndvdlSchdulManageCnt" parameterType="comDefaultVO" resultType="int">
		SELECT 
		COUNT(*) totcnt
		FROM COMTNSCHDULINFO
		WHERE 1=1
	 <if test="searchKeyword != null and searchKeyword != ''">
	 	
	 		<if test='searchCondition == "SCHDUL_NM"'>
 				AND SCHDUL_NM LIKE '%' || #{searchKeyword} || '%'
 			</if>
 			<if test='searchCondition == "SCHDUL_CN"'>
 				AND SCHDUL_CN LIKE '%' || #{searchKeyword} || '%'
 			</if>
	 	
	 </if>
	</select>



</mapper>