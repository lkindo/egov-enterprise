server:
  port: 8080
  error:
    include-stacktrace: always
    include-exception: true
spring:
  main:
    allow-circular-references: false
    lazy-initialization: false
  config:
    activate:
      on-profile: dev
  autoconfigure:
    exclude:
      - org.springframework.ai.autoconfigure.openai.OpenAiAutoConfiguration
      - org.springframework.ai.autoconfigure.openai.OpenAiConnectionDetailsAutoConfiguration

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5433/egovdb}
    jdbc-url: ${DB_URL:jdbc:postgresql://localhost:5433/egovdb}
    username: ${DB_USERNAME:egov}
    password: ${DB_PASSWORD:egov123}
#    url: ${DB_URL:jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres?currentSchema=public&prepareThreshold=0}
#    jdbc-url: ${DB_URL:jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres?currentSchema=public&prepareThreshold=0}
#    username: ${DB_USERNAME:postgres.kmtcbkxvrbnfijvbdsrx}
#    password: ${DB_PASSWORD:s5isI0KE48Bd9kD1}
    driver-class-name: org.postgresql.Driver
    hikari:
      jdbc-url: ${DB_URL:jdbc:postgresql://localhost:5433/egovdb}

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        generate_statistics: true

  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp

logging:
  level:
    root: info
    nuri: debug
    egovframework: debug
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace
    org.hibernate.stat: debug
    org.springframework.orm.jpa: debug
    org.springframework.security: debug

jwt:
  # CRITICAL: JWT_SECRET environment variable MUST be set in production.
  # Using a default secure length string for local development to prevent startup failures.
  secret: ${JWT_SECRET:dGhpcy1pcy1hLXZlcnktbG9uZy1zZWNyZXQta2V5LWZvci1lZ292LWVudGVycHJpc2UtbW9kZXJuaXphdGlvbg==}
