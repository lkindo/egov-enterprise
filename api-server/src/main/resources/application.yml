spring:
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true
  jackson:
    serialization:
      fail-on-empty-beans: false
  sql:
    init:
      mode: never

file:
  upload:
    path: ${FILE_UPLOAD_PATH:./storage/uploads}
