-- DEC-OPS-136(DIP D7): 리프레시 토큰을 원문이 아니라 SHA-256 해시로 저장한다.
-- 기존 행은 원문이다. 새 코드는 해시로만 찾으므로 이 행들은 다시 쓰이지 않지만, 원문 자체가 비밀이라
-- DB(백업·복제본 포함)를 읽은 사람이 그 값으로 재발급할 수 있다. 전부 지운다 — 모든 사용자가 다시 로그인한다.
-- 사용자 데이터가 아니라 세션 자격이며, 사용자 위임(2026-09-25)이 이 삭제를 허용했다.
SET LOCAL lock_timeout = '5s';
SET LOCAL statement_timeout = '60s';

DELETE FROM tb_auth_rfsh_tk;
