<?xml version="1.0" encoding="UTF-8"?><!--
	?섏젙??	?섏젙??				?섏젙?댁슜
  =========     =======    =================================================
  2011.10.05	?뺤쭊??	蹂댁븞 痍⑥빟???먭??ы빆???곕Ⅸ ?섏젙?ы빆 諛섏쁺
  							${searchKeyword}瑜? #{searchKeyword} 濡?蹂寃?  							${searchCondition}??<isEqual property="searchCondition" compareValue="?좏깮議곌굔紐?>?쇰줈 ?섏젙
  2011.10.05	?뺤쭊??	?⑤씪??POLL 以묐났 ?ы몴 泥댄겕 SQL 異붽?  							
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="OnlinePollPartcptn">

	<!-- ?⑤씪?퇠OLL李몄뿬::ResultMap ?좎뼵 -->
	<resultMap id="OnlinePollManageVO" type="egovframework.com.uss.olp.opm.service.OnlinePollManage">
		<result property="pollId" column="POLL_ID"/>
		<result property="pollNm" column="POLL_NM"/>
		<result property="pollBeginDe" column="POLL_BGNDE"/>
		<result property="pollEndDe" column="POLL_ENDDE"/>
		<result property="pollKindCode" column="POLL_KND"/>
		<result property="pollDsuseYn" column="POLL_DSUSE_ENNC"/>
		<result property="pollAutoDsuseYn" column="POLL_ATMC_DSUSE_ENNC"/>		
		<result property="frstRegisterPnttm" column="FRST_REGIST_PNTTM"/>
		<result property="frstRegisterId" column="FRST_REGISTER_ID"/>
		<result property="lastUpdusrPnttm" column="LAST_UPDT_PNTTM"/>
		<result property="lastUpdusrId" column="LAST_UPDUSR_ID"/> 			
	</resultMap>
	
	<!-- ?⑤씪?퇠OLL李몄뿬::紐⑸줉議고쉶 寃뚯떆臾쇱젙蹂?-->
	<select id="selectOnlinePollManageList" parameterType="comDefaultVO" resultType="egovMap">
	 <![CDATA[
		SELECT
		A.POLL_ID,
		A.POLL_NM, 
		A.POLL_BGNDE																POLL_BEGIN_DE,
		A.POLL_ENDDE																POLL_END_DE,
		A.POLL_KND																	POLL_KIND_CODE,
		A.POLL_DSUSE_ENNC,
		A.POLL_ATMC_DSUSE_ENNC,
		A.FRST_REGISTER_ID,
		TO_CHAR(A.FRST_REGIST_PNTTM,'YYYYmmdd')									FRST_REGISTER_PNTTM,
		(SELECT USER_NM FROM NEMPLYRINFO WHERE ESNTL_ID = A.FRST_REGISTER_ID)	FRST_REGISTER_NM 
		FROM NONLINEPOLLMANAGE A
		WHERE 1=1
		AND A.POLL_ID IN (SELECT DISTINCT(POLL_ID) POLL_ID FROM NONLINEPOLLIEM)
		AND A.POLL_DSUSE_ENNC = 'N'
		AND (( A.POLL_ATMC_DSUSE_ENNC = 'N')  OR (A.POLL_ATMC_DSUSE_ENNC = 'Y' AND TO_CHAR(NOW(),'YYYYmmdd') >= REPLACE(A.POLL_BGNDE,'-','') AND TO_CHAR(NOW(),'YYYYmmdd') <= REPLACE(A.POLL_ENDDE,'-',''))) 
	 ]]>
	 <if test="searchKeyword != null and searchKeyword != ''">
	 	
	 		
	 			<if test="searchCondition == 'POLL_NM'">
	 				AND POLL_NM LIKE CONCAT('%', #{searchKeyword}, '%')
	 			</if>
	 		
	 	
	 </if>
	 
		ORDER BY A.FRST_REGIST_PNTTM DESC
		LIMIT #{recordCountPerPage} OFFSET #{firstIndex}
	
	</select>
	<!-- ?⑤씪?퇠OLL李몄뿬::紐⑸줉議고쉶_寃뚯떆臾?珥앷컻?? -->
	<select id="selectOnlinePollManageListCnt" parameterType="comDefaultVO" resultType="int">
	<![CDATA[	
		SELECT 
		COUNT(*) totcnt
		FROM NONLINEPOLLMANAGE A
		WHERE 1=1
		AND A.POLL_ID IN (SELECT DISTINCT(POLL_ID) POLL_ID FROM NONLINEPOLLIEM)
		AND A.POLL_DSUSE_ENNC = 'N'
		AND (( A.POLL_ATMC_DSUSE_ENNC = 'N')  OR (A.POLL_ATMC_DSUSE_ENNC = 'Y' AND TO_CHAR(NOW(),'YYYYmmdd') >= REPLACE(A.POLL_BGNDE,'-','') AND TO_CHAR(NOW(),'YYYYmmdd') <= REPLACE(A.POLL_ENDDE,'-',''))) 
	 ]]>
	 <if test="searchKeyword != null and searchKeyword != ''">
	 	
	 		
	 			<if test="searchCondition == 'POLL_NM'">
	 				AND POLL_NM LIKE CONCAT('%', #{searchKeyword}, '%')
	 			</if>
	 		
	 	
	 </if>
	</select>
	
	
	<!-- ?⑤씪?퇠OLL李몄뿬::?곸꽭蹂닿린 -->
	<select id="selectOnlinePollManageDetail" resultType="egovMap">
	SELECT 
		A.POLL_ID,
		A.POLL_NM,
		A.POLL_BGNDE					POLL_BEGIN_DE,
		A.POLL_ENDDE					POLL_END_DE,
		A.POLL_KND						POLL_KIND_CODE,
		A.POLL_DSUSE_ENNC,
		A.POLL_ATMC_DSUSE_ENNC,
		A.FRST_REGISTER_ID,
		A.FRST_REGIST_PNTTM				FRST_REGISTER_PNTTM,
		A.FRST_REGISTER_ID,
		A.LAST_UPDT_PNTTM,
		A.LAST_UPDUSR_ID  
		FROM NONLINEPOLLMANAGE A
		WHERE 1=1
		AND A.POLL_ID=#{pollId} 
	</select>

	
	<!-- ?⑤씪?퇠OLL??ぉ::紐⑸줉議고쉶 寃뚯떆臾쇱젙蹂?-->
	<select id="selectOnlinePollItem" resultType="egovMap">
	 
		SELECT 
		POLL_ID,
		POLL_IEM_ID,
		POLL_IEM_NM,
		FRST_REGISTER_ID,
		FRST_REGIST_PNTTM,
		LAST_UPDUSR_ID,
		LAST_UPDT_PNTTM
		FROM NONLINEPOLLIEM
		WHERE 1=1
		AND POLL_ID = #{pollId} 
		ORDER BY POLL_IEM_NM DESC
	 
	</select>

	<!-- ?⑤씪?퇠OLL李몄뿬::?⑤씪?퇠OLL寃곌낵 ?낅젰 -->
	<insert id="insertOnlinePollResult">	     
	 

	INSERT INTO NONLINEPOLLRESULT ( 
	POLL_ID,
	POLL_IEM_ID,
	POLL_RESULT_ID,
	FRST_REGIST_PNTTM,
	FRST_REGISTER_ID,
	LAST_UPDT_PNTTM,
	LAST_UPDUSR_ID
	)VALUES( 
	#{pollId},
	#{pollIemId},
	#{pollResultId},
	NOW(),
	#{frstRegisterId},
	NOW(),
	#{lastUpdusrId} 
	)
	 
	</insert>
	
	<!-- ?⑤씪?퇠OLL李몄뿬::?⑤씪?퇠OLL李몄뿬 ?잛닔 議고쉶(李몄뿬 ?щ? ?먮떒???꾪빐?? -->
	<select id="selectOnlinePollResult" resultType="int">
	 
	 SELECT COUNT(*)
	 FROM NONLINEPOLLRESULT
	 WHERE POLL_ID = #{pollId}
	 AND FRST_REGISTER_ID = #{frstRegisterId}
	 
	 
	</select>

	<!-- ?⑤씪?퇠OLL李몄뿬::?⑤씪?퇠OLL?듦퀎瑜?議고쉶?쒕떎.  -->
	<select id="selectOnlinePollPartcptnStatistics" resultType="egovMap">
	 
	SELECT 
	A.POLL_IEM_ID,
	COUNT(A.POLL_IEM_ID) POLL_IEM_ID_CNT,
	(100/ (SELECT COUNT(*) CNT FROM NONLINEPOLLRESULT WHERE POLL_ID = A.POLL_ID)) * COUNT(A.POLL_IEM_ID),1 POLL_IEM_PERCENT
	FROM NONLINEPOLLRESULT A
	WHERE 1=1
	AND A.POLL_ID = #{pollId} 
	GROUP BY A.POLL_ID, A.POLL_IEM_ID
	 
	</select>
</mapper>
