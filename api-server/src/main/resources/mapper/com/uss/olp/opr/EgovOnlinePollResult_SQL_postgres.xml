<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="OnlinePollResult">
	
	<!-- ?⑤씪?퇠OLL愿由?:紐⑸줉議고쉶 寃뚯떆臾쇱젙蹂?-->
	<select id="selectOnlinePollManageList" resultType="egovMap">
	 
		SELECT
		A.POLL_ID,
		A.POLL_NM, 
		A.POLL_BGNDE,
		A.POLL_ENDDE,
		A.POLL_KND,
		A.POLL_DSUSE_ENNC,
		A.POLL_ATMC_DSUSE_ENNC,
		A.FRST_REGISTER_ID,
		TO_CHAR(A.FRST_REGIST_PNTTM,'YYYYmmdd') 								FRST_REGISTER_PNTTM,
		(SELECT USER_NM FROM NEMPLYRINFO WHERE ESNTL_ID = A.FRST_REGISTER_ID)	FRST_REGISTER_NM 
		FROM NONLINEPOLLMANAGE A
		WHERE 1=1
		AND POLL_ID = #{pollId} 
	 
	 
		ORDER BY A.FRST_REGIST_PNTTM DESC
	
	</select>

	<!-- ?⑤씪?퇠OLL寃곌낵::紐⑸줉議고쉶 寃뚯떆臾쇱젙蹂?-->
	<select id="selectOnlinePollResult" resultType="egovMap">
	 
		SELECT 
		A.POLL_ID,
		A.POLL_IEM_ID,
		(SELECT POLL_IEM_NM FROM NONLINEPOLLIEM WHERE POLL_ID=A.POLL_ID AND POLL_IEM_ID=A.POLL_IEM_ID) POLL_IEM_NM,
		A.POLL_RESULT_ID,
		A.FRST_REGISTER_ID,
		(SELECT USER_NM FROM NEMPLYRINFO WHERE ESNTL_ID = A.FRST_REGISTER_ID)	FRST_REGISTER_NM,
		A.FRST_REGIST_PNTTM															FRST_REGISTER_PNTTM,
		A.LAST_UPDUSR_ID,
		A.LAST_UPDT_PNTTM
		FROM NONLINEPOLLRESULT A
		WHERE 1=1
		AND A.POLL_ID = #{pollId} 
		ORDER BY POLL_IEM_NM


	 
	</select>
	
    <!-- ?⑤씪?퇠OLL寃곌낵::??젣  -->
	<delete id="deleteOnlinePollResult">
		
			DELETE FROM NONLINEPOLLRESULT 
			WHERE POLL_RESULT_ID=#{pollResultId}
		
	</delete>


</mapper>
