<?xml version="1.0" encoding="UTF-8"?>
<!-- 
	?섏젙??                ?섏젙??                         ?섏젙?댁슜
  =========     =======    =================================================
  2011.8.12   	?덈???    	 	?뚯씠釉??쒖??붿뿉 ?곕Ⅸ ?섏젙?ы빆 諛섏쁺
							DELETE_YN	->	DELETE_AT
  2017.06.05    理쒕몢??         ibatis - > mybatis
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="NoteManage">
	
	<!-- 履쎌?愿由?:履쎌?愿由?議고쉶(?듬?泥섎━?? -->
	<select id="selectNoteManage" resultType="egovMap">
	 
	 SELECT
		A.NOTE_ID,
		(SELECT TRNSMITER_ID FROM NNOTETRNSMIT WHERE NOTE_ID = A.NOTE_ID LIMIT 1) TRNSMITER_ORG_ID,
		(SELECT USER_NM FROM COMVNUSERMASTER WHERE ESNTL_ID = (SELECT TRNSMITER_ID FROM NNOTETRNSMIT WHERE NOTE_ID = A.NOTE_ID LIMIT 1)) TRNSMITER_NM,
        (SELECT USER_ID FROM COMVNUSERMASTER WHERE ESNTL_ID = (SELECT TRNSMITER_ID FROM NNOTETRNSMIT WHERE NOTE_ID = A.NOTE_ID LIMIT 1)) TRNSMITER_ID,
        TO_CHAR((SELECT FRST_REGIST_PNTTM FROM NNOTETRNSMIT WHERE NOTE_ID = A.NOTE_ID LIMIT 1),'YYYY-mm-dd HH24:MI:SS') TRNSMITER_PNTTM,
		A.NOTE_SJ,
		A.NOTE_CN,
		A.ATCH_FILE_ID,
		A.FRST_REGISTER_ID,
		A.FRST_REGIST_PNTTM FRST_REGISTER_PNTTM,
		A.LAST_UPDUSR_ID,
		A.LAST_UPDT_PNTTM
	 FROM NNOTE A
	 WHERE 1=1 
	 AND A.NOTE_ID=#{noteId} 
	
	</select>
	
	<!-- 履쎌?愿由?:履쎌?愿由? ?깅줉  -->
	<insert id="insertNoteManage">	     
	  
		INSERT INTO NNOTE ( 
			NOTE_ID,
			NOTE_SJ,
			NOTE_CN,
			ATCH_FILE_ID,
			FRST_REGISTER_ID,
			FRST_REGIST_PNTTM,
			LAST_UPDUSR_ID,
			LAST_UPDT_PNTTM
		)VALUES(
			#{noteId},
			#{noteSj},
			#{noteCn},
			#{atchFileId},
			#{frstRegisterId},
			NOW(),
			#{frstRegisterId},
			NOW()
		)
	 
	</insert>

	<!-- 履쎌?愿由?:蹂대궦履쎌?  ?깅줉  -->
	<insert id="insertNoteTrnsmit">	     
	 
		INSERT INTO NNOTETRNSMIT ( 
			NOTE_ID,
			NOTE_TRNSMIT_ID,
			TRNSMITER_ID,
			DELETE_AT,
			FRST_REGISTER_ID,
			FRST_REGIST_PNTTM,
			LAST_UPDUSR_ID,
			LAST_UPDT_PNTTM
		)VALUES(
			#{noteId},
			#{noteTrnsmitId},
			#{trnsmiterId},
			'N',
			#{frstRegisterId},
			NOW(),
			#{frstRegisterId},
			NOW()
		)
	
	</insert>
	<!-- 履쎌?愿由?:諛쏆?履쎌?  ?깅줉  -->
	<insert id="insertNoteRecptn">	     
	
	INSERT INTO NNOTERECPTN(
		NOTE_ID,
		NOTE_TRNSMIT_ID,
		NOTE_RECPTN_ID,
		RCVER_ID,
		OPEN_YN,
		RECPTN_SE,
		FRST_REGISTER_ID,
		FRST_REGIST_PNTTM,
		LAST_UPDUSR_ID,
		LAST_UPDT_PNTTM
	)VALUES(
		#{noteId},
		#{noteTrnsmitId},
		#{noteRecptnId},
		#{rcverId},
		#{openYn},
		#{recptnSe},
		#{frstRegisterId},
		NOW(),
		#{frstRegisterId},
		NOW()
	)
	
	</insert>
	
	<!-- 履쎌?愿由?:?꾩씠??寃?? -->
	<select id="EovNoteEmpListPopup" parameterType="comDefaultVO" resultType="egovMap">
	
	SELECT 
		EMPLYR_ID,
		USER_NM EMPLYR_NM, 
		ESNTL_ID UNIQ_ID,
		SEXDSTN_CODE,
		BRTHDY,
		HOUSE_ADRES HOME_ADRES,
		DETAIL_ADRES,
		EMAIL_ADRES,
		OFFM_TELNO 
	FROM NEMPLYRINFO 
	WHERE 1=1 
	 
	 <if test="searchKeyword != null and searchKeyword != ''">
	 	
			<if test="searchCondition == 'USER_NM'">
			AND USER_NM LIKE CONCAT('%', #{searchKeyword}, '%')
			</if>
			<if test="searchCondition == 'EMPLYR_ID'">
			AND EMPLYR_ID LIKE CONCAT('%', #{searchKeyword}, '%')
			</if>
			<if test="searchCondition == 'OFFM_TELNO'">
			AND OFFM_TELNO LIKE CONCAT('%', #{searchKeyword}, '%')
			</if>
	 	
	 </if>
	 
		ORDER BY USER_NM DESC
		LIMIT #{recordCountPerPage} OFFSET #{firstIndex}
	
	</select>

	<select id="EovNoteEmpListPopupCnt" parameterType="comDefaultVO" resultType="int">
	
	SELECT 
		COUNT(*) totcnt
	FROM 
	     NEMPLYRINFO
	WHERE 1=1 
	 <if test="searchKeyword != null and searchKeyword != ''">
	 	
			<if test="searchCondition == 'USER_NM'">
			AND USER_NM LIKE CONCAT('%', #{searchKeyword}, '%')
			</if>
			<if test="searchCondition == 'EMPLYR_ID'">
			AND EMPLYR_ID LIKE CONCAT('%', #{searchKeyword}, '%')
			</if>
			<if test="searchCondition == 'OFFM_TELNO'">
			AND OFFM_TELNO LIKE CONCAT('%', #{searchKeyword}, '%')
			</if>
	 	
	 </if>
	</select>
</mapper>
