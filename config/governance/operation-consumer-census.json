{
  "schemaVersion": 1,
  "authority": "operation-consumer-census",
  "generatedAt": "2026-09-07",
  "notes": [
    "Every documented OpenAPI operation must be consumed by the frontend, mechanically derivable as a springdoc alias duplicate, or listed here with a category and a reason.",
    "bff-route-handler and non-browser-consumer entries are machine-verified: an evidence file must exist and reference the operation path suffix. A category label alone proves nothing.",
    "unwired entries are real debt. expected.unwiredMax is a monotonic ratchet — raising it is a visible diff, never a silent exception.",
    "Alias duplicates (springdoc _N suffixes over multi-path @RequestMapping) are derived, never listed. Listing one is an error.",
    "AXIS 2 (screen reachability): consumed on axis 1 only means a production TypeScript module calls the operation. Axis 2 additionally checks that the service method wrapping that call site is invoked from outside services/ and tests — the case that first exposed the gap was deleteRespondent, whose only caller was its own unit test.",
    "Axis 2 keeps no exception list on purpose: 77 entries would become a drawer (H2). It is a single monotonic ratchet (expected.screenOrphanMax) and prints the full offending list on failure, mirroring status-color-guard.",
    "Axis 2 is deliberately lenient, following UnreachableServiceLinter: a method counts as reachable if `.name(` appears anywhere in non-service production code. Common names may pass by collision — that direction misses violations rather than inventing them."
  ],
  "expected": {
    "operationCount": 371,
    "unwiredMax": 10,
    "screenOrphanMax": 32
  },
  "entries": [
    {
      "operationId": "checkHealth",
      "category": "unwired",
      "note": "Application-level liveness surface. PD-OPS-003 decided (2026-09-08) to keep the production compose healthcheck pointed at /actuator/health, which also covers registered indicators (DB, disk) — this endpoint only confirms the application answers. Retiring it was considered and deferred because external load balancers or monitoring may already target the path; that needs deployment-environment confirmation, not a repository decision.",
      "evidence": []
    },
    {
      "operationId": "checkIdDplct",
      "category": "unwired",
      "note": "Duplicate-id check that exists only to serve the signup flow. PD-AUTH-003 decided (2026-09-08) not to surface public self-signup, so this stays unwired by decision; it becomes reachable only if that decision is revisited.",
      "evidence": []
    },
    {
      "operationId": "confirmInformalSanction",
      "category": "superseded-surface",
      "note": "InformalSanctionApiController serves this domain at /api/v1/informal-sanctions (aliased at /api/v1/admin/system/ism). DEC-OPS-040 consolidated the ISM screen into /approvals, which calls ApprovalApiController instead; DEC-OPS-053 then removed the dead IsmAdminService. Both controllers wrap the same InformalSanctionService, so this is a superseded surface, not a missing feature. Neither controller is a subset of the other (approvals adds processed/task-types, ISM keeps detail/update), so retiring it is a backend design decision — see GAP-WIRING-001.",
      "supersededBy": [
        "getPending",
        "getMyHistory",
        "getProcessed",
        "createApproval",
        "confirm",
        "cancelApproval"
      ]
    },
    {
      "operationId": "createNotification",
      "category": "unwired",
      "note": "Self-notification creation for the authenticated principal. Admin dispatch to other users goes through the separate /admin/notifications/dispatch operation (DEC-OPS-042), which is wired.",
      "evidence": []
    },
    {
      "operationId": "deleteClCode",
      "category": "superseded-surface",
      "note": "Common code classification delete. The server performs a logical delete (CommonCodeCategory.delete() sets useYn='N'), and the classification edit form already exposes a use-flag toggle that reaches the same state through updateClCode (CommonCodeCategory.update(..., useYn, ...)). update is in fact the better path because it also records lastMdfrId. DEC-OPS-068 verified both entity methods before retiring the dead frontend delegate; the HTTP surface stays.",
      "supersededBy": [
        "updateClCode"
      ]
    },
    {
      "operationId": "deleteCmmnCode",
      "category": "superseded-surface",
      "note": "Common code group delete. Same shape as deleteClCode — CommonCodeGroup.delete() sets useYn='N' and the group edit form reaches the same state through updateCmmnCode, which additionally records lastMdfrId. The dead frontend delegate was retired in DEC-OPS-068 after checking both entity methods; the HTTP surface stays.",
      "supersededBy": [
        "updateCmmnCode"
      ]
    },
    {
      "operationId": "deleteInformalSanction",
      "category": "superseded-surface",
      "note": "InformalSanctionApiController serves this domain at /api/v1/informal-sanctions (aliased at /api/v1/admin/system/ism). DEC-OPS-040 consolidated the ISM screen into /approvals, which calls ApprovalApiController instead; DEC-OPS-053 then removed the dead IsmAdminService. Both controllers wrap the same InformalSanctionService, so this is a superseded surface, not a missing feature. Neither controller is a subset of the other (approvals adds processed/task-types, ISM keeps detail/update), so retiring it is a backend design decision — see GAP-WIRING-001.",
      "supersededBy": [
        "getPending",
        "getMyHistory",
        "getProcessed",
        "createApproval",
        "confirm",
        "cancelApproval"
      ]
    },
    {
      "operationId": "getConnectStats",
      "category": "unwired",
      "note": "User-facing connection statistics; the stats screens consume the admin statistics operations.",
      "evidence": []
    },
    {
      "operationId": "getInformalSanction",
      "category": "superseded-surface",
      "note": "InformalSanctionApiController serves this domain at /api/v1/informal-sanctions (aliased at /api/v1/admin/system/ism). DEC-OPS-040 consolidated the ISM screen into /approvals, which calls ApprovalApiController instead; DEC-OPS-053 then removed the dead IsmAdminService. Both controllers wrap the same InformalSanctionService, so this is a superseded surface, not a missing feature. Neither controller is a subset of the other (approvals adds processed/task-types, ISM keeps detail/update), so retiring it is a backend design decision — see GAP-WIRING-001.",
      "supersededBy": [
        "getPending",
        "getMyHistory",
        "getProcessed",
        "createApproval",
        "confirm",
        "cancelApproval"
      ]
    },
    {
      "operationId": "getInformalSanctionList",
      "category": "superseded-surface",
      "note": "InformalSanctionApiController serves this domain at /api/v1/informal-sanctions (aliased at /api/v1/admin/system/ism). DEC-OPS-040 consolidated the ISM screen into /approvals, which calls ApprovalApiController instead; DEC-OPS-053 then removed the dead IsmAdminService. Both controllers wrap the same InformalSanctionService, so this is a superseded surface, not a missing feature. Neither controller is a subset of the other (approvals adds processed/task-types, ISM keeps detail/update), so retiring it is a backend design decision — see GAP-WIRING-001.",
      "supersededBy": [
        "getPending",
        "getMyHistory",
        "getProcessed",
        "createApproval",
        "confirm",
        "cancelApproval"
      ]
    },
    {
      "operationId": "getInstitutionCodeDetail",
      "category": "unwired",
      "note": "Institution code detail. PD-CODE-001 decided (2026-09-08) to keep the receive log read-only: the ledger write path stays unbuilt because chg_se_cd has no authoritative value domain (the screen reads 1/2/3, this module's tests use \"I\", and there is no seed, enum or contract). Interpreting it without a spec would overwrite or delete core data. Reopening requires the standard/供給처 specification — see GAP-CODE-001.",
      "evidence": []
    },
    {
      "operationId": "getMenuCreationList",
      "category": "unwired",
      "note": "Menu creation candidates by authority code; the menu admin screen builds its tree from the standard menu list.",
      "evidence": []
    },
    {
      "operationId": "getNotification",
      "category": "unwired",
      "note": "Notification detail — the notification centre renders from the list response and never fetches a single item.",
      "evidence": []
    },
    {
      "operationId": "getScheduleList",
      "category": "unwired",
      "note": "Flat schedule list. The schedule screens query the department-scoped endpoints instead.",
      "evidence": []
    },
    {
      "operationId": "login",
      "category": "bff-route-handler",
      "note": "Next route handler proxies login server-side so the refresh token never reaches browser JavaScript; the browser calls /api/auth/login, not the backend operation.",
      "evidence": [
        "frontend/src/app/api/auth/login/route.ts"
      ]
    },
    {
      "operationId": "logout",
      "category": "bff-route-handler",
      "note": "Same BFF boundary as login — the route handler clears the HttpOnly cookies it set.",
      "evidence": [
        "frontend/src/app/api/auth/logout/route.ts"
      ]
    },
    {
      "operationId": "registerInformalSanction",
      "category": "superseded-surface",
      "note": "InformalSanctionApiController serves this domain at /api/v1/informal-sanctions (aliased at /api/v1/admin/system/ism). DEC-OPS-040 consolidated the ISM screen into /approvals, which calls ApprovalApiController instead; DEC-OPS-053 then removed the dead IsmAdminService. Both controllers wrap the same InformalSanctionService, so this is a superseded surface, not a missing feature. Neither controller is a subset of the other (approvals adds processed/task-types, ISM keeps detail/update), so retiring it is a backend design decision — see GAP-WIRING-001.",
      "supersededBy": [
        "getPending",
        "getMyHistory",
        "getProcessed",
        "createApproval",
        "confirm",
        "cancelApproval"
      ]
    },
    {
      "operationId": "reissue",
      "category": "bff-route-handler",
      "note": "Token reissue runs server-side in the route handler; the refresh token is never exposed to the client bundle.",
      "evidence": [
        "frontend/src/app/api/auth/reissue/route.ts"
      ]
    },
    {
      "operationId": "signup",
      "category": "unwired",
      "note": "Public self-signup. PD-AUTH-003 decided (2026-09-08) to keep admin provisioning only: opening it would require captcha, email verification and an approval queue — new feature work, not wiring — and does not match the current internal-enterprise product shape. The endpoint stays frozen by SignupContractLinter (the service pins Role.USER, so no privilege escalation), and this entry records the decision rather than an unexamined gap.",
      "evidence": []
    },
    {
      "operationId": "triggerError",
      "category": "unwired",
      "note": "Forced-500 diagnostic, bean-excluded under @Profile(\"!prod\"). Invoked by hand when verifying GlobalExceptionHandler; no automated consumer.",
      "evidence": []
    },
    {
      "operationId": "updateInformalSanction",
      "category": "superseded-surface",
      "note": "InformalSanctionApiController serves this domain at /api/v1/informal-sanctions (aliased at /api/v1/admin/system/ism). DEC-OPS-040 consolidated the ISM screen into /approvals, which calls ApprovalApiController instead; DEC-OPS-053 then removed the dead IsmAdminService. Both controllers wrap the same InformalSanctionService, so this is a superseded surface, not a missing feature. Neither controller is a subset of the other (approvals adds processed/task-types, ISM keeps detail/update), so retiring it is a backend design decision — see GAP-WIRING-001.",
      "supersededBy": [
        "getPending",
        "getMyHistory",
        "getProcessed",
        "createApproval",
        "confirm",
        "cancelApproval"
      ]
    }
  ]
}
