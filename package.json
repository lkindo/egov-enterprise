{
  "name": "egov-enterprise-root",
  "version": "1.0.0",
  "private": true,
  "//engines": "[W1-H2] 루트 스크립트(scripts/dev.mjs·verify.mjs)도 Node 로 실행된다. frontend 에만 engines 를 걸면 루트에서 EOL Node 20 으로 도구를 돌리는 스큐가 그대로 남는다. .nvmrc(22)와 같은 축으로 고정한다.",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "node --env-file-if-exists=.env scripts/dev.mjs",
    "//dev": "루트 .env 를 로드한 뒤 백엔드·프론트를 같은 환경에서 낳는다. 한쪽만 JWT_SECRET 을 받으면 서명검증이 전량 실패해 '로그인 후 로그인창' 무한루프가 되므로(2026-07-19 발생) 대칭 주입을 스크립트로 강제한다. 상세는 scripts/dev.mjs 주석.",
    "backend": "gradlew.bat :api-server:bootRun",
    "frontend": "pnpm -C frontend dev",
    "//separate-launch": "backend/frontend 를 따로 띄우면 JWT_SECRET 대칭이 보장되지 않는다. 양쪽 기동 로그의 시크릿 지문(sha256 앞 8자)이 같은지 확인하라.",
    "all": "npm run dev",
    "//gradle": "Windows-primary: gradlew.bat 로 통일(dev 와 일관). 크로스플랫폼 통합 게이트는 'npm run verify'(node scripts/verify.mjs — OS 감지) 를 진입점으로 사용하라.",
    "test": "npx cross-env TZ=Asia/Seoul gradlew.bat test -Dfile.encoding=UTF-8",
    "test:ci": "npx cross-env TZ=Asia/Seoul gradlew.bat test --continue -Dfile.encoding=UTF-8",
    "test:coverage": "npx cross-env TZ=Asia/Seoul gradlew.bat test jacocoRootCoverageVerification --continue -Dfile.encoding=UTF-8",
    "test:db-bridge": "node --test .agent/scripts/db-bridge.test.js",
    "test:ci-scope": "node --test scripts/ci-change-scope.test.mjs",
    "test:governance-gates": "node --test scripts/governance-gates-contract.test.mjs",
    "test:operational-contracts": "node --test \"scripts/*.test.mjs\" \".agent/scripts/*.test.js\"",
    "test:required-checks": "node --test scripts/required-checks-contract.test.mjs",
    "test:shared-memory": "node --test scripts/shared-memory-contract.test.mjs",
    "test:base-profile": "node --test scripts/reusable-base-census.test.mjs",
    "ui-quality:baseline:build": "node --test scripts/ui-quality-baseline-build-contract.test.mjs && node scripts/ui-quality-baseline-build.mjs",
    "ui-quality:baseline:launch": "node scripts/ui-quality-baseline-launch.mjs",
    "base:census": "node scripts/reusable-base-census.mjs --check",
    "base:generate-db": "node scripts/generate-reusable-base-db.mjs",
    "base:generate-source": "node scripts/generate-reusable-base-source.mjs",
    "verify": "node scripts/verify.mjs full",
    "verify:docs": "node scripts/verify.mjs docs",
    "verify:fast": "node scripts/verify.mjs fast",
    "verify:push": "node scripts/verify.mjs push",
    "verify:full": "node scripts/verify.mjs full",
    "verify:be": "node scripts/verify.mjs be",
    "verify:fe": "node scripts/verify.mjs fe",
    "verify:e2e": "node scripts/verify.mjs e2e",
    "verify:ops": "node scripts/verify.mjs ops",
    "//verify:ops": ".github/required-checks.json ↔ ci.yml 잡·매트릭스 ↔ GitHub 실제 브랜치 보호 설정 대조. required status check 가 빠지면 하네스가 강제력 없는 권고로 강등되므로 병합·릴리스 전에 명시 실행한다. ruleset 조회에 admin 읽기 권한이 필요해 `verify all` 에는 넣지 않았다(AGENTS H5: 조용한 skip 금지 → 별도 스코프)."
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "ajv": "8.20.0"
  }
}
