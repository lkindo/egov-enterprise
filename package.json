{
  "name": "egov-enterprise-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx -y concurrently -n \"API,WEB\" -c \"blue,green\" \"gradlew :api-server:bootRun\" \"pnpm -C frontend dev\"",
    "backend": "gradlew :api-server:bootRun",
    "frontend": "pnpm -C frontend dev",
    "all": "npm run dev"
  },
  "dependencies": {
    "pg": "^8.20.0"
  }
}
