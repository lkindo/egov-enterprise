{
  "name": "egov-enterprise-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx -y concurrently -n \"API,WEB\" -c \"blue,green\" \"./gradlew bootRun\" \"pnpm --prefix frontend dev\"",
    "backend": "./gradlew bootRun",
    "frontend": "pnpm --prefix frontend dev"
  }
}
